services:
  - type: web
    name: onichase-v2-room-server
    runtime: python
    plan: free
    rootDir: .
    buildCommand: ""
    startCommand: bash -lc "python3 scripts/engine/v2_online_room_server.py --host 0.0.0.0 --port ${PORT:-8765}"
