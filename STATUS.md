# STATUS

## Current Focus
Turn the expanded `v4` nationwide rail data into a stable gameplay/playtest checkpoint.

## Done
- `v1` is archived as the early Yamanote real-data prototype.
- Main `v2` remains the nationwide Shinkansen playable build with planning/live/capture/replay and Render-backed online rooms.
- Public `v3` is frozen as the Tokyo MapLibre release-candidate baseline at `https://eigenoperator.github.io/OniChase/v3.html`.
- `v3` baseline data: `1804` station groups, `2194` physical stations, `113` service routes, `4612` track centerlines, and `41186` v2-compatible trip instances.
- `v3` core suite passes `30` tests; remaining `9` external/homonym data edges are documented for v4.
- `v4` physical map builds from local MLIT N02-2024 with `10239` physical stations, `9052` station groups, `21932` track centerlines, and `178` operators after manual physical-station overrides.
- `station_identity_v2` preserves physical station coordinates while grouping gameplay/interchange identity by N02 group code first; same-name stations are not globally collapsed.
- Latest v4 identity audit reports `427` same-name split names, `0` multi-name station groups, and `0` line coverage warnings.
- Public `v4.html` displays the nationwide physical railway map with MapLibre, Japan outline layers, overview/detail track switching, station labels, optional physical station dots, line search, and selected-line highlighting.
- v4 line inventory lists `596` operator-line pairs, `552` unique line names, `178` operators, `0` stationless lines, and `0` trackless lines.
- v4 track-continuity audit uses 500m endpoint snap and reports `6` reviewed multi-component operator-line pairs, `0` unreviewed.
- v4 line rendering uses line-level color when known and falls back to company color; current inventory marks `152` line-specific colors and `444` company fallbacks.
- Current v4 structured train collection has `126176` weekday train instances across `22` source collections and `178` operators after the global station-match-distance quality gate.
- v4 train health audit is clean on service ids and stop validity: `0` duplicate service ids, `1` duplicate signature group, `0` short trains, `0` missing station-group stops, `0` missing physical-station stops, and `0` bad time-order trains.
- v4 gameplay bundle now exports `115924` playable compact trips, `600` service routes/patterns, and skips `309` short gameplay trips after compact dedupe.
- v4 room-server bundle now has `9052` station groups and `120420` SQLite-backed trips.
- v4 planner corridor audit passes the first `6` major hub checks with `0` warnings.
- Local v4 browser probes pass axioms/entry/replay checks and confirm stop-level display routes: 東京/神田/秋葉原 no longer expose stray `東北線`, 鎌倉 exposes `湘南新宿ライン` instead of raw `東北線`, 鎌倉 `横須賀線・総武快速線` now stitches same-operator Tokyo splits such as `492S -> 493F` through to 千葉, 御茶ノ水/新宿 split orange `中央線快速` from yellow `中央・総武線各駅停車`, and 立川 splits `青梅線`/`中央線` by outgoing segment.
- v4 route highlighting now filters JR East `東海道線` station halos to the actual Tokyo-side operating-route stations, so clicking `東海道線` at 東京 no longer lights the Yokosuka/Sobu rapid branch-only stations `新川崎`, `武蔵小杉`, `西大井`, `保土ヶ谷`, or `東戸塚`. The underlying MLIT physical data still names that 品鶴線/横須賀線-side infrastructure as `東海道線`, so the UI applies an explicit operating-route highlight filter.
- v4 selected-train path highlighting now only trusts the train's own future trace plus explicit reviewed path hints; it no longer uses scored candidate physical routes to guess a train path. Reviewed 大阪/天王寺/関西空港 hints split airport-bound no-stop segments into 大阪環状線 via 西九条, 阪和線 via 日根野, and 関西空港線. The regression audit verifies the first `40` 大阪→関西空港 cases, including `はるか`, highlight the west side and do not leak `東海道線`.
- Browser-stitched v4 through trains are now trace-authoritative for selected-train highlighting: primary route endpoint coverage can no longer collapse away recorded current/downstream trace sections. The highlight audit now fails if a stitched through train drops any recorded future trace route.
- v4 train/path highlights are now governed by the continuity axiom: every player-facing selected-train path must be one continuous physical or reviewed path chain. Station-pair route slices are rejected unless both endpoints anchor to their stops, adjacent same-route ranges are stitched through their boundary station, and the highlight audit now checks `355591` selected-path start cases for disconnected segment gaps. Corridor-alias through trains such as 北陸新幹線/上越新幹線 now resolve each selected future stop pair to a real physical shinkansen segment for path and station halos, so a train selected at 高崎 keeps the current 上越新幹線 section before continuing through 大宮/上野/東京. A reviewed correction maps erroneous Sky Access `京成高砂 -> 東松戸` `京成金町線` traces to `京成成田空港線` for highlight continuity without broadening route-choice rows to ordinary 北総線 trains.
- v4 through-service display audit covers all browser-stitched trips: `678` stitched through trips, `0` stale `東海道線+総武線` / `総武線+東海道線` labels, and expected labels for `横須賀線・総武快速線`, `京急`/`都営浅草線`, and `京成`/`都営浅草線`.
- Reusable v4 through-service audit reports `1026` split-trip candidates: `942` covered by existing browser rules, `52` confirmed direct-service rules, `26` reviewed likely reused-number/data-context non-UI cases, and `6` still needing review.
- v4 route-choice QA now globally scans all `9052` station groups for segment mismatches, virtual-corridor leaks, generic `路線`, remote Yokohama through routes/labels, selected-train label violations, JR East northern-trunk scope, major-station Shinkansen visibility, transfer-equivalent station group route completeness, reviewed limited-express train-number presence, and post-train-selection alighting availability; latest route-choice scan reports `1624259` rows/choices/labels and `0` anomalies.
- v4 Akita/Tohoku Shinkansen planning now follows the coupled-service axiom: on the Tokyo-Morioka shared segment, `はやぶさ・こまち` and related Tohoku/Hokkaido/Akita coupled movements appear as one player-facing corridor `東北・北海道・秋田新幹線`; after Morioka, Akita-side departures return to `秋田新幹線` and northbound Tohoku/Hokkaido departures remain `東北・北海道新幹線`. Route-choice regression now requires Tokyo `東北・北海道・秋田新幹線` plus Komachi numbers `1/3/7/9/23`; selected-train highlighting trusts stitched recorded traces so `こまち` remains continuous across `東北・北海道新幹線 -> 田沢湖線 -> 奥羽線`.
- v4 sparse-route and Akita/Tohoku Shinkansen regression pass is green after the latest browser fix: full route-choice audit scanned `9052` station groups, `111175` trips, and `1589722` route-choice/label checks with `0` anomalies; the focused Morioka probe confirms `こまち` toward 田沢湖/秋田 is `秋田新幹線` while shared-segment returns remain `東北・北海道・秋田新幹線`. Earlier selected-train highlight chunk `0-30000` reported `0` failures, planner interaction reported `0` failures, and nationwide sparse route-choice chunks over all `9052` station groups report `278` sparse choices with `0` suspicious.
- v4 nationwide hub data audit now checks `21` player-relevant hubs from 東京 through 札幌/那覇空港 with route-count floors and same-name disambiguation guards; latest run reports `21/21` hubs checked and `0` anomalies.
- v4 long-distance playability audit now runs `100` fixed-class cases (`30` hand-authored plus `70` novelty-biased coverage cases) and `100` seeded random route-chain cases by default, then audits every reached origin/transfer/final station surface; latest expanded stress pass ran two `150` fixed + `300` random batches (`900/900` playable total), checking `3215` waypoint station surfaces, `1554` unique waypoint stations, `310` unique leg routes, and `0` anomalies.
- v4 subagent blind-route challenge audit can now load an external JSON case file; the first 20 subagent-authored routes pass `16/20` as written, with the 4 misses isolating adjacent-waypoint gaps on 新潟→小出, 智頭→若桜, 徳島→海部, and 八代→人吉.
- v4 expanded subagent blind-route challenge set now has `200` externally authored cases covering about `476` station names and `185` route hints; latest run passes `172/200`, audits `623` waypoint surfaces, covers `441` unique waypoint stations and `198` unique leg routes, with the remaining `28` failures classified as station-disambiguation/name issues, BRT/DMV/substitute-service edges, reality-disconnected lines, or private/third-sector boundary gaps needing targeted review.
- v4 subagent challenge cleanup now records current station-name guidance without treating different-name nearby stations as station-equivalent transfers. Confirmed station-name fixes include Nishitetsu `西鉄福岡（天神）` and Yui Rail `県庁前` with explicit Okinawa station identity; different-name walking transfers such as `龍ケ崎市`/`佐貫`, `西武秩父`/`御花畑`, and `空港第2ビル`/`東成田` remain unsupported in the current station-boundary model.
- JR West duplicate collection merging now preserves the line-specific context for 湖西線 train variants instead of letting same-train-number 東海道線/山陽線/北陸線 variants mask them. 湖西線 source coverage is now `192` repaired JR West official trains after source-level split-column stitching; browser/gameplay probes show ordinary 湖西線 departures at 近江今津, 堅田, 山科, and 近江舞子.
- JR West official 湖西線 multi-column timetable rows are now stitched at source when adjacent columns share the same official train page, boundary station, and a `0-15` minute boundary gap. The collector currently stitches `19` reviewed chains from `39` source rows, including `3891M+3811M+3411M` 敦賀->姫路, `3143M+3443M` 敦賀->姫路, `3438M+3138M` 姫路->敦賀, `4841M+128M` 近江今津->米原, and `143M+4848M` 長浜->近江今津. `scripts/tests/v4_jrwest_kosei_split_stitch_audit.js` keeps those chains from regressing in the gameplay bundle.
- JR West `サンダーバード` trace identity now follows the actual Osaka/Kyoto/Tsuruga service path instead of the source timetable tab or endpoint station lines: Osaka-Kyoto is `東海道線`, Kyoto-Tsuruga is `湖西線`, including no-stop pairs such as 京都/敦賀 and 敦賀/近江今津. The builder disables source-line terminal-tail preservation for limited express trains, and `scripts/tests/v4_jrwest_thunderbird_trace_audit.js` verifies all `33` current Thunderbird trips.
- v4 now has a report-only nationwide limited-express/long-distance trace suspect audit at `scripts/tests/v4_limited_express_trace_suspect_audit.js`. Latest run scanned `115924` compact trips, identified `3022` named/shinkansen/limited-express candidates, checked `30984` adjacent stop pairs, and wrote `data/v4_limited_express_trace_suspect_audit.json` with `3386` suspects. The report distinguishes normal/low-priority virtual-shinkansen skip pairs (`3007`) and reviewed Thunderbird 湖西線 long physical segments (`27`) from higher-priority conventional trace risks such as `ひたち` 上野/水戸 long stop-pair traces.
- v4 gameplay line-trace identity now keeps a source line on 1-2 segment terminal tails when the raw timetable still labels those endpoint stops as that source line. This restores 奈良線 at 奈良 for 京都-奈良 trains: browser probes show `601M` and `602M` as all-奈良線 through 京都/木津/平城山/奈良, and a nationwide same-source-line short-endpoint audit reviewed `5464` short endpoint shapes with `0` remaining same-class issues.
- v4 planning interaction now keeps the current-plan board unchanged while turning the lower decision area into a paged flow: `1/3` line choice, `2/3` train choice, and `3/3` destination choice, with back navigation between pages. Local Playwright verification covered route selection, train selection, and both back paths with no page errors.
- v4 planning pages now require an explicit line click before advancing from `1/3` to `2/3`; single-line stations no longer skip the line-choice page just because a matching route is already highlighted. Playwright verified `東羽衣` starts on `1/3` with one `阪和線` choice and only advances after that row is clicked.
- JR West official multi-column train pages now parse each coupled column as its own train portion and inherit the shared segment for split/join portions. This fixes `関空快速・紀州路快速`: browser probes at 天王寺 now collapse the pair into umbrella rows such as `13:15 関空快速・紀州路快速`, then offer `関空快速 -> 関西空港` and `紀州路快速 -> 和歌山` as portions. The earlier `関西空港線` duplicate-priority workaround was removed from current-collection merging; only the reviewed 湖西線 priority remains.
- v4 selected-train labels now apply the ordinary direction-side through-running rule to 名古屋鉄道 as well, including airport-branch services, and use plain labels such as `名鉄空港線` without parentheses.
- v4 branded-prefix transfer audit now scans `65` JR/private candidate interchanges: `4` reviewed direct including `蒲田`/`京急蒲田`, `48` active unreviewed direct, and `13` nearby walking candidates needing review.
- v4 v3-release-candidate quality audit now scans all old-source lines still retained in v4: `39605` retained trains across `108` lines, with `291` hard anomalies all from Tokyo Metro Chiyoda old Shinjuku-group rows rematched onto Marunouchi Shinjuku.
- v4 coupled split/join registry is now wired into the website: paired same-station departures on a shared segment and official combined train-title rows collapse into an umbrella `A・B` row, then require a second portion choice before downstream stops; coupled trip pairs also count as `same_train` during their detected shared time window.
- v4 coupled-service audit now finds gameplay evidence for `15/18` registered families: `12` with paired trip evidence, `3` with official combined-title evidence, `2` scoped not-applicable for the current weekday source, `1` Sunrise cross-source no-pair case, and `0` missing service portions. The JR West fix now represents `関空快速・紀州路快速` as true split portions rather than an inferred label-only case.
- v4 limited-express UI regression checks now explicitly cover the reviewed train-number cases at 敦賀, 京都, 新大阪, 白浜, 新宿, 大船, 成田空港, 松本, and 大月, including `サンダーバード`, `はるか`, `くろしお`, `成田エクスプレス`, `あずさ`, `かいじ`, and `富士回遊`.
- AXIOMS now requires every concrete station/line/train/operator defect to be generalized into a nationwide audit/search and same-class fix, not treated as a local-only patch.
- v4 transfer-equivalent matching now caps reviewed direct interchange pairs at `5km`, preventing broad name pairs such as `JR小倉`/`小倉` from linking unrelated same-name stations across regions.
- v4 load-performance issue was traced to coupled same-train equivalence initialization, not network fetch: local browser profiling showed timetable readiness stuck around `45s`, with `buildCoupledTripEquivalences` alone taking about `44.5s`. The website now caches per-trip coupled search labels/station sets and buckets candidate comparisons by split/join station and time; the same local path is now about `5.3s` total (`~2.0s` map ready, `~3.3s` timetable ready). The map bundle was also slimmed by deriving `serviceGeometry` from `trackCenterlines`, reducing `docs/data/v4_gameplay_map_bundle.json.gz` from about `12.7M` to `9.2M`.
- v4 first-map load was further profiled and optimized: the remaining initial delay came from the map bundle and third-party map assets. The gameplay builder no longer repeats per-track `stationGroupIds` or legacy `gameNodes`, reducing `docs/data/v4_gameplay_map_bundle.json.gz` to about `4.9M` (`~28.1M` JSON text). MapLibre JS/CSS and the first OpenMapTiles glyph PBFs are now vendored under `docs/assets/vendor`, removing first-load dependency on `unpkg.com` and `fonts.openmaptiles.org`. Local browser timing now shows DOM ready around `45ms` and map bundle ready around `1.35s`; route-choice audit remains clean.
- Pages web bundle was slimmed by pruning unreferenced `docs/data` artifacts; local `docs/` now builds to about `44M` while v4 still loads and passes the 青梅 probe.
- Render v4 room-server deployment is forbidden unless Scorp explicitly reverses the axiom; push-triggered server deploy is disabled after repeated failures.

## In Progress
- Promote the v4 nationwide gameplay bundle into a public playtest pass.
- Continue hardening the remaining Odakyu coupled-service gaps by adding a weekend/holiday timetable source or explicitly scoping those registry entries away from weekday gameplay.
- Review whether the meter-scale 肥薩線 N02 fragment should remain visible or be hidden in rendered geometry.

## Blockers
- Raw MLIT N02-2024 source files are local-only and ignored by git; regenerated v4 artifacts require those local files or a documented download step.
- Large collection caches remain local-only and should not be committed.

## Decisions
- [2026-04-07] GIS-first Shinkansen became the main `v2`; old prototypes stay archived.
- [2026-04-13] Physical map stations must keep real locations; gameplay station groups may allow transfers without faking map coordinates.
- [2026-04-24] `v3` is frozen as a release candidate; remaining external/homonym identity work belongs to `v4`.
- [2026-04-25] `v4` starts from nationwide N02 physical geometry plus `station_identity_v2`, not from another Tokyo-only hand-built map.
- [2026-04-25] v4 timetable stop matching should reuse the v3 station alias/station-group method, not invent a separate identity system.

## Next
1. Triage `data/v4_limited_express_trace_suspect_audit.json` by family, starting with conventional non-shinkansen high-priority groups such as `ひたち` before treating normal virtual-shinkansen skip pairs as reviewed.
2. Decide whether v4 gameplay should ingest weekend/holiday Odakyu Romancecar sources so `はこね・えのしま` and `メトロはこね・メトロえのしま` can be represented when they do not exist in the current weekday source.
3. Review the remaining Yamagata Shinkansen source gap: current v3-rematched data keeps `つばさ` Tokyo-Fukushima B segments and Fukushima-Yamagata/Shinjo M segments, but not enough same-number pairs to synthesize full Tokyo-Yamagata/Shinjo trips without re-ingesting or repairing the source collection.
4. Continue the remaining `24/200` blind-route failures by separating true timetable gaps from wrong hand-authored waypoints; priority suspects are Shibayama Railway, Shimabara Railway, Matsuura Railway, Toyama Chitetsu rail, and several same-name/route-title disambiguation cases.
