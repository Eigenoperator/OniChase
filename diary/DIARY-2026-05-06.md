# DIARY - 2026-05-06

今天先做项目状态收口和 GitHub 同步准备。昨天的 v4 工作已经形成一个比较清楚的 checkpoint：mini-Shinkansen 的公共线别和物理 trace 身份拆开，`つばさ`/`こまち` 不再漏进普通 `奥羽線`/`田沢湖線`；route-choice audit 被拆成可控阶段并能稳定跑完整全国；limited-express trace suspect 继续清理到 `0` unreviewed suspects；Sunrise 也补进了非新干线直挂/併结规则。

Sunrise 的最后一次修正很重要：不是只让东京 route choice 从三个变成一个，还要检查玩家实际看到的 2/3 车次页。现在 `サンライズ瀬戸・出雲` 在东京显示为一个 coupled row，label 使用官方口径 `サンライズ瀬戸・出雲`，portion 分别是 `サンライズ瀬戸 -> 高松` 和 `サンライズ出雲 -> 出雲市`。这件事已经进了 `v4_route_choice_audit.js` 的 focused stage，避免以后只检查数据存在却漏掉 UI 文案。

今天同步时保留了本地大缓存目录不入库，只提交实际代码、数据 bundle、状态文档、diary 和 memory。当前验证项包括 `node --check scripts/tests/v4_route_choice_audit.js`、`git diff --check`、以及 Sunrise focused audit `0 anomalies`。

随后把 Sunrise 从东京单点回归扩成了双向/中间站审计。盲扫发现两个边界：第一，`高松` 这个站名全国有多个同名站，测试必须明确用香川县 JR 四国的 `高松`，否则会误查到石川县七尾线或东京多摩单轨的同名站；第二，`岡山` 当前站本身就是 Sunrise 的分岐/合流站，旧的 coupled row 构造只处理“未来会经过分岐站”的情况，所以在分岐站本身会漏掉 umbrella。现在 split/join 判断允许当前站作为 evidence，dedupe 也优先保留 Sunrise synthetic portion，避免共享段碎片吃掉瀬戸/出雲 portion。

新的 focused audit 覆盖 `東京`、`横浜`、香川县 `高松`、`出雲市`、`岡山`：东京必须只有一个 `サンライズ瀬戸・出雲` row；横滨要同时看到下行 coupled row 和上行东京方向；高松/出雲市上行必须分别能去东京；冈山必须保留分岐方向的 `サンライズ瀬戸・出雲` umbrella；季节 `91/92` 仍不得进入普通 gameplay。最新 `v4_route_choice_audit.js --stages focused` 为 `0 anomalies`。
