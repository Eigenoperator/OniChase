## 2026-04-20

- 今天把 `v3` 从“真实东京 MapLibre 地图 + 本地 gameplay shell”推进到“可以接多人房间”的第一版：页面入口现在明确分成单人和多人，多人可以创建/加入房间、复制房间号、选择 Runner/Hunter、ready/unready，并在主界面看到 Room 面板。
- 服务端方向也同步打开了：`scripts/engine/v2_online_room_server.py` 已经支持 `--dataset v3-tokyo`，所以 v3 多人不是另写一套玩法，而是继续复用 v2 的 room / planning / live / capture payload，只换成东京真实地图和真实 timetable 数据。
- 本地验证通过：单人可以进 v3、选车、选下车站并进入 Live；双人可以创建房间、加入、双方 ready、进入对局，再从顶栏进入 Live。下一步是给 v3 Tokyo room server 找线上部署地址，再把 public config 指过去。
- 下午继续把部署路径补齐：`render.yaml` 现在声明独立的 `onichase-v3-room-server`，启动命令固定为 `--dataset v3-tokyo`，public v3 config 已经指向预期的 HTTPS endpoint。当前本地 v3 health 验证通过，线上 endpoint 还需要 Render 真正创建/同步服务后才会从 `Not Found` 变成可用。
