# DIARY - 2026-06-10

今天先做 repo 盘点，不动重型 bundle。结论是：git 很干净，V5 仍是活跃主线且 readiness 证据为绿，但 Scorp 决定先回头修 V4。

关键原因是 `STATUS.md` / release docs 和当前 checked-in audit JSON 不一致：`data/v4_route_choice_audit_release.json` 现在有 `anomalyCount: 2`，`data/v4_selected_train_highlight_release.json` 有 `failureCount: 200`，`data/v4_data_quality_audit.json` 有 `errorCount: 1`。

这些红点集中在三类：`埼京線` route-choice identity、selected-train endpoint/path coverage（`越後湯沢`、`熱海`、`尾久`、`佐和`、`田端` 等），以及 八王子 `むさしの号` station coverage。

下一步不继续扩 V5，先做 V4 修复 triage：对齐状态文档和 release evidence，然后修这三类 audit，再跑 V4 gate。V5 保持当前 readiness 状态，whole-data readiness 和 plan-tail anchor gate 后续继续作为 release gate 候选。

下午把 V4 先修了一轮。route-choice 当前重跑只剩 上野→日暮里 `常磐線`，这是乘客视角的常磐线选择跑在北侧 trunk 物理轨上的合法情况，所以把 audit 放宽成精确站对例外；随后 route-choice release evidence 到 `anomalyCount: 0`，data-quality 也回到 `0 error / 0 warning`。

selected-train highlight 的旧红点已经不是当前主因。新的 current scan 暴露出缺首段几何和近铁特急跨线跳站问题：上越线、近铁奈良/大阪/名古屋/山田线在某些 first-hop 或 reviewed skip 上没有把 boarding stop 覆盖进去。修法是给非严格几何 trip 加 bounded station-pair bridge，并给近铁特急补经 伊勢中川、生駒、大和八木、伊勢市、近鉄四日市 的 reviewed path hints。8 shard 全量 selected-highlight 当前扫了 `110489` trips，`failureCount: 0`。

还留一个 follow-up：selected 修完后 route-choice global stage 仍是 `0` anomalies，但 `--stages duplicates` 单独跑 180 秒超时。这个要作为 audit performance 问题继续查，不能把 full default route-choice script 的运行时间也说成绿。

随后把这个 follow-up 也修了。原因不是数据红，而是 duplicates scan 一次扫 9052 个 station 太慢；给 route-choice audit runner 加了 station chunk 路径，默认每 1000 站一块、复用同一个加载好的页面。duplicates-only 现在约 82 秒扫完，`duplicateStationTitleCount: 0`；full default route-choice gate 约 255 秒跑完所有 stage，`anomalyCount: 0`。这次可以更踏实地说 V4 当前 gate 绿了。
