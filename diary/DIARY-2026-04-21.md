## 2026-04-21

- 今天把 v3 从“能跑的东京地图页”继续往真实可测的多人版本推进了一大步。`v3.html` 已经稳定作为官方 MapLibre 东京页：真实东京 map bundle、真实 timetable bundle、v2-style gameplay shell、单人/多人入口、Ready Room、三层 planning 列表、真实线路记号、以及日语原名显示都接在同一个页面里。
- 地图和计划展示继续贴近真实铁路语义。完成了同公司同名站标签聚合，避免同一个公司在同一个 station group 里重复刷站名，同时保留 JR East / JR Central / Tokyo Metro 这类不同公司的真实物理语义；`Current Plan` 也从泛泛的 `Planned travel leg` 改成真实 stop time 的 `上車 HH:MM · 下車 HH:MM`。
- 修了横須賀線等选中列车高光断裂的问题。旧逻辑只会在单条 geometry fragment 里切相邻停站路径，碰到横須賀線、湘南新宿、総武快速、京葉/武蔵野这类跨多段真实 N02 轨道片段的服务就会空段；现在优先走 route 自身真实 geometry，失败时再走真实轨道 graph 寻路，不退回站点直线。核心 JR 直通线路的相邻停站高光审计已经做到 `0` 断点。
- 稳住了 v3 数据和服务器基础。清点 dirty worktree 后，把稳定的数据/脚本输出单独分组、审计、提交并推送；重建后的 v3 timetable 有 `41186` 个 trip，duplicate unified id/signature 都为 `0`，rendered lines without trips 为 `0`。本地和 Render 上的 `onichase-v3-room-server` `/health` 都确认 `dataset_name = v3-tokyo`。
- 单人 smoke 已通过：headless Firefox 从東京进入 Runner 单人模式，选择丸ノ内線、选车、选本郷三丁目，生成一段带真实上车/下车时间的计划，并自然从 planning countdown 进入 `LIVE`。留下的小瑕疵是 train preview marquee 在 DOM/视觉上可能重复，以及 Replay 按钮还隐藏着。
- 今天最花心力的是把“直通显示”从模糊的同公司/同站点推断，改成物理线路规则。新 axiom 已记录：新干线按各自名称分开；非新干线只有在同一物理轨道、同一站台/乘车面运行才算直通。东京站重新显示山手線、京浜東北線、横須賀線、東海道線等普通线；东海道线不再和东海道新干线互相借车；自由が丘、日吉、横浜等东急区间不再把副都心線之类直通服务当成单独乘车线路。
- 又系统检查了其它直通族：京急/浅草線/京成、東急/副都心線/西武/東武、目黒/三田/南北/相鉄、埼玉高速鉄道、みなとみらい等。代表站点现在基本按物理乘车线显示：品川是京急本線，浅草区间是都営浅草線，押上/青砥/京成高砂是京成物理线，二俣川/西谷是相鉄，みなとみらい/元町・中華街是みなとみらい，浦和美園是埼玉高速鉄道。剩余已知数据缺口是当前 bundle 还没有把 `13号線副都心線` 作为小竹向原的物理 pattern 暴露出来。
- 按反馈把东京 Metro 的 UI 名称改成常用线名，不再显示编号法定名：`4号線丸ノ内線` 变成 `丸ノ内線`，`8号線有楽町線` 变成 `有楽町線`，`13号線副都心線` 变成 `副都心線`。东京、池袋、涩谷、日本桥、小竹向原、目黑等探针都没有再出现可见的 `号線`。
- 最后做了本地双浏览器多人 smoke：Runner 创建房间，Hunter 加入，同房双方 ready 后进入 Planning，再双方 ready 推进到 LIVE。两个客户端都加载 `41186` trips，同步到 `Live · next planning 07:00 · 58m` 和游戏时钟 `06:02`。headless 环境里复制房间号会失败，但房间号显示、加入、ready、对局同步都正常；下一步该做真实公网浏览器的双人实玩。
