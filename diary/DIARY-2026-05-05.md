# DIARY - 2026-05-05

今天主要把 v4 的 route-choice 数据正确性继续往全国化回归推进。稀疏/单候选线路审计补了站点分片能力，完整扫过 `9052` 个 station group，`278` 个 sparse choice 全部无 suspicious。

今天又把“直通”和“直挂/併结”的边界纠正了一次。直挂不是路线合并规则：非新干线直挂车使用 umbrella + portion 选择器，例如 `成田エクスプレス` 和 `関空快速・紀州路快速`；但新干线是显示例外，不能合并成 `東北・北海道・秋田新幹線`，也不显示 `はやぶさ・こまち` umbrella 车次行。东京/上野/大宫仍分别显示 `東北・北海道新幹線` 与 `秋田新幹線`，盛冈往田泽湖/秋田方向显示 `秋田新幹線`。

胜负判定仍然保留直挂等价：新干线虽然不使用直挂显示规则，但共享物理段里仍按 `same_train` 判定。最新完整 `v4_route_choice_audit` 扫过 `9052` 个 station group、`111175` 个 trip、`1589722` 个 route-choice/label 检查，`0` anomalies；其中新干线直挂 `same_train` 等价边为 `368`，非新干线 umbrella 样本包括 `成田エクスプレス` 与 `関空快速・紀州路快速`。

还确认了 `つばさ` 的问题不是同一个前端显示 bug，而是当前 v3-rematched 源数据没有保留足够的东京-福岛 B 段与福岛-山形/新庄 M 段同号配对；不能在前端硬造，后续要回到源数据修复。

下午把这个源数据问题真正修掉了。`つばさ` 的 B/M 分段不能按 `serviceName + serviceNumber` 直接压成一条，否则东京-福岛段和福岛-山形/新庄段会互相吞掉；现在 merge 层保留 `Tsubasa` 的 train instance 分段，并在 v3 train index 去重时优先保留能和同号 B/M 成对的行。重新跑过 `v3_tokyo_bundle -> v4_existing -> v4_current -> v4_gameplay` 之后，网页里东京已经能看到 `山形新幹線`，并且有 `14` 条真实同号配对合成出的东京-山形/新庄 `つばさ`。

也把 `v4_route_choice_audit.js` 的全量扫描方式拆开了。之前单个 browser evaluate 同时做 global/duplicate/known/focused 容易卡在 global 后面的状态里；现在全量命令仍然是一条命令，但内部按阶段跑，global 再按 `20000` trip chunk 合并。第一轮重构后全量跑完 `3:58.60`，扫了 `1589947` 个 global route-choice/label row、`9052` 个 duplicate station、`579` 个 stitched preview，`0` anomalies。

最后处理了 limited-express trace suspect 里的 `ひたち`。根因是上野、仙台这类大站的物理站线别会把出站长区间错带到 `東北線` 或 `仙山線`，而 `ひたち/ときわ` 真实应该是品川-东京 `東海道線`、东京-上野 `東北線`、上野/仙台往常磐侧 `常磐線`。builder 里加了 reviewed segment rule，并修正命名特急 terminal-tail 判断；现在 `ひたち` suspect 数是 `0`。

随后又把前端显示口径收紧了一次：秋田新干线和其他 stitched/through 车次不再额外显示“直通”说明，车次行、预览、current plan、replay 行和底部滚动播放都只显示正常公开车名/线路名。新增的全局 route-choice label 审计会抓这种合成 `直通` 文案，当前完整扫描 `throughDisplayTextCount: 0`、`0` anomalies。

验证时没有只看东京一处：专门的浏览器探针扫了全部 `111175` 个 trip 和 `565` 个 stitched preview，`labelMatchCount: 0`、`previewMatchCount: 0`；东京仍显示 `秋田新幹線`，也没有合并的 `東北・北海道・秋田新幹線`。正式 route-choice audit 小分片也保持 `0` anomalies。

最后又把 `つばさ`/`こまち` 的高亮和分类口径修正成更准确的两层模型。共享干线段可以用真实物理 path：东京-福岛/盛冈是 `東北新幹線`；但福岛/盛冈之后不能把新干线车写进普通 `奥羽線` 或 `田沢湖線`，数据库和玩家显示都要保持 `山形新幹線`/`秋田新幹線` 的独立身份。今天的修复把这个规则放进 ingest、AXIOMS 和 selected-train 回归里；验证结果是 `Tsubasa`/`Komachi` 的 ordinary branch trace leak 为 `0`，selected-train 抽样 `5000` 车覆盖 `131` 个 mini-Shinkansen 独立 trace case 且 `0` failures，网页分线在福岛/米泽/山形/新庄和盛冈/田泽湖/大曲/秋田都同时保留普通线与新干线线别。

晚上把 1/2/3 三件收掉了。`v4_route_choice_audit.js` 的全量扫描现在用同一个已加载页面连续跑 global chunks，并把 `mini-shinkansen` 做成正式 stage；默认全量审计 `3:26.71` 跑完，`1589947` 个 route-choice/label row、`9052` 个 duplicate station、mini-Shinkansen `8` 站 `450` 条新干线 entry/`457` 条普通线 entry 全部 `0` anomalies。

limited-express trace suspect 也继续往前推了一层。正常新干线跳站不再挡住传统特急；`ひだ36号` 名古屋-米原的数据 trace 从错误的 `関西線` 修回 `東海道線`；NEX 东京-成田机场、あずさ 上诹访-松本、スペーシア日光 大宫-栃木补了 reviewed multi-leg path。最新 suspect 扫描 `111969` trips、`3109` candidates、`30719` adjacent pairs，`unreviewedSuspectCount: 0`。

最后修了 Sunrise。问题不是高松源数据完全没了，而是普通 `サンライズ出雲`、混合分支行和季节 `サンライズ出雲91/92` 在前端被当成多个独立候选；混合行又因为标签偏向出雲，把瀬戸的高松分支剪掉了。现在东京只显示一个 `サンライズ瀬戸・出雲` route choice，车次行是一个 umbrella，下面有 `サンライズ瀬戸 -> 高松` 和 `サンライズ出雲 -> 出雲市` 两个 portion；季节 91/92 不进入普通 gameplay。验证用 JR 西日本官方 Sunrise PDF 对照，并把东京 Sunrise 回归加进 `v4_route_choice_audit.js` focused stage。

Scorp 又指出 Sunrise 仍然不对，复查后发现上一轮回归只检查了 route choice 和 umbrella 存在，没有检查 2/3 的玩家可见车次 row 文字。实际可玩流程只剩一个 row，但 label 还显示成重复的 `サンライズ瀬戸・サンライズ出雲`。现在 umbrella label 统一成官方口径 `サンライズ瀬戸・出雲`，并且 focused audit 额外检查：东京选中 Sunrise route 后，2/3 必须只有一个 coupled row，label 必须是 `サンライズ瀬戸・出雲`，portion 终点必须包含 `高松` 和 `出雲市`。
