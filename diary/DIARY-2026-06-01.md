# DIARY - 2026-06-01

今天主要是把 V5 ship 这段工作做成可交接状态，并同步到 GitHub。

上一轮的关键结论是：高松周边船的问题不能用 v4 临时 patch 解决，必须沿着 V5 的真实 ship source、ship map、timetable bundle 和交互状态流修。现在这条线已经收住：V5 用真实官方数据支持 `高松港 -> 土庄港`，并且 runner 在到港、上船、到达接续点三个阶段都有 marker；到达土庄港后可以继续从土庄港选择下一段船。

随后补齐了雌雄島海運。旧数据把官方的 `高松 -> 女木島 -> 男木島` 航路错误折成 bare `高松 <-> 男木` 直达，导致 UI 里看不到女木港，也看不到正确的高松到男木/女木链路。现在 source/runtime 已改成四个真实分段：`高松港->女木港`、`女木港->男木`、`男木->女木港`、`女木港->高松港`，每个方向 6 班；ship map 也补了 `女木港` 和对应路线形状。重建后确认 bare `高松` sailing 为 0。

交互侧也补了一个重要规则：如果目的港本身没有 rail/bus 接驳，比如女木港这种短途岛港，玩家到达后会停在 `PORT_WAIT`，仍然可以继续选下一段船或返航，而不是因为“2km 内没有接驳”被禁用。

回归测试现在由 `scripts/tests/v5_ship_interaction_audit.js` 守住。它使用真实 V5 页面和真实船班，检查高松-土庄 marker/续乘，也检查高松港能列出女木港、女木港能列出男木、雌雄島海運四个方向各 6 班。最新安静输出：

```text
OK v5 ship interaction: markers=3, firstPort=土庄港, onward=4, meon=4
```

下午又发现 `高松港 -> 家浦` 虽然有真实豊島フェリー班次，但 UI 行被灰掉，原因是旧 catchability 规则要求目的港必须有 2km 内 rail/bus/airport 接驳。家浦这种岛港没有陆上接驳时，现在改为把目的港本身作为 zero-distance `PORT_WAIT` 到达点：玩家能直接坐船到家浦，到达后继续从家浦选返航/下一段船。回归测试已扩展到 `高松港 -> 家浦 -> 高松港`，最新安静输出：

```text
OK v5 ship interaction: markers=3, firstPort=土庄港, onward=4, meon=4, teshima=1
```

随后又修了同一条链路里的真正尾部问题：`addShipStep()` 已经允许从 `PORT_WAIT` 港口继续坐下一段船，但 `previewPlayer()` 回放计划时漏掉了 `PORT_WAIT -> TAKE_SHIP`，所以 `高松港 -> 家浦 -> 高松港` 虽然显示在 Current Plan 里，planner tail 仍停在 `08:16 家浦`。现在 replay 会执行第二段船，到高松侧接续点约 `08:58`；TAK bus access 也从高松侧 airport-bus stop 计算，不再从家浦算。顺手修了 bus planner 在 `BUS_STOP` anchor 下的 tile 加载和异步重画。

产品方向也收窄了：暂时不做 highway/night bus 主线，只把 V5 bus 暴露为机场接驳和港口接驳。实现上不重建大 bundle，而是在 bus map/planner tile merge 时筛选：`bus_airport` 全保留，另外保留经过 `v5_port_connectors.json` 港口 bus stop 的 route/trip；普通 local/highway/night 数据仍留在源 bundle，但不进入当前 planner/map。验证里机场链路、船/港口链路和普通 bus UI 都是绿的，青森フェリーターミナル样本仍保留 `青森線` 作为港口接驳。

晚上开始做 V5 整体数据审查，并把两个过去高频问题做成正式 gate：一是机场/港口/bus 距离不能消失，二是船和 bus 规划必须使用完整 Current Plan 的尾部。新增 `v5_data_readiness_audit.py` 和 `v5_plan_tail_anchor_audit.js`；前者输出静态总账，后者用浏览器场景检查 `高松港 -> 家浦 -> 高松港 -> TAK bus access`、机场/港口距离文案、以及 `PORT_WAIT`/`SHIP`/`BUS_STOP` marker 坐标。本轮所有轻量 gate 都是绿的，核心结果是 readiness `failureCount: 0`，plan-tail anchors `failureCount: 0`，TAK bus access 从 `08:58` 高松侧 `BUS_STOP` 算，不再从家浦算。

当前 GitHub 上最近的相关提交包括：

- `3df0d244 Fix Meon Takamatsu island ferry legs`
- `9ef0b7d8 Document v5 ship interaction gate`
- `2e1d80af Add v5 ship interaction audit`
- `3f89ede9 Fix ship arrival connector markers`
- `5661e4c2 Auto-continue after ship selection`

下一步可以回到 V5 公共交通主线，优先开始 highway/night bus 的第一批可玩线路；ship 这块现在已有数据修复、UI 行为和回归测试兜底。
