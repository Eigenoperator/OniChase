# DIARY - 2026-05-12

今天主要把 v4 的 selected-train / selected-route 高亮视觉收口。之前为了让选中车次更显眼，高亮层和底层线路的颜色、线宽、描边存在差异；Scorp 明确要求“高亮显示要和线路完全相同”，所以把高亮层改成沿用真实线路的颜色、几何和线宽比例，不再使用白色 casing 或自创强调色。

随后检查 `メトロはこね` 不高亮的问题。实际数据和 path 都存在：从 `大手町` 选中后会生成 `東京メトロ千代田線`、`小田急小田原線`、`小田急箱根鉄道線` 三段真实 path。问题不是缺数据，而是高亮层被改得和底层线路几乎完全同透明度，视觉上看不出来。最终方案是保留“颜色/线宽/几何完全像真实线路”，但 selected overlay 使用更高 opacity，让玩家能明确看到被选中的车次，同时不破坏“高亮必须沿真实线路走”的公理。

验证结果：

- `v4_selected_train_highlight_audit.js --limited-express-only --trip-start 1500 --trip-limit 200 --max-failures 5`：`failureCount = 0`
- `v4_map_pan_performance_gate.js`：`ok = true`, `failureCount = 0`
- targeted `メトロはこね` probe：生成 3 个 selected-trip-path feature，线路分别为 `東京メトロ千代田線`、`小田急小田原線`、`小田急箱根鉄道線`

已提交并推送 GitHub Pages 对应仓库：

- `012a9e6 Make selected train highlights visible with route colors`

下午继续处理 v4 地图显示和可玩数据缺口。

地图站名这边，Scorp 连续指出 zoom 10-12 附近站名显示不均、同名重影、HTML overlay 接管过早，以及 Runner/Hunter 被站名层影响的问题。现在的结论是：低 zoom 继续交给 MapLibre 的碰撞/优先级标注，高 zoom 的全站名由 HTML overlay 接管，但接管点必须是 `zoom >= 12`；`zoom < 12` 不能看起来像全量站名已经铺开。为了避免站名层压住玩家标记，Runner/Hunter 又加了独立的高层级保护 overlay，只在站名 overlay 生效的尺度启用。对应线上提交包括：

- `8d67ee4 Show full HTML labels from zoom 11`
- `ce73150 Restore label handoff at zoom 12`
- `c26d407 Limit pre-handoff station labels`

随后检查东京迪士尼周边的绿色环线。发现 `ディズニーリゾートライン` 在地图里有物理轨道和 4 个车站，但没有 gameplay timetable，所以会出现“地图有线、玩家却没有车可选”的体验断层。把它加入 special manual 官方高频线路采集：按リゾートゲートウェイ -> 東京ディズニーランド -> ベイサイド -> 東京ディズニーシー -> リゾートゲートウェイ的环线生成可玩车次，并重建 current collection 与 v4 gameplay bundles。验证 compact timetable 里现在有 `382` 条 `ディズニーリゾートライン` trips。已推送：

- `4123f95 Add Disney Resort Line gameplay service`

箱根方面，Scorp 怀疑缆车采集不全、应该是 5 分钟一班。检查后确认需要区分两个系统：`箱根登山ケーブルカー` 当前已有 `336` trips，其中 `276` 来自 special manual 的 5 分钟补丁，`60` 来自 NAVITIME；但官方 2025 平日 PDF 反而显示 cable car 的真实班次大多是 15-25 分钟级别。官方页面写“通常约 1 分間隔”的是 `箱根ロープウェイ`，不是 cable car。v4 目前没有 ropeway 的物理线路/站点，因此这不是继续加密 cable car 的问题，而是未来 v5/非铁路交通需要把 `箱根ロープウェイ` 作为独立 mode 加进地图和 timetable。

票价后台任务也做了一次状态检查。票价 subagent 的句柄已经不可等待，但它留下的文件显示当前真实票价规则覆盖为 `404/604` routes、`48` covered operators、`130` missing operators、`200` missing routes。它修改了 `docs/data/v4_fare_rules.json`、`data/v4_fare_rule_coverage_audit.json` 和 `scripts/ingest/collect_v4_fare_rules.py`，但这些尚未提交。当前判断是：票价系统已经有骨架和一部分真实规则，但不应阻塞 v4 release；除非 Scorp 明确要求，否则作为后台/实验性能力继续推进。
