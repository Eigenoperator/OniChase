# DIARY - 2026-04-06

今天把 `v3` 真正推进到了“能玩”的阶段，不再只是 GIS 展示图。最重要的一步，是把 `v1` 已经跑顺的 phase 和抓捕逻辑搬进 `v3` 网页端，让地图、diagram、plan board、result、replay 都开始共享同一份 Shinkansen bundle。这样 `v3` 不只是看线路，而是开始变成一个真正的 GIS-first 游戏壳。

同时也继续把 `v2` 和 `v3` 之间的交互思想互相回流。现在路线聚焦、diagram hover、站点发车集合这些东西，已经不再只是文档上的方向，而是能在线上页面里直接摸到的交互。今天的感觉是：这条“真实交通数据 + 游戏逻辑 + 现代地图视图”路线开始长出骨头了。
