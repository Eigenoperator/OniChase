## 2026-05-01

- 今天把 v4 全国线路显示问题收束到边界站和跨线 segment 的一致性上：两条线路共享的分界点站不再只归到其中一边，而是按实际物理线路 membership 同时保留在两边线路里。`audit_v4_boundary_station_line_membership.py` 重新跑完后，544 个边界 operator group 的 `missingMembershipCount=0`。
- 修复 gameplay bundle 生成时的几类全国性错配：跨公司边界优先使用相邻站共同的唯一物理线路；同名站会根据前后站物理线路上下文重新匹配，避免例如不同会社/地区同名站被错误吸到远处；山手线这种玩家识别优先的线路则不会被底层物理线覆盖。
- 前端线路选择也跟着补强：急行、特急和直通列车如果下一站跳过中间边界站，仍然优先显示当前 segment/上车站真正可服务的线路，而不是掉回远端直通名或泛化标签。上野、東京、横浜、八王子等之前容易暴露问题的站点都被纳入浏览器审计。
- 为了防回归，扩展了 `scripts/tests/v4_route_choice_audit.js` 和 route display integrity audit。最终浏览器全局扫描 1,607,125 个线路 choice，`anomalyCount=0`、`segmentMismatchCount=0`；换乘等价线路审计检查 75 个 cluster、157 个 station group、814 个 pending trip，也保持缺失线路/目的地为 0。
- 重新生成并提交 v4 gameplay bundle、manifest 和边界/显示审计输出。对应提交是 `b952ef6 Fix v4 boundary route display choices`。工作树里仍然保留了一批更早的 collector/cache/coverage 实验文件，今天没有把它们混进这次边界站显示修复。
- 下午继续用“列车号连续性”检查全国特急缺失。先修复 JR East 详情页里同一列车跨列直通段被拆开的解析问题，成田エクスプレス 9/13/17/... 这类新宿始发到成田机场的班次现在会合并成完整停站序列；同时补采成田线、常磐线、中央线官方集，并把 NEX/中央线补充集纳入 current 构建。顺手把 JR East 官方源的 `service_instance_id` 加上列车号，避免同一详情页多列列车复用同一个 ID。
- 重新生成 current/gameplay 后，成田エクスプレス的原始缺段已进入最终集合，current 为 125331 班、重复 service id 为 0；JR East 官方 2026 年 5 月页面也确认若干剩余“缺号”其实是现行时刻的跳号或不同始发模式，例如品川常磐线从 ときわ85 直接跳到 ときわ97，中央线东京晚间 あずさ只有 41/45/49/53/55。
- 对剩余高优先候选做了抽查：こうのとり16/20/24、きのさき14、スーパーはくと6、スーパーまつかぜ8 都已在数据里以不同起点存在，更多是审计候选的误报/季节运行范围，而不是当前工作日数据整段缺失。最新 route display integrity 仍保持 0 missing trace、0 ambiguous trace、0 duplicate train-station route membership。
- 进一步把缺号审计从“候选清单”收口成“红灯清单”：12 个已审核正常模式进入 `reviewedGapPatterns`，分为不同始发、官方跳号、并行列车族编号三类；`highPriorityGapCandidateCount` 从 14 降到 2，只剩旧 v3 私铁源里确实缺的 `はこね24/26/28` 和 `スペーシアＸ8/10`。
- 在线复核最后两个私铁红项后，没有把它们硬塞进 weekday 列车集：东武官方时刻表显示 `スペーシアＸ8/10` 是鬼怒川温泉始发而不是東武日光始发，当前数据里已经存在；公开的小田急 `はこね24/26/28` 停站表标注为土日祝，当前 v4 是 weekday snapshot。审计因此新增这两类 reviewed pattern，`highPriorityGapCandidateCount=0`。
- 继续联网/源页复核剩下 28 个低优先跳号候选，确认它们都是更远始发、更远终到、分支终点、短折返、官方跳号或假日-only 这类正常模式。全部写入 `reviewedGapPatterns` 后，特急车号缺失审计变成 `gapCandidateCount=0`、`highPriorityGapCandidateCount=0`、`reviewedGapPatternCount=42`。
- 最后把这些人工复核过的特急缺号案例固化进 UI 回归审计：新增重点站检查覆盖 敦賀/京都/新大阪/白浜/新宿/大船/成田空港/松本/大月，并要求 `サンダーバード 2`、新宿始发 NEX 9/13/17/21/25、白浜经过的 `くろしお 12/16/22`、大月 `富士回遊 3/7/11/15/93` 等代表车号都必须在线路选择里出现。完整浏览器审计重新跑完，`anomalyCount=0`。
- 开始 player interaction 前的数据总验收，先做全国重点枢纽抽样。这个过程抓到一个真实红灯：人工确认的 `JR小倉`/`小倉` 换乘规则没有距离上限，会把京都小倉和北九州小倉连到一起，导致北九州小倉出现近铁京都线/奈良线。已给 reviewed direct transfer 增加 5km 上限，并新增 `v4_hub_data_audit.js`，用应有线路消歧同名站。东京、新宿、横浜、名古屋、京都、新大阪、大阪、岡山、広島、小倉、博多、熊本、鹿児島中央、高松、金沢、仙台、新青森、新函館北斗、札幌、那覇空港、沖縄県庁前 21 个枢纽全部通过，`anomalyCount=0`。
- 继续做长距离可玩性路径验收，并刻意加入偏僻小站。新增 `v4_long_distance_playability_audit.js`，按真实换乘点逐段验证玩家能否从起点选车到下一站点。30 条路线全部通过：包括 東京→博多/新函館北斗/金沢/白浜，京都→天橋立，名古屋→高山/新宮，岡山→出雲市/松山，高松→宇和島，博多→佐世保/長崎，鹿児島中央→枕崎，札幌→稚内/根室，新青森→大湊，仙台→女川，東京→奥多摩/銚子，新宿→河口湖，松本→南小谷，米子→境港，高知→奈半利/宿毛，沖縄ゆいレール等。结果 `30/30` playable、`anomalyCount=0`。
- 顺着长距离验收继续把“走到换乘站时顺手体检当前站点”做进脚本。现在每个起点、换乘点、终点都会检查当前 station surface：预期线路是否还在线路选择里、是否有可上车候选、下一段目的站是否能找到候选车；终到/无下游 rows 不再误报，只记为 `skippedNonBoardableDepartureCount`。最新跑完 `30` 条真实路线，覆盖 `72` 个 waypoint station surface，`anomalyCount=0`。
- 根据“固定样例不容易发现新问题”的反馈，把长距离验收升级成 seeded 随机路线链：脚本会随机挑起点，使用真实候选车次自己滚动规划 1-4 段、生成随机目的地，再回到系统站面验证玩家是否能按这些 waypoint 完成。默认保留 30 条固定路线，并追加 20 条随机路线；seed `20260501` 的本轮样本包括 別保→根室、法隆寺→姫路、湯檜曽→新潟、浦川→伊那北、三谷→温泉津、新神戸→熊本 等。最新结果 `50/50` playable、`137` 个 waypoint station surface、`anomalyCount=0`。
- 继续把两类长距离验收都扩大到 100 组，并让生成器优先选择没测过的 stationGroup/route title。固定类现在是 30 条手写基准 + 70 条稳定 coverage 生成路线；随机类默认 100 条 seeded route-chain。跑 200 组时发现一个终点站晚到误报：coverage 大滝→秋田 在 `24:20` 到达终点后没有后续可上车车次，这不应算换乘站异常；已改成只有存在下一段时才要求 boardable departures。最终结果：`caseCount=200`、`fixedCaseCount=100`、`randomCaseCount=100`、`passedCaseCount=200`、`waypointAuditCount=682`、`uniqueWaypointStations=495`、`uniqueLegRoutes=165`、`anomalyCount=0`。
