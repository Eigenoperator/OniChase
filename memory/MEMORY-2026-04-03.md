# Daily Memory - 2026-04-03

- 2026-04-03 10:12:53 PDT - On the first prompt of the new natural day, read `STATUS.md`, checked the `diary/` folder, confirmed that `DIARY-2026-04-02.md` was missing, and backfilled it in concise Chinese with a focus on the day's hunter-mode-oriented playtest and client iteration work.
- 2026-04-03 11:34:49 PDT - Converted Scorp's new hunter-mode decisions into an implementation-facing design note in `HUNTER_MODE_PLAN.md`: during `PLANNING`, the hunter sees the runner at exact station if stationary or abstractly `on the line` if riding without the exact train number; during `LIVE`, the hunter sees nothing; hunter operations stay symmetric with runner operations; and the first focused hunter tests should assume the runner is not moving so the hunter-side information and control loop can be isolated.
- 2026-04-03 13:29:20 PDT - Promoted Scorp's new git-sync preference into `AXIOMS.md`: do not commit/push for every small change, but do commit/push when a change is significant and ensure at least one sync happens on each natural day that included project work.
- 2026-04-03 13:55:23 PDT - Implemented the first hunter-mode visibility filter in both `app/local_client.py` and `ui/web_client.html`: when hunter is the active side during `PLANNING`, runner information is shown as exact station if stationary or abstractly `On the Yamanote Line` if riding; during `LIVE`, runner visibility is hidden again; and runner route traces are no longer leaked to hunter view.
- 2026-04-03 13:55:23 PDT - Added two dedicated stationary-runner hunter test scenarios in `data/scenarios/` so the first hunter-mode playtests can focus on the hunter-side planning and interception loop.
- 2026-04-03 13:58:42 PDT - Refined the hunter-side runner presentation in both clients after Scorp clarified that the UI should not directly say which line the runner is on: the hunter now only gets a coarse between-stations cue such as `Between Tokyo and Ueno`, while the map position itself is left for the player to interpret.
- 2026-04-03 14:07:36 PDT - Tightened the hunter-side presentation again after Scorp clarified that even `Between A and B` text was too explicit: when the runner is riding, the hunter now gets only a map cue with no textual position hint, and `SCHEMA.md` now explicitly defines a future `MapGeometryBundle` layer for real station coordinates and line polylines.
- 2026-04-03 14:52:39 PDT - Added `ROADMAP.md` to formalize the current product sequence: `v1` is Yamanote-only core gameplay validation, `v2` expands to the full Shinkansen map as the first multi-line version, and `v3` expands to full Tokyo complexity after the earlier versions have validated the chase loop and UX.
- 2026-04-03 15:03:22 PDT - Added a `Start Game` button to both `app/local_client.py` and `ui/web_client.html`: during `PLANNING`, the playtest user can now manually end planning early and enter `LIVE`, and both clients explicitly state that the real multiplayer version should require both players to agree before the game starts.
- 2026-04-03 15:43:57 PDT - Updated the mode-specific playtest preset windows in both clients: runner tests still default to `06:00 -> 07:00`, while hunter tests now default to `07:00 -> 08:00` so hunter-mode verification is not cut off at `07:00`.
- 2026-04-03 16:01:21 PDT - Fixed the actual hunter preset logic in both clients after Scorp reported that the hunter plan board was still pinned to `WAIT_UNTIL 08:00`: hunter-mode presets now make the runner the passive waiting side and leave the hunter with an empty plan so the hunter can be played directly.
- 2026-04-03 16:07:50 PDT - Fixed a ring-line map-click bug in `app/local_client.py`: while planning from a train, clicking a map station to choose where to get off now uses the same downstream destination set as the visible destination list, so valid loop targets such as `TOKYO` are no longer blocked by a simplistic current-board-stop equality check.
- 2026-04-03 16:09:54 PDT - Fixed the more direct clickability issue Scorp pointed out: player markers could visually sit on top of station symbols and make the target hard to click. The native client now enlarges station hit-testing and offsets station-local markers slightly away from the exact node center, while the browser client makes player markers ignore pointer events so clicks fall through to the station beneath.
- 2026-04-03 16:31:42 PDT - Added explicit live capture handling to both the native and browser clients for hunter-mode playtests: once `LIVE` begins, both clients now check the same minimal `same_node / same_train` capture conditions used by the current engine surface, immediately flip the match into `ENDED` on capture, and draw a clear on-map capture mark so catch judgement can be visually verified instead of silently continuing.
- 2026-04-03 16:44:28 PDT - Improved the new live-capture ending feedback in both clients: the native client now writes a dedicated capture summary and detail card with the capture time, rule, and both player states, while the browser client now exposes the same explanation in the info panel and quickbar so hunter-mode tests are easier to interpret at a glance.
- 2026-04-03 17:02:11 PDT - Ran a first 10-case `v1` development self-test directly against the real Yamanote weekday simulator: the set covered immediate same-node capture, seven stationary-runner interception cases from `SHINJUKU 07:00`, one deliberate miss by chasing the wrong station, and one same-train capture case. All 10 outcomes matched expectation at the engine layer, so no new rules/logic bug was surfaced in this pass.
- 2026-04-03 17:29:44 PDT - Drew the first `v2` strategic map sketch in `visuals/shinkansen_v2_map_draft.svg`, using a nationwide Shinkansen schematic with major trunks and hubs so we can discuss the multi-line expansion path after the `v1` Yamanote prototype without jumping straight to Tokyo complexity.
- 2026-04-03 17:48:31 PDT - Reworked the `v2` map draft after Scorp rejected the first overly schematic version: `visuals/shinkansen_v2_map_draft.svg` is now redrawn as a current real Shinkansen network map with real open lines and real stations across JR East, JR Hokkaido, JR Central, JR West, and JR Kyushu, explicitly removing future-only extensions from the draft.
