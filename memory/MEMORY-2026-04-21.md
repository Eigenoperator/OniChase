# Daily Memory - 2026-04-21

- 2026-04-21 11:42:10 PDT - Wrote the day's v3 checkpoint into `diary/DIARY-2026-04-21.md`: official `v3.html` is now the MapLibre Tokyo page with the real Tokyo map bundle, real timetable bundle, v2-style gameplay shell, single/multiplayer entry, Ready Room, three-layer planning list, real line symbols, and Japanese-original station/line/train display. The same checkpoint records the completed same-company station-label aggregation rule, the 横須賀線 selected-train highlight fix, and the `Current Plan` display change from generic leg text to real `上車 HH:MM · 下車 HH:MM` times.
- 2026-04-21 11:43:02 PDT - Committed and pushed the 2026-04-21 diary update in commit `917dcce` (`Write diary for 2026-04-21`).
- 2026-04-21 11:58:52 PDT - Fixed two v3 gameplay/online bugs and pushed commit `614d5f2` (`Fix v3 room exit and plan undo`). The Ready Room now has `Leave Room`, which stops online polling, clears local room state, returns to the entry screen, and calls the room server `/leave` endpoint to free the seat. Plan undo now removes a full train leg (`BOARD_TRAIN` + `RIDE_TO_STATION`) instead of leaving a dangling onboard state, while `Clear Future` / `Undo Last` preserve the currently running onboard leg during hourly planning.
- 2026-04-21 12:33:56 PDT - Fixed v3 station highlighting and the related train-selection stall, then pushed commit `5f3b39f` (`Fix v3 line-specific station highlights`). Station highlights now resolve to route-specific physical station ids instead of every physical station in the same station group, so examples like 後楽園 highlight the 丸ノ内線 point for `4号線丸ノ内線` and the 南北線 point for `7号線南北線`. The selected-train highlighter no longer builds the full Tokyo all-track graph as an interactive fallback, cutting the 東京 -> 後楽園 -> 南北線 train click path from about `36s` to about `6.6ms` while still expanding downstream stops.
- 2026-04-21 12:36:26 PDT - Created this `memory/MEMORY-2026-04-21.md` file after confirming no daily memory existed for 2026-04-21. Left unrelated dirty worktree items untouched: `docs/data/shinkansen_v2_weekday_train_instances_merged.json`, `ui/v3_tokyo_phase1_map.html`, `data/v3_external/`, `data/v3_tokyo_jreast_core_cache/`, and `data/v3_tokyo_rinkai_cache/`.
- 2026-04-21 19:47:36 PDT - Read `AXIOMS.md`, `HISTORY.md`, `IDENTITY.md`, `MEMORY.md`, `SCHEMA.md`, `SOUL.md`, `USER.md`, `STATUS.md`, and the latest daily memory/diary to reestablish project direction. The next practical step is to triage the large dirty v3 data/script worktree, validate the stable subset, and commit it before continuing with Render `onichase-v3-room-server` provisioning and public two-browser v3 multiplayer testing.
- 2026-04-21 19:54:45 PDT - Triaged the dirty worktree into stable v3 data/script outputs, generated docs-data mirrors, source/audit scripts, and intentionally uncommitted large source caches. Rebuilt `v3_train_index` and `v3_tokyo_v2_bundle`, reran `run_v3_data_quality_audits.py`, and confirmed no failures: unified trains `41186`, duplicate unified ids `0`, duplicate unified signatures `0`, rendered lines without trips `0`, and trains with unmapped stops `0`. Verified local `v2_online_room_server.py --dataset v3-tokyo` `/health` and public `https://onichase-v3-room-server.onrender.com/health`; both report `dataset_name = v3-tokyo`.
- 2026-04-21 19:56:32 PDT - Committed and pushed the validated v3 stable set to GitHub in commit `c66264e` (`Validate v3 Tokyo timetable bundle`). Left known non-stable/unrelated dirty items out of that commit: `docs/data/shinkansen_v2_weekday_train_instances_merged.json`, `ui/v3_tokyo_phase1_map.html`, `data/v3_external/`, `data/v3_tokyo_jreast_core_cache/`, and `data/v3_tokyo_rinkai_cache/`.
- 2026-04-21 20:17:51 PDT - Ran a local headless Firefox/geckodriver single-player smoke test against `docs/v3.html`. The core Runner flow worked: loaded `41186` trips, entered single-player, showed `14` Tokyo departure lines, selected `4号線丸ノ内線`, selected a train, selected `本郷三丁目`, generated a one-leg plan with real boarding/alighting times, started the game, and naturally transitioned from planning countdown into `LIVE` with the runner on the train and hunter hidden. Noted minor UI rough edges for later review: train preview marquee text appears duplicated in DOM/possibly UI, and the Replay button is hidden even though the internal simulation path updates replay details.
- 2026-04-21 20:31:17 PDT - Fixed v3 through-running line display in `docs/v3.html` and `ui/v3_maplibre.html`. Physical route detection now uses only a route's own service geometry instead of borrowed through-service track aliases, route choices no longer fall back to nonphysical trip aliases, and Tokyo Metro through trains on Tokyu/Toyoko sections are grouped under the Tokyu physical line. Headless Firefox probes confirmed `自由が丘` shows only `東横線 / 東急` and `大井町線 / 東急`, `日吉` shows Tokyu lines, `横浜`/`菊名` show `東横線 / 東急`, and choosing Toyoko at `自由が丘` lists Toyoko train rows instead of `13号線副都心線` as a separate category.
- 2026-04-21 20:38:41 PDT - Corrected the v3 through-running fix so Shinkansen and ordinary JR lines never share boarding operators. Removed the special `shinkansen` <-> `jr_*` borrowing rule from `routesShareBoardingOperator` in both v3 HTML files. Headless Firefox probes confirmed `品川` keeps `東海道線 / JR東日本` rows as ordinary Tokaido trains while `東海道・山陽新幹線 / JR新幹線` rows remain `のぞみ`/`ひかり`/`こだま`; `東京`, `新横浜`, `小田原`, and `熱海` also keep ordinary and Shinkansen choices separate, while `自由が丘`/`日吉` still preserve the Tokyu through-running display fix.
