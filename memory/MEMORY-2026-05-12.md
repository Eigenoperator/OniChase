# Daily Memory - 2026-05-12

- 2026-05-12 11:17 PDT - Finalized v4 selected-highlight visual behavior. Scorp clarified that selected route/train highlights should match the underlying railway line, so selected overlays now use the same route color, same geometry, and same line-width scale as base rail lines; white casings and fake highlight colors are disabled. Follow-up `メトロはこね` debugging showed the path was not missing: selecting it at `大手町` produces 3 true route segments, `東京メトロ千代田線`, `小田急小田原線`, and `小田急箱根鉄道線`. The visual issue was that the overlay had become too indistinguishable from the base line, so selected overlays now keep route styling but use opacity `1` for visibility. Validation passed: limited-express selected-train audit shard `failureCount=0`; map-pan performance gate `ok=true`. Deployed commit `012a9e6`.

