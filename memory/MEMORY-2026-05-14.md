# Memory - 2026-05-14

- 2026-05-14 11:45:42 PDT - V5 is now explicitly continuing from the v4 nationwide railway gameplay shell rather than becoming a separate rebuild. The current public v5 page is `https://eigenoperator.github.io/OniChase/v5.html`. Recent v5 work added a local precomputed walking graph with 0.5/1/2/3/5 km layers, then activated the 3 km walking layer in the v4-derived planner. Walking transfers now appear as `Walk to ...` choices, add `WALK_TO_STATION` plan steps, cost ¥0, use distance-derived walking time, and are understood by preview, Current Plan, replay, simulation, and same-node capture. Same-name but different-station transfers are no longer zero-minute teleports: same-name transfer candidates use walking graph time and impossible train connections are filtered out. The first multi-layer map interaction is also live: the `Walking Map` button keeps the railway map as a dimmed base, focuses around the current station, draws the 3 km circle, shows walking edges and clear clickable destination station labels, and clicking a destination writes a walking leg.
- 2026-05-14 11:50:06 PDT - Added the first right-side v5 multimodal planning mode picker. The 1/3 planner page now starts with five circular transport buttons: rail, walking, flight, ship, and bus. Rail and walking are enabled; flight, ship, and bus are present but disabled placeholders for later v5 systems. Rail mode shows only rail route rows, walking mode shows only 3 km `Walk to ...` rows and opens the walking map layer, so railway and walking choices are no longer mixed in the same list. Local Playwright verification confirmed five mode buttons, rail default with 25 route rows and no walk rows, walking mode with 36 walk rows, disabled placeholders for the unsupported modes, and active walking map overlay with 70 station features.
- 2026-05-14 11:59:30 PDT - Fixed Walking Map station-label duplication. When the walking map layer is active, all railway station label MapLibre layers (`label-hub`, `label-major`, `label-local`, dense/all labels, and selected station label) are hidden via layout visibility, and the HTML dense-station label overlay is cleared and hidden. Walking option labels remain visible, so the focused walking map now shows only walking station names instead of overlapping railway and walking labels. Local Playwright verification confirmed all railway label layers are `none`, the HTML overlay has zero children, walking labels remain visible, and no page errors occur.
- 2026-05-14 12:09:05 PDT - Fixed duplicate runner/hunter display in Walking Map. The walking map dimming routine now hides the MapLibre player marker halo, dot, and label layers while preserving the HTML player marker overlay, so each player is shown once. Local Playwright verification confirmed `player-marker-halo`, `player-marker-dot`, and `player-marker-label` are all `none` during Walking Map, while one HTML marker remains visible and no page errors occur.
- 2026-05-14 12:38:33 PDT - Made v5 walking speed slower and runtime-configurable. The default walking speed is now `1.0 m/s` instead of the earlier 2.0 m/s because current walking uses straight-line distance rather than actual pedestrian paths. `docs/v5.html` now computes walking time from `distanceMeters / state.walkingSpeedMetersPerSecond` for planning rows, map features, same-name transfer filtering, preview, Current Plan, and replay expansion; the precomputed artifact's `walkTimeSec` is treated as source metadata rather than gameplay truth. Added a visible Walking Speed selector with 0.8/1.0/1.2/1.4/1.6 m/s options so future stamina/fatigue systems can modify walking speed. Regenerated v5 walking edge artifacts with source speed 1.0 m/s and updated docs. Local Playwright verification confirmed a 423 m walk is 8 minutes at 1.0 m/s and updates to 9 minutes at 0.8 m/s without page errors.
- 2026-05-14 12:54:10 PDT - Removed the explicit Walking Speed UI and set the hidden v5 default walking speed to `5 km/h` (`1.3888888888888888 m/s`). The internal runtime speed parameter remains in place for future stamina/fatigue logic, but players no longer see or change it directly. Regenerated walking artifacts with source speed 5 km/h and updated docs. Local Playwright verification confirmed the page has no `walking-speed-select`, the internal speed is 5 km/h, and a 423 m walking option displays as 6 minutes without page errors.
- 2026-05-14 12:59:42 PDT - Reworked v5 walking speed from a hidden scalar into a continuous function entry point. `docs/v5.html` now routes walking-time calculation through `walkingSpeedMetersPerSecondFor(playerId, context)`, which currently defaults to 5 km/h but can continuously account for future stamina ratio, carried load, surface, weather, or explicit context speed without adding UI presets. Preview, transfer filtering, replay expansion, and walking legs now pass player identity into the speed function where relevant. Updated v5 docs to state that walking speed is a function rather than a discrete option set. Local Playwright verification confirmed no speed UI, base speed 1.3888888888888888 m/s, and a staminaRatio 0.53 context reduces speed to 0.7361111111111112 m/s while increasing the same 423 m walk from 305 seconds to 575 seconds.
- 2026-05-14 13:05:13 PDT - Changed the active v5 walking exchange radius from 3 km to 2 km. The generated walking graph still contains the 0.5/1/2/3/5 km layers, but `docs/v5.html` now filters walking choices, same-name transfer eligibility, and Walking Map radius by the 2000 m active threshold.
- 2026-05-14 14:33:23 PDT - Added rail-map auto-focus for v5 planning. Selecting a railway line now eases the MapLibre view to the selected route near the current planning position, and selecting a train suppresses the intermediate route zoom then fits the map to that train's upcoming stop segment using the existing real trip path helpers. Syntax verification passed with `node --check`; local browser smoke confirmed the map bundle loads, but full route-row interaction testing was blocked because the local headless page stayed at `Loading 112577 real trips...` beyond 120 seconds.
- 2026-05-14 14:40:21 PDT - Updated v5 selected-train map focus to include all future stops from the current boarding point instead of only the first eight stops. The focus bounds now start from the same real `tripPathSegments()` used by selected-train highlighting and include every downstream stop coordinate, so choosing a train moves the map to a scale and position that can show the whole remaining ride. Corrected Playwright flow through single-player entry verified route selection zoomed from 5.00 to 12.60 and train selection zoomed to 13.00 with destination rows available.
- 2026-05-14 14:53:16 PDT - Made selected-trip stop circles clickable on the map. `selected-trip-stops` now has its own click and hover handlers, and ordinary station-layer clicks first check whether the clicked station is a current future stop. If a pending trip is selected, clicking an orange stop adds `BOARD_TRAIN` plus `RIDE_TO_STATION`; if the player is already onboard, the same handler calls `addRideToStation`. The clickable stop set now comes from current planner context rather than only `state.selectedTripStopIds`, so onboard future-stop highlights remain actionable. Playwright verified pending-trip map click creates the expected plan leg; the onboard branch is implemented but the quick randomized script did not hold a stable TRAIN state for a full click test.
- 2026-05-14 15:19:01 PDT - Added hidden physical-station tracking under the v5 station-group UI. The player still sees station groups, but preview/result expansion now records `board_physical_station_id`, `alight_physical_station_id`, and rail leg `from_physical_station_id`/`to_physical_station_id`. Same-station-group boarding now uses the previous alight physical station and next board physical station to calculate transfer time from distance and the walking-speed function, falling back to the old zero-minute same-group behavior only when physical coordinates are unavailable. Browser verification found 折尾 physical stations about 281m apart and produced a 4-minute `same_station_group_physical_walk`; a normal selected ride still displayed group labels while expanded legs carried physical ids.
- 2026-05-14 15:50:06 PDT - Added the v5 Shinkansen same-station transfer minimum rule. When a same-station-group transfer has one side on Shinkansen and the other side not on Shinkansen, transfer time is now `max(5 minutes, physical walking time)`. Preview planning carries the previous alighted trip id so right-side departure filtering can apply the rule after a planned Shinkansen/non-Shinkansen ride, and result expansion carries the previous trip object for replay validation. Browser verification produced a normal-to-Shinkansen same-group transfer with 104m physical distance and 75s walking time, correctly raised to 5 minutes with reason `same_station_group_shinkansen_minimum`.
- 2026-05-14 16:00:32 PDT - Added the ordinary same-station transfer minimum and a v5 walking-transfer audit. Same-station-group boarding now keeps initial station boarding at 0 minutes when there is no previous ride context, but any post-ride same-station transfer is at least 1 minute; Shinkansen/non-Shinkansen same-station transfers remain at least 5 minutes. Added `scripts/tests/v5_walking_transfer_audit.js`, which verifies initial same-station boarding stays 0, ordinary same-physical-station transfer becomes 1 minute, and Shinkansen/non-Shinkansen transfer becomes 5 minutes. The audit passed locally against `http://127.0.0.1:4181/v5.html`.
- 2026-05-14 16:09:20 PDT - Added `scripts/tests/v5_walking_interaction_audit.js` as the broader v5 walking regression gate. It accepts `--walk-radius-km` (tested with `2`) and verifies rail/walk mode switching, Walking Map activation/deactivation, active walking graph radius bounds, same-name different-station walking transfer time, same-station physical transfer minimum 1 minute, and Shinkansen/non-Shinkansen same-station minimum 5 minutes. The audit passed locally against `http://127.0.0.1:4182/v5.html` with no failures.
- 2026-05-14 17:21:49 PDT - Started v5 Japan domestic flight data collection. Added `docs/v5_flight_data.md` documenting configurable flight gameplay buffers, IATA airport ids, physical-flight identity, and the codeshare dedupe axiom: one physical flight can have multiple marketed flight numbers and must not become duplicate playable flights. Added `data/v5_domestic_flight_sources.json` with the first official source inventory and `scripts/ingest/collect_v5_domestic_flights.py`, which parses ANA's all-area domestic timetable PDF (`2026-07-01` to `2026-10-24`) into `data/v5_domestic_flights_ana_20260701_20261024.json`. Initial ANA run parsed 1461 marketed rows into 1459 physical flight records, converted all airport names to 3-letter IATA codes, found 0 duplicate physical keys, 0 missing IATA records, 0 unknown operator codes, and marked 936 rows as codeshare/operator-share candidates for later cross-source merging.
- 2026-05-14 19:13:25 PDT - Updated the v5 flight data contract so every flight must include a machine-readable service calendar with exact `operatingDates`, ISO `operatingWeekdays`, weekday names, and parse status. ANA collection now expands calendar notes such as `7/1-31,8/7-16運航` into dates and weekdays instead of keeping only raw text. Rebuilt `data/v5_domestic_flights_ana_20260701_20261024.json`: all 1459 physical ANA-source records now have weekday sets, with 578 default all-period records, 881 parsed date-note records, 0 unparsed calendar notes, and 0 missing weekday records. JAL official timetable page currently states that flights from `2026-07-01` to `2026-08-31` will be available in early June 2026, so the JAL collector should use the same required service-calendar interface once that source is available.
- 2026-05-14 20:57:09 PDT - Added Skymark to the v5 domestic flight collector. Downloaded the official Skymark English summer timetable PDF (`2026-06-01` to `2026-10-24`) and generated `data/v5_domestic_flights_skymark_20260601_20261024.json`. The Skymark adapter skips `[Via ...]` composite route displays so they do not become fake physical flights, then dedupes repeated hub-table entries by physical flight number, airport pair, times, and calendar markers. Current Skymark audit: 268 parsed physical-leg rows, 197 unique physical flights, 33 skipped via sections, 0 unknown airport names, 0 duplicate keys, and 0 bad calendars; 177 flights are default all-period and 20 use parsed marker notes such as `※1/※2/※4/※5/※6`. Updated `data/v5_domestic_flight_sources.json` with implemented Skymark plus candidate official sources for AIRDO, Solaseed, StarFlyer, FDA, IBEX, ORC, AMX, Toki Air, Peach, Jetstar Japan, and Spring Japan. Solaseed's official PDF cache was downloaded, but text extraction is empty, so it needs OCR/table handling rather than plain `pdftotext`.
- 2026-05-14 21:37:59 PDT - Added AIRDO to the v5 domestic flight collector using the official AIRDO timetable HTML (`2026-03-29` to `2026-10-24`). Generated `data/v5_domestic_flights_airdo_20260329_20261024.json` with 64 physical AIRDO records across 22 directed airport pairs; all records have IATA airports and all-period weekday calendars, with 0 duplicate keys, 0 unknown airport names, and 0 bad calendars. Because AIRDO footnotes in the parsed page are mainly time-adjustment/customer notices rather than weekly service restrictions, the current adapter keeps them as source markers but expands the base rows to all dates in the published period. Cross-source overlap check against ANA's ADO-operated records found 63 exact physical-key overlaps, 2 ANA-only keys, and 1 AIRDO-only key, confirming that a future merged flight bundle must dedupe ANA-marketed ADO records against AIRDO official records and handle small source-version/time differences by service date.
- 2026-05-14 23:02:52 PDT - Added StarFlyer, IBEX, and Toki Air to the v5 domestic flight collector. StarFlyer uses official route HTML pages (`2026-03-29` to `2026-10-24`) and generated 62 physical flights across 12 directed airport pairs; it skips 4 embedded route rows that appear inside another route page. StarFlyer overlap check against ANA's SFJ-operated records found 60 exact matches and 2 StarFlyer-only flights. IBEX uses the official timetable HTML (`2026-03-29` to `2026-10-24`) and generated 58 physical flights after deduping 116 repeated airport-section rows; all 58 exactly overlap ANA's IBX-operated records. Toki Air uses the official schedules page (`2026-03-29` to `2026-08-31`) and generated 26 physical period-specific records with parsed Japanese weekday rules and explicit extra dates; all have clean IATA and service calendars. Updated source inventory statuses for StarFlyer, IBEX, and Toki Air to implemented.
- 2026-05-14 23:22:14 PDT - Expanded v5 domestic flight carrier coverage and pushed commit `ea2a7b2`. FDA now uses official route HTML pages for `2026-03-29` to `2026-10-24`, skips connection-guide content, and emits 108 physical flights with 82 all-period calendars plus 26 parsed operating-date calendars. Jetstar Japan now uses its official domestic PDF (`2026-03-29` to `2026-10-24`) and emits 115 physical flights; 108 calendars are parsed/all-period and 7 complex Japanese notes remain flagged as `unparsed_note_default_all_period`. Spring Japan now uses its official domestic PDF updated `2026-05-13` and emits 14 physical flights; several multi-line period calendars are flagged for a dedicated parser. Added `filter_v5_domestic_flights_by_carrier.py` and derived fallback artifacts from ANA's official all-area timetable for Solaseed (`99`), ORC (`52`), AMX (`8`), and JAC (`60`) because their own current machine-readable sources are blocked/image-based/not yet parsed. Source inventory and flight-data docs now explicitly mark JAL/JTA/RAC and Peach as remaining major source gaps.
- 2026-05-14 23:26:48 PDT - Found Peach's official domestic 2026 summer timetable PDF from the Route & Flight Schedules page and added it to the v5 collector. Generated `data/v5_domestic_flights_peach_20260329_20261024.json` with 104 APJ physical flights, 77 default all-period calendars, 27 parsed operating-date calendars, and 0 unknown airports. Updated source inventory and docs so Peach is no longer listed as a source gap. Remaining major non-fallback flight source gap is JAL/JTA/RAC; Jetstar has 7 complex calendar notes pending and Spring has 4 calendar rows pending/fuzzy due PDF multi-line layout.
