# Daily Memory - 2026-04-13

- 2026-04-13 13:25:48 PDT - Performed the first daily sync of the day: checked git status, verified that `diary/DIARY-2026-04-08.md` and `diary/DIARY-2026-04-09.md` were missing despite substantial tracked work on those days, confirmed there were no project commits on `2026-04-10` through `2026-04-12`, and created today's `memory/MEMORY-2026-04-13.md` with an exact-second timestamp so the diary/memory chain is back in sync before continuing work.
- 2026-04-13 13:28:45 PDT - Started concretely splitting `v3 phase 1` instead of keeping it abstract: defined that the first phase should focus only on two things—an enlarged real-position map and the first real Tokyo-area rail service-data base—then wrote `V3_PHASE1_SCOPE.md` to pin down the first geographic boundary, initial operator/line scope (`JR`, selected private railways, and Shinkansen approach lines), required physical/service outputs, and the rule that high-density readability must not be achieved by moving stations away from their real positions.
- 2026-04-13 13:40:34 PDT - Added a new project axiom that whenever a line is included in scope, it should be included as a whole line rather than as an arbitrary partial fragment, so future `v3` scope planning does not quietly degrade into chopped-up operator segments without an explicit service-family justification.
- 2026-04-13 13:46:14 PDT - Continued decomposing `v3 phase 1` into concrete execution artifacts by adding `V3_PHASE1_LINES.md` and `V3_PHASE1_STATIONS.md`, which explicitly list the whole-line families to include first (JR, Shinkansen, and selected private-rail trunks) and the first priority station groups to use for geometry validation, label ranking, service-data checks, and future gameplay pilot slices.
- 2026-04-13 13:47:45 PDT - Refined `V3_PHASE1_LINES.md` so every included line now carries an explicit operator/system tag such as `Shinkansen / JR Central`, `JR East`, `Tokyu`, or `Keikyu`, making future source-inventory and ingestion planning less ambiguous than a bare line-name list.
- 2026-04-13 13:57:22 PDT - Added a stronger scope rule that private-rail inclusion should happen at the company level rather than by cherry-picking a few lines, then updated `V3_PHASE1_LINES.md` so the Tokyo-area phase-1 private-rail section is now expressed as whole-company inclusion candidates (`Tokyu`, `Odakyu`, `Keio`, `Keikyu`, `Tobu`, `Seibu`, `Keisei`) instead of pretending that only one or two hand-picked lines from each company are the true long-term scope.
- 2026-04-13 14:14:34 PDT - Added a new geometry rule for `v3`: interchange stations must not be collapsed into one fake shared coordinate just because gameplay allows transfers there; instead, the physical map/schema must preserve separate real station locations while `station_group` can still represent the transfer-capable game/logical grouping.
- 2026-04-13 14:37:59 PDT - Started the first concrete `v3` source-ingestion plan by adding `V3_SOURCE_INVENTORY.md`, defining the planned source stack for geometry (`N02`), station identity (`physical_station` plus `station_group`), and service/timetable ingestion (reusing the proven Shinkansen `v2` pipeline first, then Tokyo-area GTFS or official timetable pages operator by operator) so the next step can move from scope planning into real data collection.
- 2026-04-13 15:02:32 PDT - Built the first `v3` Tokyo seed outputs from the proven real-position Shinkansen data by adding `scripts/dev/render_v3_tokyo_seed_map.py` and generating `data/v3_tokyo_phase1_seed.json` plus `visuals/v3_tokyo_phase1_seed_map.svg`, so we now have a concrete current Tokyo map artifact to review at every next step instead of only discussing scope in the abstract.
- 2026-04-13 15:08:50 PDT - Upgraded the `v2` map path logic so hovered trip paths are no longer drawn by directly connecting stop points; they now reuse route slices from the service polyline, which keeps path highlighting consistent with the line-following movement model and moves `v2` one step closer to the future `v3` requirement of following real route geometry rather than simple station-to-station straight segments.
- 2026-04-13 15:21:36 PDT - Started the actual `v3` real-geometry integration by updating `scripts/ingest/build_v3_shinkansen_bundle.py` to prefer route geometry loaded from `data/v3_real_geometry_routes.json` over station-sequence polylines, then created that canonical geometry-source file so we can begin importing true N02-derived route polylines incrementally without redesigning the bundle format again later.
- 2026-04-13 15:35:36 PDT - Began the first full real-geometry pass for all Shinkansen lines by downloading MLIT `N02` datasets locally, validating that `N02-24` exists and includes the missing Hokuriku west extension stations through Tsuruga, and committing to use `N02-24` rather than the older `N02-22` snapshot as the primary nationwide Shinkansen geometry source.
- 2026-04-13 15:35:36 PDT - Added `scripts/ingest/build_v3_real_geometry_routes_from_n02.py`, which builds graph-connected real route geometry directly from `N02` railroad sections and existing route station order, then generated a full `data/v3_real_geometry_routes.json` covering all ten Shinkansen route families (`TOKAIDO`, `SANYO`, `KYUSHU`, `NISHI_KYUSHU`, `TOHOKU`, `HOKKAIDO`, `JOETSU`, `HOKURIKU`, `YAMAGATA`, `AKITA`) with route-by-route snap diagnostics.
- 2026-04-13 15:35:36 PDT - Rebuilt `data/v3_shinkansen_bundle.json`, refreshed `data/v3_gis/track_centerlines.geojson` plus `data/v3_gis/service_paths.geojson`, and regenerated the current Tokyo map outputs `data/v3_tokyo_phase1_seed.json` and `visuals/v3_tokyo_phase1_seed_map.svg`, so the `v3` pipeline now uses real Shinkansen geometry instead of station-sequence polylines.
- 2026-04-13 15:52:14 PDT - Started the next `v3` expansion step beyond Shinkansen-only geometry by adding `scripts/dev/render_v3_tokyo_phase1_network_map.py`, then generated `data/v3_tokyo_phase1_network_seed.json` and `visuals/v3_tokyo_phase1_network_map.svg`, which draw the current Tokyo-area physical network directly from `N02-24` for the first included sets of Shinkansen, core JR lines, and the first private-rail companies while keeping every visible line and station at its real position.
- 2026-04-13 16:48:07 PDT - Formalized the first Tokyo-core `JR` naming model for `v3` by adding `data/v3_jr_tokyo_service_families.json`, which separates `physical_line`, rider-facing `service_family`, and concrete `through_pattern` for 13 core JR families such as `Ueno-Tokyo Line`, `Shonan-Shinjuku Line`, `Keihin-Tohoku・Negishi Line`, `Saikyo・Kawagoe Line`, and `Chuo・Sobu Line (Local)`, then updated `V3_GIS_SCHEMA.md` so this rider-facing naming layer is now part of the explicit schema direction instead of only living in discussion.
- 2026-04-13 17:03:16 PDT - Started turning the new Tokyo-core `JR` naming model into visible map output by deciding that the next `v3` step should not stay abstract: we should render the same real-position Tokyo network in two synchronized views, one as the physical infrastructure layer and one as the rider-facing JR service-family layer, instead of leaving `service_family` as a schema-only concept.
- 2026-04-13 17:05:16 PDT - Added `scripts/dev/render_v3_tokyo_phase1_service_views.py` and generated `data/v3_tokyo_phase1_service_views.json` plus `visuals/v3_tokyo_phase1_service_views.svg`, producing the first dual-view Tokyo `v3` artifact where the left panel keeps the physical network at true positions and the right panel overlays JR rider-facing service families on the same real geometry; at the same time, I also finalized and kept the supporting `v3` render artifacts for the nationwide Shinkansen real-geometry full map and the expanded Tokyo phase-1 network seed so the current map baseline is now fully reproducible from checked-in scripts.
- 2026-04-13 17:09:44 PDT - Started the first concrete Tokyo-area train-data intake layer for `v3` by adding `data/v3_tokyo_timetable_source_registry.json`, a machine-readable registry that pins down the real timetable collection strategy for `JR East`, `JR Central Shinkansen`, and the first whole-company private-rail operators, and added `ui/v3_tokyo_phase1_map.html` as a local interactive pilot page for comparing physical versus JR service-family views on the same real-position Tokyo map.
- 2026-04-13 17:17:11 PDT - Expanded `v3 phase 1` from “Tokyo core plus selected trunks” to the full Tokyo-area urban rail systems now intended for scope: all Tokyo Metro lines, all Toei rail lines, the full Rinkai Line, Yurikamome, Tokyo Monorail, and Toden Arakawa; updated `V3_PHASE1_SCOPE.md`, `V3_PHASE1_LINES.md`, `V3_SOURCE_INVENTORY.md`, and `data/v3_tokyo_timetable_source_registry.json` accordingly so the operator and timetable plan now matches the newer all-Tokyo-rail direction.
- 2026-04-13 17:29:45 PDT - Checked the current Tokyo network seed against N02 and confirmed that the current map was still missing all Tokyo Metro lines, all Toei lines, Rinkai, Yurikamome, Tokyo Monorail, and also broader Tokyo-area systems like Tama Monorail and Tsukuba Express; then updated the `v3` network renderer, scope files, and timetable source registry so these urban systems are now in the intended Tokyo-area phase-1 geometry and source plan.
