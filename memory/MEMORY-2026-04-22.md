# Daily Memory - 2026-04-22

- 2026-04-22 10:27:08 PDT - Backfilled `diary/DIARY-2026-04-21.md` so it covers the full April 21 v3 workday instead of only the morning checkpoint. The diary now summarizes the v3 Tokyo official page state, station-label and plan-display polish, selected-train highlight repair, validated `41186`-trip v3 data/server health, single-player smoke, physical through-running rule and audits, Tokyo Metro common-name labels, and the local two-browser multiplayer smoke test.
- 2026-04-22 10:32:19 PDT - Fixed v3 train-list preview scrolling in `docs/v3.html` and `ui/v3_maplibre.html`. Train preview rows now omit arrival/departure time after the next-stop label (`次 新橋`, not `次 新橋 06:xx`), and marquee animation is enabled only after browser layout confirms the preview text is wider than its row. A headless Firefox probe confirmed normal Tokaido rows do not get `needs-marquee`, no next-stop preview contains a clock time, and an artificially narrow long preview does get `needs-marquee`.
- 2026-04-22 10:35:17 PDT - Added route-color bars to v3 Current Plan ride legs in `docs/v3.html` and `ui/v3_maplibre.html`, reusing the same `route-swatch` styling as the line chooser while leaving non-ride rows uncolored. A headless Firefox probe built a Tokaido plan and confirmed the Current Plan swatch color matched the selected Tokaido line (`rgb(242, 142, 28)`) and rendered at `32px` height.
- 2026-04-22 11:02:45 PDT - Removed the inline `Click to edit alighting station.` helper from v3 Current Plan ride rows in `docs/v3.html` and `ui/v3_maplibre.html`; rows now show only the boarding/alighting time text while preserving the existing editable-row click behavior.
- 2026-04-22 11:46:34 PDT - Ran 10 public two-browser v3 playtest rooms against `https://eigenoperator.github.io/OniChase/v3.html` and the Render `onichase-v3-room-server`, alternating desktop/mobile viewport widths between Runner and Hunter. Initial `/health` timed out after 30s but a follow-up wake succeeded with `dataset_name = v3-tokyo`; all rooms (`O86FU4`, `V8OAD2`, `PRZXU7`, `FC7NAG`, `NFOAZB`, `FBB76O`, `VYRCVO`, `4TRN45`, `DTTIJ0`, `Q01MX6`) created, joined, readied, loaded `41186` trips, and reached LIVE with synced clocks. Successful multi-leg coverage included 横須賀線 -> 東横線, 京葉線 -> 有楽町線, 上越新幹線 -> 京浜東北線, 東海道・山陽新幹線 -> 東急新横浜線, 総武快速線 -> 半蔵門線, 山手線 continuation, 丸ノ内線 continuation, and 都営大江戸線 continuation. The main bug found is that some selected follow-up transfer legs do not persist/render in Current Plan after selection, especially cross-operator/equivalent-station transfers such as 東海道線 -> 東横線 at 横浜, 山手線 -> 京急 at 品川, 丸ノ内線 -> 南北線 at 四ツ谷, and 小田急 from 新宿; headless clipboard copy still fails benignly while room code display/join works.
- 2026-04-22 12:07:57 PDT - Fixed the public-playtest Current Plan race by adding monotonic `plan_revision` handling to the v3 room server and matching client-side submit sequence guards in `docs/v3.html` and `ui/v3_maplibre.html`. The client now marks local plan changes dirty, ignores stale `/plan` responses, preserves local self steps while a newer submit is pending, and sends `plan_revision` with every plan submit; the server stores per-seat `plan_revision` and refuses older revisions without clearing readiness or overwriting steps. Added `scripts/tests/test_v3_online_plan_revision.py`; `python3 -m unittest scripts.tests.test_v3_online_plan_revision` and `python3 -m py_compile scripts/engine/v2_online_room_server.py scripts/tests/test_v3_online_plan_revision.py` both passed. Local online regression room `BM8SZS` kept both `東海道線: 東京 -> 横浜` and `東横線: 横浜 -> 菊名`. After GitHub Pages and Render deployed the fix, 10 post-fix public rooms completed (`OM1EKK`, `048CAM`, `SZFZIM`, `DX66I3`, `9WWXLI`, `PJSER4`, `8IGT3U`, `298LJI`, `2I7IUI`, `YVXM4O`); all reached LIVE, and every selected multi-leg plan row persisted, including prior failures `東海道線 -> 東横線`, `山手線 -> 京急`, `丸ノ内線 -> 南北線`, plus 京葉線 -> 有楽町線, 都営新宿線 -> 三田線, 総武快速線 -> 半蔵門線, 山手線 -> 西武新宿線, 新幹線 -> JR/東急, 横須賀線 -> 東横線 -> 副都心線-facing service, 上野東京ライン -> 千代田線, and 京王線 -> 井の頭線.
- 2026-04-22 14:44:11 PDT - Added `scripts/dev/run_v3_public_battle_records.py`, a dependency-free geckodriver/WebDriver harness that opens two public `v3` Firefox sessions, creates Render rooms, submits route plans, drives Ready -> Planning -> LIVE, and records each battle through the page's `match_event_log` replay system. After fixing two overly broad Hunter presets to use data-valid station groups (`中野` for 東西線 and `新宿三丁目` for 都営新宿線), the final 10-room run (`UG3K0P`, `8F29DK`, `FDALM6`, `XWRGGX`, `JGGFIF`, `XT7I7B`, `UNFPP9`, `9V8UCA`, `9KCIH4`, `YXCFQ2`) completed with `failures = 0`, every room reached `LIVE`, and battle records were saved to `reports/v3_public_battle_records_20260422_143910.json` and `.md`. Coverage included 東海道線 -> 東横線, 山手線 -> 京急, 小田急 -> 井の頭線, 丸ノ内線 -> 南北線, 東西線, 京葉線 -> 有楽町線, 都営新宿線 -> 都営三田線, 総武快速線 -> 半蔵門線, 山手線 -> 西武新宿線, 上越新幹線 -> 京浜東北線, 埼京線 -> 京浜東北線, 東海道・山陽新幹線 -> 東急新横浜線, 横須賀線 -> 東横線, 上野東京ライン -> 千代田線, 京王線 -> 井の頭線, and 京急 -> 都営浅草線 -> 京成押上線.
- 2026-04-22 14:58:30 PDT - Added `scripts/dev/render_v3_battle_timeline.py`, a reusable local renderer for v3 battle records. It reads `reports/v3_public_battle_records_20260422_143910.json`, pulls route colors from `docs/data/v3_tokyo_map_bundle.json.gz`, and writes per-game SVG diagrams plus an `index.html` under `reports/v3_battle_timelines_20260422_143910/`. The layout puts time in the center, Runner on the left, Hunter on the right, colored ride intervals beside each player, and station/transfer callouts at their event times with collision avoidance for tight transfers such as 京急 -> 都営浅草線 -> 京成押上線. Local debug rendered all 10 SVGs, verified the index references all images, and used geckodriver screenshots to visually inspect Game 1 and Game 10.
