# Daily Memory - 2026-04-22

- 2026-04-22 10:27:08 PDT - Backfilled `diary/DIARY-2026-04-21.md` so it covers the full April 21 v3 workday instead of only the morning checkpoint. The diary now summarizes the v3 Tokyo official page state, station-label and plan-display polish, selected-train highlight repair, validated `41186`-trip v3 data/server health, single-player smoke, physical through-running rule and audits, Tokyo Metro common-name labels, and the local two-browser multiplayer smoke test.
- 2026-04-22 10:32:19 PDT - Fixed v3 train-list preview scrolling in `docs/v3.html` and `ui/v3_maplibre.html`. Train preview rows now omit arrival/departure time after the next-stop label (`次 新橋`, not `次 新橋 06:xx`), and marquee animation is enabled only after browser layout confirms the preview text is wider than its row. A headless Firefox probe confirmed normal Tokaido rows do not get `needs-marquee`, no next-stop preview contains a clock time, and an artificially narrow long preview does get `needs-marquee`.
- 2026-04-22 10:35:17 PDT - Added route-color bars to v3 Current Plan ride legs in `docs/v3.html` and `ui/v3_maplibre.html`, reusing the same `route-swatch` styling as the line chooser while leaving non-ride rows uncolored. A headless Firefox probe built a Tokaido plan and confirmed the Current Plan swatch color matched the selected Tokaido line (`rgb(242, 142, 28)`) and rendered at `32px` height.
- 2026-04-22 11:02:45 PDT - Removed the inline `Click to edit alighting station.` helper from v3 Current Plan ride rows in `docs/v3.html` and `ui/v3_maplibre.html`; rows now show only the boarding/alighting time text while preserving the existing editable-row click behavior.
- 2026-04-22 11:46:34 PDT - Ran 10 public two-browser v3 playtest rooms against `https://eigenoperator.github.io/OniChase/v3.html` and the Render `onichase-v3-room-server`, alternating desktop/mobile viewport widths between Runner and Hunter. Initial `/health` timed out after 30s but a follow-up wake succeeded with `dataset_name = v3-tokyo`; all rooms (`O86FU4`, `V8OAD2`, `PRZXU7`, `FC7NAG`, `NFOAZB`, `FBB76O`, `VYRCVO`, `4TRN45`, `DTTIJ0`, `Q01MX6`) created, joined, readied, loaded `41186` trips, and reached LIVE with synced clocks. Successful multi-leg coverage included 横須賀線 -> 東横線, 京葉線 -> 有楽町線, 上越新幹線 -> 京浜東北線, 東海道・山陽新幹線 -> 東急新横浜線, 総武快速線 -> 半蔵門線, 山手線 continuation, 丸ノ内線 continuation, and 都営大江戸線 continuation. The main bug found is that some selected follow-up transfer legs do not persist/render in Current Plan after selection, especially cross-operator/equivalent-station transfers such as 東海道線 -> 東横線 at 横浜, 山手線 -> 京急 at 品川, 丸ノ内線 -> 南北線 at 四ツ谷, and 小田急 from 新宿; headless clipboard copy still fails benignly while room code display/join works.
- 2026-04-22 12:07:57 PDT - Fixed the public-playtest Current Plan race by adding monotonic `plan_revision` handling to the v3 room server and matching client-side submit sequence guards in `docs/v3.html` and `ui/v3_maplibre.html`. The client now marks local plan changes dirty, ignores stale `/plan` responses, preserves local self steps while a newer submit is pending, and sends `plan_revision` with every plan submit; the server stores per-seat `plan_revision` and refuses older revisions without clearing readiness or overwriting steps. Added `scripts/tests/test_v3_online_plan_revision.py`; `python3 -m unittest scripts.tests.test_v3_online_plan_revision` and `python3 -m py_compile scripts/engine/v2_online_room_server.py scripts/tests/test_v3_online_plan_revision.py` both passed. Local online regression room `BM8SZS` kept both `東海道線: 東京 -> 横浜` and `東横線: 横浜 -> 菊名`. After GitHub Pages and Render deployed the fix, 10 post-fix public rooms completed (`OM1EKK`, `048CAM`, `SZFZIM`, `DX66I3`, `9WWXLI`, `PJSER4`, `8IGT3U`, `298LJI`, `2I7IUI`, `YVXM4O`); all reached LIVE, and every selected multi-leg plan row persisted, including prior failures `東海道線 -> 東横線`, `山手線 -> 京急`, `丸ノ内線 -> 南北線`, plus 京葉線 -> 有楽町線, 都営新宿線 -> 三田線, 総武快速線 -> 半蔵門線, 山手線 -> 西武新宿線, 新幹線 -> JR/東急, 横須賀線 -> 東横線 -> 副都心線-facing service, 上野東京ライン -> 千代田線, and 京王線 -> 井の頭線.
- 2026-04-22 14:44:11 PDT - Added `scripts/dev/run_v3_public_battle_records.py`, a dependency-free geckodriver/WebDriver harness that opens two public `v3` Firefox sessions, creates Render rooms, submits route plans, drives Ready -> Planning -> LIVE, and records each battle through the page's `match_event_log` replay system. After fixing two overly broad Hunter presets to use data-valid station groups (`中野` for 東西線 and `新宿三丁目` for 都営新宿線), the final 10-room run (`UG3K0P`, `8F29DK`, `FDALM6`, `XWRGGX`, `JGGFIF`, `XT7I7B`, `UNFPP9`, `9V8UCA`, `9KCIH4`, `YXCFQ2`) completed with `failures = 0`, every room reached `LIVE`, and battle records were saved to `reports/v3_public_battle_records_20260422_143910.json` and `.md`. Coverage included 東海道線 -> 東横線, 山手線 -> 京急, 小田急 -> 井の頭線, 丸ノ内線 -> 南北線, 東西線, 京葉線 -> 有楽町線, 都営新宿線 -> 都営三田線, 総武快速線 -> 半蔵門線, 山手線 -> 西武新宿線, 上越新幹線 -> 京浜東北線, 埼京線 -> 京浜東北線, 東海道・山陽新幹線 -> 東急新横浜線, 横須賀線 -> 東横線, 上野東京ライン -> 千代田線, 京王線 -> 井の頭線, and 京急 -> 都営浅草線 -> 京成押上線.
- 2026-04-22 14:58:30 PDT - Added `scripts/dev/render_v3_battle_timeline.py`, a reusable local renderer for v3 battle records. It reads `reports/v3_public_battle_records_20260422_143910.json`, pulls route colors from `docs/data/v3_tokyo_map_bundle.json.gz`, and writes per-game SVG diagrams plus an `index.html` under `reports/v3_battle_timelines_20260422_143910/`. The layout puts time in the center, Runner on the left, Hunter on the right, colored ride intervals beside each player, and station/transfer callouts at their event times with collision avoidance for tight transfers such as 京急 -> 都営浅草線 -> 京成押上線. Local debug rendered all 10 SVGs, verified the index references all images, and used geckodriver screenshots to visually inspect Game 1 and Game 10.
- 2026-04-22 15:09:27 PDT - Fixed the v3 battle timeline behavior-label rendering in `scripts/dev/render_v3_battle_timeline.py`: Runner moment labels now use valid SVG anchors (`end`/`start`) instead of invalid `right`/`left`, so `Board`/`Transfer`/`Alight` labels stay inside their action cards. Also converted non-proper-noun timeline UI copy to English (`Start`, `Board`, `Transfer`, `Alight`, `No capture`, `Game`, `v3 Public Battle Timeline`) while preserving Japanese station, route, and train names. Regenerated the 10 SVGs and `index.html`; `py_compile`, targeted text scans, generated-output assertions, Playwright screenshot capture, and browser bounding-box checks all passed.
- 2026-04-22 15:17:12 PDT - Updated v3 private-rail display names so game route selection and battle reports include the operator prefix for private railways: e.g. `東急東横線`, `京急本線`, `京成押上線`, `京王井の頭線`, and `西武新宿線`. The display-layer formatter in `docs/v3.html` and `ui/v3_maplibre.html` leaves Tokyo Metro and Toei common names alone (`丸ノ内線`, `都営新宿線`), while `scripts/dev/run_v3_public_battle_records.py` and `scripts/dev/render_v3_battle_timeline.py` now use bundle route ids to prefix old and future battle records. Regenerated `reports/v3_public_battle_records_20260422_143910.md` and all 10 timeline SVGs/index; py_compile, targeted report scans, and a local Playwright check of `routeTitle()` all passed.
- 2026-04-22 15:23:16 PDT - Fixed the remaining v3 battle-report/timeline route display names by adding the same public route-name mapping used by the game UI to `scripts/dev/run_v3_public_battle_records.py` and `scripts/dev/render_v3_battle_timeline.py`. Regenerated the latest Markdown report and all 10 SVG timelines so internal names like `JR_EAST_TOKAIDO`, `TOEI_ASAKUSA`, `SHINKANSEN_JOETSU`, and numbered Tokyo Metro names now render as `東海道線`, `都営浅草線`, `上越新幹線`, and `丸ノ内線`; human-readable battle reports also no longer show `SG_*` station ids after start stations. Verified with py_compile, targeted output scans for internal ids/names, and a local Playwright `routeTitle()` check covering JR, Metro, Toei, Shinkansen, Tokyu, and Keikyu.
- 2026-04-22 16:14:30 PDT - Extended `scripts/dev/run_v3_public_battle_records.py` with `--mode heavy`, generated 10-leg public battle scenarios, exact-trip plan locking, non-Shinkansen/daytime route guards for long auto plans, and expected-capture assertions for `same_node` and `same_train`. The final public GitHub Pages + Render run completed 10/10 rooms (`A1WUIT`, `XPTWML`, `6343B0`, `CN5Q35`, `15331T`, `LQC25I`, `SRTWKN`, `U5CDC5`, `TJAMB5`, `2HEYZZ`) with every Runner/Hunter plan resolving 10 legs, no issues, same-node capture at `東京 06:00`, same-train capture on `日比谷線 07:02`, and eight long no-capture games each producing 44 replay events. Saved the final records to `reports/v3_public_battle_records_20260422_161132.json` and `.md`, and regenerated the Markdown after adding report-name fixes for `JR_TOHOKU` -> `宇都宮線` and `小田原線` -> `小田急小田原線`.
- 2026-04-22 16:17:58 PDT - Rendered the heavy 10-game battle records into reusable timeline pictures under `reports/v3_battle_timelines_20260422_161132/`, including per-game SVGs plus `index.html`. Updated `scripts/dev/render_v3_battle_timeline.py` with the same report-name fixes for `宇都宮線` and `小田急小田原線`, regenerated all 10 pictures, verified with `py_compile`, scanned outputs for internal route/station labels, and opened the index in Playwright to confirm all 10 visualizations load.
- 2026-04-22 16:23:11 PDT - Regenerated the heavy battle timeline pictures as longer SVGs after noticing the 10-leg games could be clipped or too compressed. `scripts/dev/render_v3_battle_timeline.py` now defaults to `14px/minute` and calculates final SVG height from actual ride-card and moment-label bottoms, not just the last event minute. Re-rendered all 10 files in `reports/v3_battle_timelines_20260422_161132/`; heights now range from `2134px` to `4794px`, output scans still show no internal route/station labels, and Playwright confirmed the index loads all 10 images.
- 2026-04-22 16:45:43 PDT - Added reusable v3 regression tooling: `scripts/dev/audit_v3_display_names.py` scans bundle-derived display names plus Markdown/HTML/SVG surfaces for internal route/station labels, numbered subway names, and missing private-operator prefixes; `scripts/dev/run_v3_public_heavy_regression.py` runs or validates heavy public battle records, checks 10+ legs, expected capture outcomes, room phases, recorded issues, optionally regenerates timelines, and runs the display-name audit. Verified with `py_compile`, a direct display audit over the latest heavy report/timelines, and both validation-only and render-enabled regression runs against `reports/v3_public_battle_records_20260422_161132.json`.
- 2026-04-22 17:14:27 PDT - Fixed v3 selected-train map highlighting in `docs/v3.html` and `ui/v3_maplibre.html`. Selecting a train now hides the broad selected-route geometry/route-stop halo and draws only the train's future physical stop-to-stop segments, with each segment colored by the physical route chosen for that segment. Segment route selection now prefers boardable physical routes, then physically connected station routes, and only then falls back to the service route; missing circular-route slices can use the all-track graph while preserving the train's own route id/color. Playwright verified that a selected 山手線 train from 東京 has `selected-route-path` empty, all selected trip features use 山手線, and the route-stop halo is hidden; a 日比谷線 through-service on the 東武 side starts with 東武日光線 then 東武伊勢崎線 physical segments.
- 2026-04-22 17:21:07 PDT - Tightened v3 physical train filtering after a screenshot showed `副都心線` at 池袋 listing JR 埼京線/川越線 trains via a transitive `相鉄本線` alias overlap. Borrowed-route representation for a train now requires the selected route to have real route-pattern stops at both the boarding station and the next stop, rather than accepting broad nearby geometry matches such as 副都心線 near 新宿三丁目 for a 池袋 -> 新宿 JR segment. Playwright reproduced the issue and verified the fix: `副都心線` departures at 池袋 no longer include the `07:03 指扇 -> 新木場` JR train, that JR train's boardable routes are only `埼京線・川越線`, and a real 副都心線-facing through service from 池袋 highlights `副都心線` for the first physical segment.
- 2026-04-22 17:28:43 PDT - Fixed the山手線 selected-train freeze introduced by the previous physical-highlight fallback. Selecting a 東京発 山手線 train spent about `36s` in `tripPathSegments()` because a missing local route slice triggered `globalGraphSliceCoordinates()`, which built the full Tokyo track graph on the UI thread. `coordinatesForTripSegment()` now falls back to a lightweight adjacent-station connector when a route can anchor both stops but lacks a direct slice, preserving the train's route id/color without building `allTrackGraph`. Playwright timing dropped 山手線 `tripPathSegments()` to about `51ms`, `setSelectedTrip()` stayed around `257ms`, no all-track graph was created, selected-route geometry stayed hidden, and the 池袋 `副都心線` regression still excluded 埼京線・川越線 trains.
- 2026-04-22 17:55:53 PDT - Corrected v3 selected-train whole-future physical highlighting in `docs/v3.html` and `ui/v3_maplibre.html`. The selected-trip path still renders every future stop-to-stop segment, but segment route choice now scores trip identity, selected route identity, normalized through-corridor aliases, physical service at both endpoints, and boundary bridges instead of trusting the shortest nearby geometry. Browser probes verified the 池袋 17:01 `副都心線` through-service renders 13 future features split as `副都心線` 3 + `東急東横線` 6 + `横浜高速鉄道みなとみらい21線` 4, keeps broad route geometry/stop halo hidden, and still excludes JR 埼京線/川越線 rows; 東京発 山手線 renders 53 future 山手線 segments in about `251ms` without building `allTrackGraph`; boundary audits passed for 京急本線, 京成押上線, 京急空港線, みなとみらい21線, and 池袋 -> 小竹向原 副都心線.
- 2026-04-22 17:58:38 PDT - Pushed `0a3103d` (`Fix v3 whole-trip physical highlights`) and verified GitHub Pages had deployed the new `v3.html` (`Last-Modified: Thu, 23 Apr 2026 00:55:55 GMT`). A public URL browser logic smoke against `https://eigenoperator.github.io/OniChase/v3.html` confirmed the 池袋 `副都心線` through-service still renders 13 future features split as `副都心線` 3 + `東急東横線` 6 + `横浜高速鉄道みなとみらい21線` 4, no JR 埼京線/川越線 rows appear, and 東京発 山手線 remains all `山手線` with `setSelectedTrip()` around `249ms` and no `allTrackGraph`.
- 2026-04-22 18:44:44 PDT - Added the first executable v3 axiom regression suite. New `scripts/tests/v3_browser_probe.js` runs the official `docs/v3.html` through a local Playwright browser with a MapLibre stub and real local bundles, while `scripts/tests/v3_browser_test_utils.py` provides local HTTP-server helpers. New unittest files cover AXIOMS text, public/local page sync, Tokyo major route separation, Tokyo Metro numbered-name suppression, 池袋 `副都心線` no-`埼京線・川越線` filtering, Shinkansen/ordinary separation, strict transfer-equivalence/same-train capture behavior, 京急/京成/みなとみらい boundary physical segments, and selected-train golden highlights for 池袋 17:01 `副都心線` and 東京発 `山手線`. `python3 -m unittest discover -s scripts/tests` passed `12` tests in about `8s`.
- 2026-04-22 19:06:45 PDT - Added the first P1/P2 v3 hardening layer. `scripts/dev/normalize_v3_replay_record.py` normalizes public battle records into canonical `v3.replay.1` entries with dataset, game-rules, source, player, plan, phase, capture, and result fields, then validates the latest 10 heavy public games. `scripts/build/build_v3_page.py` treats `docs/v3.html` as the source of truth for `ui/v3_maplibre.html`, and `scripts/dev/build_web_client_bundle.py` now writes the local mirror from the public page. Added replay schema and page-build unittests; `python3 -m unittest discover -s scripts/tests` passed `17` tests, `build_v3_page.py --check` passed, and replay validation passed on `reports/v3_public_battle_records_20260422_161132.json`.
- 2026-04-22 20:07:09 PDT - Promoted v3 replay into the product UI. `docs/v3.html` now exposes the Result/Replay panel and toolbar Replay button, builds canonical browser-side `v3.replay.1` records, supports copyable `#replay=...` links and JSON, reloads shared replay hashes after timetable load, and shows three fixed replay answers: Runner plan, Hunter gap, and capture outcome. Synced `ui/v3_maplibre.html` from the public page, added the `replay-core` browser probe plus `scripts/tests/test_v3_replay_ui.py`, and verified `python3 -m unittest discover -s scripts/tests` passed `20` tests; `build_v3_page.py --check` and targeted replay UI unittest also passed.
- 2026-04-22 20:15:58 PDT - Started the v3 player-understanding P0. The entry overlay in `docs/v3.html` now offers `Quick Play`, `Tutorial`, and `Advanced Setup` instead of exposing only Single Player/Multiplayer first. Quick Play creates a short `06:00`-`08:00` Tokyo game, finds real starter rides for Runner and Hunter, and starts a `30s` planning countdown; Tutorial enters Runner planning with a sidebar checklist for line, train, alighting station, plan validity, LIVE/replay, and a through-service note; Advanced Setup reveals the existing local and room controls. Synced `ui/v3_maplibre.html`, added the `entry-modes` browser probe plus `scripts/tests/test_v3_entry_modes.py`, and verified the targeted entry test, `build_v3_page.py --check`, `git diff --check`, and `python3 -m unittest discover -s scripts/tests` (`24` tests) all passed.
- 2026-04-22 20:24:33 PDT - Removed Quick Play from the v3 entry flow as requested. The entry overlay now keeps only `Tutorial` and `Advanced Setup`; all Quick Play UI, event listeners, starter-plan helper code, and Quick Play test coverage were deleted. Expanded Tutorial into a more detailed guide with step-by-step line/train/alighting/valid-plan/LIVE explanations plus demonstrations for 東京 -> 山手線 -> 渋谷/新宿, 池袋 副都心線 through-service into 東急東横線 / 横浜高速鉄道みなとみらい21線, and same_node/same_train capture explanation. Synced `ui/v3_maplibre.html`, updated the `entry-modes` probe/tests to assert Quick Play is absent, and verified targeted entry tests, page sync, `git diff --check`, and full `python3 -m unittest discover -s scripts/tests` (`23` tests) all passed.
