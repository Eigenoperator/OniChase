# Daily Memory - 2026-04-19

- 2026-04-19 21:45:19 PDT - Switched `v3` to reuse the main `v2` UI interaction layer instead of maintaining a separate Tokyo-map UI: made `ui/v2_web_client.html` accept injected data/tile URLs, added `scripts/ingest/build_v3_tokyo_v2_bundle.py`, generated `data/v3_tokyo_bundle.json` / `docs/data/v3_tokyo_bundle.json` with `1529` physical stations, `302` service routes, `4147` track centerlines, and `39318` v2-compatible trip instances, changed `docs/v3.html` generation to derive from `ui/v2_web_client.html`, and verified in Playwright that v3 single-runner mode can click Tokyo, show `36` Planning Actions departures, choose one train, and show downstream ride choices.
