# Daily Memory - 2026-04-09

- 2026-04-09 13:27:07 PDT - Reviewed the recent `memory` formatting issue, confirmed that daily memory entries must stay in the single-line `- YYYY-MM-DD HH:MM:SS PDT - ...` format, and checked the stray untracked `Session` file in the repo root, confirming that it is an empty file unrelated to gameplay or runtime logic.
- 2026-04-09 13:59:35 PDT - Traced a persistent `v2` map-label scaling complaint to an unpublished local fix, then changed the web map so station labels are rendered in screen space outside the zoomed viewport instead of being scaled together with the map geometry.
- 2026-04-09 14:08:46 PDT - Adjusted the `v2` web map labels again after publication: raised the minimum label size so names stay readable, and converted label offsets to fixed screen-space pixels so labels stay visually attached to stations while panning and zooming.
- 2026-04-09 14:25:26 PDT - Fixed a deeper `v2` label-lag bug by updating the screen-space label layer immediately inside `applyViewport()`, so station names now move with the map during drag and zoom instead of waiting for a later full render cycle.
- 2026-04-09 15:26:58 PDT - Increased the `v2` label size range again for better mid/high-zoom readability by raising both the minimum and maximum screen-space font sizes and slightly thickening the halo stroke.
- 2026-04-09 15:43:00 PDT - Increased the `v2` station-label size much more aggressively, targeting a visibly larger screen-space range closer to 1.5x the previous readable pass after the user said the names were still too small.
- 2026-04-09 15:53:15 PDT - Corrected the `v2` Nishi-Kyushu map relationship after realizing the earlier visual fix was wrong: removed the fake direct `Shin-Tosu -> Takeo-Onsen` connector from the published page and trimmed the rendered Nishi-Kyushu geometry so the map no longer shows a non-real direct connection.
- 2026-04-09 16:05:41 PDT - Improved the `v2` multiplayer UX by adding copyable room-code UI in the lobby and in-game panel, surfacing clearer opponent joined/ready status, and moving essential room actions into a visible dedicated `Room` panel instead of the hidden temporary session area.
- 2026-04-09 16:20:15 PDT - Added the next `v2` multiplayer polish pass: room creation now automatically copies the generated room code, and the in-game `Room` panel now shows a much more prominent room-code pill so players can still read and share the id after entering the match.
- 2026-04-09 16:24:24 PDT - Reworked `v2` multiplayer flow into a proper ready room: after creating or joining a room, players now wait in a dedicated pre-game room, can copy the room code from the top-right area, see both seats and ready states, toggle ready/unready, and automatically enter the main game only after both sides are ready.
- 2026-04-09 16:28:37 PDT - Fixed key usability gaps in the new `v2` ready room: the seat choice is now visible and switchable inside the ready room itself, and players can mark themselves ready even before the opponent has joined, instead of the button remaining disabled until both seats were already filled.
- 2026-04-09 16:35:07 PDT - Added clearer `v2` multiplayer join/create failure messages in the lobby so room problems now surface as explicit causes like `seat_occupied`, `room_not_found`, invalid seat ownership, or public-server fetch failures instead of silently looking like the room could not be joined.
- 2026-04-09 17:25:44 PDT - Fixed two deeper `v2` issues together: the authoritative online room flow now gives the initial `06:00` phase and each hourly replanning phase a real 60-second `PLANNING` lock before `LIVE`, with online `Start Game` restored to end planning early once both players start; and player movement while riding now follows the real Shinkansen route geometry instead of a straight line between stations.
- 2026-04-09 17:30:40 PDT - Added a formal project axiom that single-player and multiplayer must always preserve the same core gameplay loop, timing rules, and planning/live/capture logic, since single-player is fundamentally the same game against an AI or automated opponent rather than a separate ruleset.
- 2026-04-09 18:33:01 PDT - Fixed a `v2` hunter planning setup bug in the web client by making the lobby seat choice immediately set the correct default match window (`07:00 -> 18:00` for Hunter, `06:00 -> 18:00` for Runner) before room creation, and by marking the hunter test preset as `input_mode: plan` so the hunter side is treated as a real planning participant rather than a passive actions-only placeholder.
- 2026-04-09 18:37:17 PDT - Simplified the main `v2` sidebar by hiding the in-game `Room`, `Plan Cursor`, and `Selected Station` sections from the public play view, and repurposed the top summary box into a concise `Next Plan` display that shows the current state plus the next few planned legs in a clearer player-facing format.
- 2026-04-09 18:44:53 PDT - Fixed two `v2` map-state presentation bugs: removed the automatic default station selection so `Tokyo` no longer stays permanently highlighted red on load, mode switch, or preset load; and tightened online preview fallback so a player still sees their own current position during `PLANNING` even if authoritative presence updates arrive slightly late.
- 2026-04-09 18:53:14 PDT - Fixed a `v2` planning-map timing bug where player positions during later `PLANNING` phases were still sampled from the match start minute instead of the current game minute, which incorrectly snapped cases like `Runner boards Nozomi 1 at 06:00` back to `Tokyo` when the game re-entered planning at `07:00`.
- 2026-04-09 18:57:19 PDT - Reworked high-zoom `v2` player rendering so `Runner` / `Hunter` markers no longer collapse into nearly invisible unlabeled dots: the map still keeps a tiny true-position point on the route geometry, but high-zoom player identity and current station/train text now render in a separate screen-space overlay layer that updates immediately during drag and zoom.
- 2026-04-09 19:00:28 PDT - Locked `v2` player markers to a constant screen size at every zoom level by moving the full `Runner` / `Hunter` badge entirely into the screen-space overlay layer and leaving only a tiny true-position anchor on the scaled map geometry, so marker size now stays at the initial readable size instead of shrinking or growing with the map.
