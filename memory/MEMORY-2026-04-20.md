# Daily Memory - 2026-04-20

- 2026-04-20 11:47:04 PDT - Re-read `AXIOMS.md`, `MEMORY.md`, `WORKSPACE.md`, and `STATUS.md` after Scorp reminded me to read axioms first; found `diary/DIARY-2026-04-19.md` missing and `STATUS.md` over the 50-line limit, then backfilled the 2026-04-19 diary and trimmed `STATUS.md` to a 33-line handoff document with the next v3 task focused on station-group and line-route QA.
- 2026-04-20 11:51:07 PDT - Added `scripts/ingest/audit_v3_tokyo_bundle.py` and generated `data/v3_tokyo_bundle_audit.json` as the first reusable v3 bundle QA report. The report confirms the next work should be identity cleanup before gameplay migration: `201` real train station keys do not map into the bundle, `134` same-name map station clusters are physically distinct but currently key-collapsed, `199` routes are tiny/empty, `10` routes are very broad, and the most obvious route issue is a map-vs-train route-id mismatch where map-derived routes use Japanese operator names while train-derived routes use normalized `operator_id`.
- 2026-04-20 12:37:16 PDT - Added the reusable `scripts/ingest/v3_station_identity.py` layer and rebuilt the v3 Tokyo bundle so timetable stops, physical map stations, and gameplay station groups now align. The bundle now has `1673` station groups, `2039` physical stations, `204` service routes, and all `39450` unified trains with `skippedTrainCount = 0`; audit now reports `train_stations_without_map_count = 0` and `trains_with_unmapped_stops_count = 0`.
- 2026-04-20 12:43:27 PDT - Added `scripts/ingest/v3_route_identity.py` and normalized route identity for v3 Tokyo, especially Odakyu limited-express train names that had been incorrectly treated as standalone route cards. Rebuilt the bundle to `108` service routes while preserving all `39450` trips; audit now reports `tiny_route_count = 0`, `collapsed_duplicate_map_name_count = 0`, and no broken bundle references.
- 2026-04-20 13:18:39 PDT - Switched the v3 Tokyo public bundle pipeline from uncompressed `v3_tokyo_bundle.json` to `v3_tokyo_bundle.json.gz`, reducing the published bundle to `6.6M`. Updated the v2/v3 shared web client to load gzip JSON with `DecompressionStream`, rebuilt `docs/v2.html` and `docs/v3.html`, removed the old uncompressed v3 bundle files, and smoke-tested localhost serving plus Node `fetch + DecompressionStream` loading.
- 2026-04-20 13:46:36 PDT - Optimized v3 Tokyo map loading and zooming locally. Added generated `v3_tokyo_tiles` for Tokyo instead of reusing the legacy Shinkansen tile pyramid, split v3 into a `1.3M` map-first bundle plus a delayed `5.4M` timetable bundle, added bundle indexes for trips/routes/stations, switched station departures to indexed lookup, and added lightweight SVG scale refresh so wheel zoom updates line widths and station dots without rebuilding the whole map on every scroll event. Static JS checks and localhost resource smoke tests pass; Firefox headless screenshot still times out in this machine's SWGL headless renderer, so real-browser visual confirmation remains best done manually.
- 2026-04-20 14:09:03 PDT - Created the first standalone MapLibre renderer spike for v3 at `ui/v3_maplibre.html` and generated `docs/v3_maplibre.html`. The page keeps gameplay/timetable logic separate while moving map rendering to MapLibre GL JS: WebGL track/service layers, zoom-expression line and point sizing, collision-aware station labels, station click, route selection, route-stop highlighting, and lazy timetable loading for station departures/train stop highlighting. Added the MapLibre entry to the generated landing page and verified JS syntax plus local resource loading; Firefox headless still times out in the local SWGL renderer.
- 2026-04-20 14:19:58 PDT - Fixed v3 MapLibre line colors so routes, track centerlines, and service path tiles use the line colors already present in the Tokyo physical-line data instead of the old default blue. Added color normalization, route aliases, fallback operator colors, contrast-aware route text colors, and regenerated the map/timetable bundles plus `docs/data/v3_tokyo_tiles`; local tile smoke tests now show multi-color service tiles with examples like Keio orange, Keikyu red, Tokyo Metro Ginza orange, Marunouchi red, and Toei Oedo magenta.
- 2026-04-20 14:30:36 PDT - Rechecked the v3 color issue after Scorp pointed out the line toward `浦和美園` still looked uncolored. The root cause was not an empty color field but the missing `埼玉高速鉄道線` physical geometry in the Tokyo network seed; added the complete Saitama Railway company line from N02, assigned SR blue `#00A6E9`, regenerated the v3 map/service bundles and Tokyo tiles, and verified `0` missing/white/transparent/fallback-gray colors across routes, track centerlines, service geometry, and generated tiles.
- 2026-04-20 14:37:03 PDT - Promoted the MapLibre renderer to be the real v3 website. Updated `scripts/dev/build_web_client_bundle.py` so `docs/v3.html` is generated directly from `ui/v3_maplibre.html`, removed the separate `docs/v3_maplibre.html` public page, removed the landing-page secondary MapLibre link, and verified `docs/v3.html` contains MapLibre while the old v2-style lobby/ready-room v3 page is gone.
- 2026-04-20 15:04:56 PDT - Connected the first v2-style gameplay shell into the official v3 MapLibre page without rewriting the game rules: `ui/v3_maplibre.html` now has runner/hunter role switching, game clock, planning/live/ended flow, hourly replanning, plan board, train outlook, selected-train downstream stop choice, player markers, selected-trip real-geometry highlighting, same-node/same-train live capture, and local replay simulation over the `39450` Tokyo train instances. Rebuilt `docs/v3.html` and verified the public artifact with Playwright: MapLibre loads, timetable lazy-loads, runner can choose a train and destination, hunter sees only `07:00+` departures, and Start advances into LIVE with the runner moving on the selected service.
- 2026-04-20 15:05:53 PDT - Reflected the v3 MapLibre promotion and gameplay-shell milestone in `HISTORY.md`, keeping `STATUS.md` under the 50-line handoff limit.
- 2026-04-20 15:20:02 PDT - Made `scripts/engine/v2_online_room_server.py` dataset-configurable instead of hardcoding the Shinkansen bundle. The server now supports `--dataset shinkansen` / `--dataset v2-shinkansen` / `--dataset v3-tokyo`, plus explicit `--bundle` and `--timetable` overrides; gzip JSON is supported, v3 Tokyo loads `data/v3_tokyo_map_bundle.json.gz` with `data/v3_tokyo_timetable_bundle.json.gz`, dataset-specific runner/hunter defaults are taken from bundle metadata, and `/health` reports dataset name, bundle paths, trip count, unique trip id count, duplicate trip id count, and default starts. Smoke tests passed for both default Shinkansen (`1139` trips, `0` duplicate trip ids, runner `SG_TOKYO`, hunter `SG_SHIN_OSAKA`) and v3 Tokyo (`39450` trips, `39417` unique trip ids, `33` duplicate trip ids, runner `SG_TOKYO`, hunter `SG_SHINJUKU`).
- 2026-04-20 15:41:52 PDT - Added the first v3 multiplayer client entry to `ui/v3_maplibre.html` and regenerated `docs/v3.html`. The official v3 page now opens with a single-player/multiplayer entry overlay, supports v3 Tokyo room creation/join/copy, Runner/Hunter seat selection, ready/unready, a main-board Room panel, online plan submission after plan edits, and server-projected player presence while preserving the same v2-style planning/live/capture rules. Added `docs/data/v3_online_config.json` with an intentionally empty `server_url` so the public page does not point at the v2 Render backend by mistake. Local Playwright smoke tests passed for single-player train selection/start-live and two-player room create/join/ready/start-live against `python3 scripts/engine/v2_online_room_server.py --dataset v3-tokyo`.
- 2026-04-20 15:57:36 PDT - Prepared the independent v3 Tokyo Render deployment path. Updated `render.yaml` so it now declares two services: `onichase-v2-room-server` explicitly running `--dataset shinkansen`, and `onichase-v3-room-server` running `--dataset v3-tokyo`. Pointed `docs/data/v3_online_config.json` at `https://onichase-v3-room-server.onrender.com` and updated `ONLINE_DEPLOYMENT.md` with separate v2/v3 commands plus expected `/health` checks. Verified locally that the v3 start command serves `/health` with `dataset_name = v3-tokyo`, `trip_count = 39450`, and `station_group_count = 1673`; the expected public v3 URL still returns `Not Found` because the Render service has not been provisioned from this environment.
- 2026-04-20 16:11:06 PDT - Cleaned the official v3 multiplayer entry page. Removed visible developer/test text such as the `--dataset v3-tokyo` command and `planning / live / capture payload`, removed Ready/Unready from the initial entry screen, added a dedicated `Ready Room` shown only after successful create/join, moved room-code copy plus Ready/Unready into that room, and changed raw `NetworkError` / fetch failures into the player-facing message `在线服务暂时不可用，请稍后再试。` Rebuilt `docs/v3.html` and verified with Playwright: entry text no longer leaks test copy, single-player entry still opens the game, local v3 room creation opens the Ready Room, a second browser can join as Hunter, both Ready buttons start the match, and the offline public endpoint path shows the friendly service-unavailable message instead of raw browser errors.
- 2026-04-20 16:24:47 PDT - Hid the non-actionable v3 right-sidebar header/status/stat block. `ui/v3_maplibre.html` now marks the `Planning Rail` side head and four dataset statistic cards as `hidden`, with a global `[hidden] { display: none !important; }` guard so existing `.stats { display: grid; }` CSS cannot override it. Rebuilt `docs/v3.html` and verified in Playwright that the side head/stat cards are not displayed and the sidebar begins at `Current Plan`.
- 2026-04-20 16:35:50 PDT - Reworked the v3 gameplay sidebar into the requested three-layer planning flow. The visible right sidebar now keeps `Current Plan` plus a `Planning` selector only; Room, Result/Replay, Selected Station, Selected Route, and Service Routes are hidden from the sidebar. `renderTrainOutlook()` now groups available departures at the plan cursor station into line choices, then filters trains by the selected line, then expands downstream stops for the selected train; on-train planning shows the current line/current train before downstream stops. Rebuilt `docs/v3.html` and verified with Playwright that Tokyo starts with line choices, choosing a line reveals train choices, choosing a train reveals destination stops, and clicking a stop adds `LEG 1` to the plan.
- 2026-04-20 16:48:33 PDT - Switched v3 MapLibre display names to Japanese-original first for station names, line names, and train labels. Station labels now prefer `displayNameJa`/`names.ja`, MapLibre symbol layers render `name_ja` before `name_en`, code-like route names such as `JR_EAST_CHUO_RAPID`, `SHINKANSEN_TOKAIDO_SANYO`, `TOEI_ASAKUSA`, `RINKAI`, and `TOKYO_MONORAIL_HANEDA` are mapped to Japanese route names, and generic English service labels such as `JR East`/`Keikyu` now fall back to the Japanese route name while Shinkansen names such as `Nozomi`/`Hayabusa` become `のぞみ`/`はやぶさ`. Rebuilt `docs/v3.html`; static QA reports `0` unmapped code-like routes, `0` unhandled pure-ASCII service names, and JS syntax checks pass for both source and generated v3 pages.
- 2026-04-20 17:05:05 PDT - Tightened the v3 planning sidebar and selected-train map highlight. The Planning section no longer shows the large current-station summary/action card; train choice rows now hide regular opaque train numbers and only keep numbers for Shinkansen / limited-express style services, while the subtitle changed from "next few stops" to `始発 / 終点 / 次`. Selected-train highlighting now builds MapLibre `LineString` features only from real service/track geometry, never from station-to-station straight-line fallback; added route-to-track aliases so examples like `のぞみ`, `京浜東北線・根岸線`, `京急`, and `りんかい線` all resolve to real geometry segments. Rebuilt `docs/v3.html`; JS syntax checks pass and geometry QA confirms real-polyline slices for the checked Nozomi, JR East regular, Keikyu, and Rinkai examples.
- 2026-04-20 17:11:50 PDT - Refined v3 destination selection rows. `availableDepartures()` now filters out trains where the current station is already the terminal stop, so terminal-only rows no longer appear as `No later stops`; downstream destination buttons now show only arrival time plus station name, removing `Add this destination to your plan` / `Set as destination`. Added compact per-route transfer chips beside destination station names using the existing route colors/text colors and Japanese route titles; this is a data-backed placeholder for future official logo assets, not a full official-line-logo collection yet. Rebuilt `docs/v3.html`; source/generated JS syntax checks pass and data QA shows `39450` terminal stop rows filtered from boardable choices.
- 2026-04-20 17:34:39 PDT - Upgraded the v3 destination-stop transfer display from placeholder chips to real line-symbol assets. Destination rows now have taller spacing and wrapping station names so long Japanese station names are not clipped; Tokyo Metro transfer badges use the official route-symbol JPGs downloaded from Tokyo Metro's trademark asset package, and the current v3 route set now has 52 additional JR/Toei/private/third-sector route-symbol SVG/PNG assets under `docs/assets/transit/route_symbols`. The sidebar now prefers image assets via `routeLogoAsset()` and falls back to generated code badges only if no real asset exists; rebuilt `docs/v3.html`, verified JS syntax, confirmed all literal asset references resolve locally, and smoke-tested `v3.html`, Metro JPG, JR SVG, and SR SVG over the local docs server.
- 2026-04-20 17:53:49 PDT - Fixed two v3 Tokyo data/UI correctness bugs from Scorp's report. Keikyu train collection now normalizes station names with NFKC before matching official timetable rows, so full-width `羽田空港第１・第２ターミナル` rows map to the real half-width station IDs; rebuilt Keikyu to `2334` trains and the full v3 timetable to `40456` trips, with `966` Keikyu stops at both `羽田空港第3ターミナル` and `羽田空港第1・第2ターミナル`. The v3 sidebar transfer badges now filter Tokyo Metro through-service aliases and dedupe by physical line symbol, so stations like `京急蒲田` show a single `KK` instead of direct-through clutter.
