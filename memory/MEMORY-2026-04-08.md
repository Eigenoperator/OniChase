# MEMORY-2026-04-08

- 2026-04-08 00:00:00 PDT
  New daily memory file created.
  Primary focus today should continue on the main `v2` online playable version, especially multiplayer usability and the newly redesigned planning-rail UI.
- 2026-04-08 09:18:00 PDT
  Continued the `v2` UI redesign instead of changing gameplay logic.
  I formalized the design direction in `UI_BRIEF_V2.md`, then pushed the main board further toward the brief by strengthening the time layer, converting the lower plan area into route-leg cards, and restructuring the right side into a more railway-like planning rail.
- 2026-04-08 09:44:00 PDT
  The user asked to revert the right sidebar layout and overall color treatment.
  I restored `ui/v2_web_client.html` and `docs/v2.html` to the earlier stable shell state from before the redesign while keeping the newer gameplay and multiplayer logic intact.
- 2026-04-08 09:53:00 PDT
  The user clarified that only the right sidebar layout and the color treatment should be rolled back, while the stronger parts of the redesign should stay.
  I therefore kept the stable sidebar and lighter palette, but restored the stronger top time layer with the large clock and phase badge.
- 2026-04-08 14:46:00 PDT
  The user said the left-side diagram was unnecessary.
  I removed the route diagram block from the main `v2` board, letting the map panel return to a single full gameplay stage while keeping the newer top time layer and the restored stable right sidebar.
- 2026-04-08 15:03:00 PDT
  The user wanted the top bar compressed.
  I removed the `OniChase V2` heading from the gameplay header, centered the game clock, kept only the Runner/Hunter toggle plus Start and Pause in the top bar, and moved the other utility controls into the right-side Session area.
- 2026-04-08 15:10:00 PDT
  The user asked to hide the `Session` and `Bundle` panels for now.
  I hid both sections from the public `v2` sidebar while leaving their underlying logic and controls in the codebase for later reuse.
- 2026-04-08 15:22:00 PDT
  The user said the map still could not zoom far enough and that labels and station dots were too large after zooming.
  I raised the `v2` zoom ceiling and changed the station-circle and label scaling rules so dense corridors can be inspected at a larger scale while text and station symbols shrink with zoom instead of dominating the screen.
- 2026-04-08 15:31:00 PDT
  The user said the station circles were still far too large after zooming.
  I pushed the `v2` zoom ceiling higher again and made the station radius, stroke width, selected halo, and future-stop ring all shrink more aggressively with zoom.
- 2026-04-08 15:39:00 PDT
  The user then said the runner/hunter markers and the rail lines were also too large after zooming in.
  I changed the `v2` map so corridor lines, service lines, hovered-trip emphasis, and the runner/hunter markers all shrink with zoom instead of continuing to balloon visually at high magnification.
- 2026-04-08 15:56:00 PDT
  The user clarified that all station positions must remain real and that dense regions should be handled only through rendering rules.
  I therefore tightened the `v2` high-zoom display rules without altering coordinates: labels appear later and shrink more, rail widths thin more aggressively, and runner/hunter markers collapse into tiny point-only markers at high zoom.
