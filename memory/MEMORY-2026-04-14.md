# Daily Memory - 2026-04-14

- 2026-04-14 12:56:18 PDT - Performed the daily sync for today: reviewed the current diary/memory chain, confirmed that `diary/DIARY-2026-04-13.md` and `memory/MEMORY-2026-04-14.md` were still missing, and prepared to backfill yesterday's diary plus today's exact-second memory before continuing the next `v2` / `v3` development pass.
- 2026-04-14 16:09:43 PDT - Extended the `v3` Tokyo train-data pass: confirmed valid first-batch weekday train-instance outputs for `Tokyo Monorail` (`429` trains across `11` stations) and `Tama Monorail` (`507` trains across `19` stations), updated the source inventory to mark both monorail systems as collected, and started company-wide ingestion scripts for `Odakyu` and `Tokyu` based on their official timetable systems.
- 2026-04-14 16:42:38 PDT - Continued the Tokyo private-rail collection push without changing scope: `Odakyu` grew to a `10000`-train company-wide checkpoint, `Tokyu` grew to a `4500`-train checkpoint, and `Keikyu` moved from a seed-only station-page crawler to a real `T5 -> T7` single-train stop-table collector with its first `500`-train checkpoint; the source registry and source inventory were updated to mark all three as actively collecting.
- 2026-04-14 16:47:06 PDT - Kept the train-data push moving past the first three private-rail checkpoints: verified `Keikyu` single-train parsing end-to-end, confirmed the live collectors had stabilized at `Odakyu 10000`, `Tokyu 4500`, and `Keikyu 500`, and started opening the next operator path by probing `Keio` and `Tsukuba Express` Nuxt bundles for their hidden timetable/stop-detail routes and API conventions.
- 2026-04-14 17:20:39 PDT - Broke open the next two operator paths in the Tokyo train-data pass: completed a first `Tsukuba Express` weekday batch with `463` real train instances using the official Navitime-backed `/api/tx/trains/timetable/weekday/{direction}` endpoints, and verified the official `Keio` API chain (`/api/keio/timetable/{station}/{line}/{direction}` plus `/api/keio/stops/{station}/{line}`), with the company-wide collector now checkpointing into `data/v3_tokyo_keio_weekday_train_instances.json` (`450` trains at the current checkpoint). I also confirmed that Tokyo Metro station pages route into the official `transfer.tokyometro.jp/website/timetable` app and that its Nuxt bundle exposes `browserBaseURL=https://transfer.tokyometro.jp/api`, which gives us a real official API base for the next collector step.
