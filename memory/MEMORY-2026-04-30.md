# Daily Memory - 2026-04-30

- 2026-04-30 12:26:24 PDT - Updated v4 limited-express train-label formatting so player-facing selected-train labels keep the public train number but do not append `号`. Plain cases now render like `ひたち3`, and combined limited expresses normalize internal suffixes too, e.g. `はしだて2・まいづる6` instead of `はしだて2号・まいづる6`. Extended `scripts/tests/v4_route_choice_audit.js` to fail if any non-shinkansen limited express selected-train label contains `数字+号`. Full v4 route-choice audit passed with `anomalyCount: 0`. No room server deploy.
- 2026-04-30 12:57:26 PDT - Audited over-separated v4 named-train route choices nationwide. Local scan initially found 675 synthetic named choices, including many false positives where line/system/operator names or ordinary rapid service types were being treated as named trains (`鹿児島市電２系統`, `北総鉄道`, `Ａ快速`, `アーバン`, `ラビット`, etc.). Checked uncertain rapid-service cases online: `Ａ快速` is an ordinary JR West lettered rapid display, while `快速みえ` is a named rapid with reserved-seat handling, so `みえ` should stay separated. Tightened `publicLimitedExpressBaseName()` with route/system-name and ordinary-rapid filters; preserved real named trains and reviewed named rapid/liner services such as `みえ`, `マリンライナー`, `快速シーサイドライナー`, and `むさしの号`. Added `routeLikeNamedChoiceCount` to the full route-choice audit so route/system labels cannot reappear as named-train choices. Full v4 route-choice audit passed with `anomalyCount: 0`. No room server deploy.
- 2026-04-30 13:04:15 PDT - Audited duplicate player-facing route choice titles nationwide. Found 98 station/title duplicates, led by `中央線` (32 stations), `都営三田線` (23), `都営浅草線` (21), `都営大江戸線` (7), and boundary duplicates such as `東海道線`. Updated `routeChoicesFromDepartures()` to aggregate choices by player-facing title while preserving total train counts and combined operator subtitles, and updated `departureMatchesRoute()` so selecting the merged row still includes trains from all matching underlying route IDs. Added a full-audit duplicate-title scan to `scripts/tests/v4_route_choice_audit.js`. Full v4 route-choice audit passed with `anomalyCount: 0`; `新橋` now has one `都営浅草線` row with 753 trains instead of two rows. No room server deploy.
- 2026-04-30 13:31:09 PDT - Fixed selected-train labels for non-JR through-running choices so direction-side line labels win before the current adjacent platform label. This fixes `新橋` -> `都営浅草線` trains bound for the Keikyu airport side: samples ending at `天空橋` now display `京急空港線` instead of `都営浅草線`. Kept JR northern trunk special handling ahead of the generic direction rule so `常磐線`/`東北本線`/`高崎線` display is not pulled to remote southern terminals. Updated `scripts/tests/v4_route_choice_audit.js` to enforce the Asakusa/Keikyu-airport label and to reflect the current A-B-C axiom: route choices must not expose remote Yokohama through-service lines, but selected train labels may display the direction-side remote line. Full v4 route-choice audit passed with `anomalyCount: 0`. No room server deploy.
- 2026-04-30 14:35:50 PDT - Continued self-audit after the Asakusa fix. Found two nationwide data/display issues: first, branch/interchange station physical labels could create one-segment lineTrace spikes such as `小田原線 -> 江ノ島線 -> 小田原線`, `唐津線 -> 筑肥線 -> 唐津線`, and `烏丸線 -> 京都線 -> 烏丸線`; second, Tobu source rows mislabeled JR 埼京/川越/湘南新宿-like trains as `東武東上本線`, causing `大宮` to show a forbidden `東武東上本線` route choice. Updated `build_v4_gameplay_bundle.py` to smooth single-segment route spikes and skip Tobu Tojo rows that are actually JR-labeled or include impossible `大宮` stops. Added `line_trace_single_segment_spike` to `audit_v4_data_quality.py` and an `大宮`/`東武東上本線` guard to the browser route-choice audit. Rebuilt v4 gameplay data; compact trips are now 122715 with 309 skipped bad/short rows. Validation passed: v4 data-quality audit `errors=0 warnings=0`, coupled-service audit unchanged (`known=18`, `known_pair=11`, `known_combined=5`, `known_missing=1`), and full browser route-choice audit passed with `anomalyCount: 0`. No room server deploy.
- 2026-04-30 14:47:24 PDT - Fixed a false red in the v4 coupled-service audit. The gameplay timetable is currently a weekday snapshot, but the Odakyu Romancecar `はこね・えのしま` and `メトロはこね・メトロえのしま` coupled entries are holiday-only for audit purposes, so the weekday audit should not report them as missing weekday data. Added `operatingDays` and weekday notes to those registry entries, added `--service-day weekday|holiday` to `audit_v4_coupled_services.py`, and made out-of-scope entries report `not_applicable_for_service_day`. Validation passed: coupled-service audit now reports `known=18`, `known_pair=11`, `known_combined=5`, `known_missing=0`, `known_not_applicable=2`; v4 data-quality audit still reports `errors=0 warnings=0`. No room server deploy.
- 2026-04-30 15:00:13 PDT - Tightened the v4 coupled-service generic candidate scan. The previous generic scan surfaced 17 one-off same-number coincidences such as `Nozomi 12` near `はるか12号`, which are useful as raw diagnostics but too noisy for the review queue. Added `--generic-min-event-count` with default `2`, suppressing low-event generic candidates while preserving a count of suppressed items. Validation passed: coupled-service audit reports `known=18`, `known_pair=11`, `known_combined=5`, `known_missing=0`, `known_not_applicable=2`, `generic=0`, `generic_suppressed_low_event=17`; v4 data-quality audit still reports `errors=0 warnings=0`. A local browser route-choice audit was attempted but exceeded a practical wait, pointing to a separate page-audit/load-performance follow-up. No room server deploy.
