# MEMORY - 2026-06-01

- V5 ship interaction now treats pure island-port arrivals as playable. If a ship destination has no rail/bus/airport access within 2km, `bestDestinationAccessForPort()` can use the port itself as a zero-distance `PORT_WAIT` destination instead of disabling the sailing.
- This fixes real rows such as `高松港 -> 家浦`: 豊島フェリー is selectable, the plan tail stays at `PORT_WAIT` at 家浦, and the next ship selection can start from 家浦.
- `previewPlayer()` must allow `PORT_WAIT -> TAKE_SHIP`; otherwise chained ferries can appear in Current Plan but the planning cursor stays at the previous island port. This was fixed for `高松港 -> 家浦 -> 高松港`, and TAK bus access now computes from the Takamatsu-side bus stop around 08:58 rather than from 家浦 08:16.
- Bus planner tile refresh must anchor on `BUS_STOP` current states and redraw the planner after async tile load; otherwise the first render can stay stuck on "No reachable bus service" even after the correct bus tile arrives.
- `scripts/tests/v5_ship_interaction_audit.js` now covers 高松港->土庄港 marker/continuation, 雌雄島海運 高松港/女木港/男木 legs, 高松港->家浦->高松港 replay, and the resulting TAK bus-access rows. Latest quiet output: `OK v5 ship interaction: markers=3, firstPort=土庄港, onward=4, meon=4, teshima=1`.
- Product decision: pause highway/night bus as a V5 gameplay surface. Current bus UI should expose only airport connectors (`bus_airport`) and port connectors, defined as bus routes/trips touching bus stops listed in `v5_port_connectors.json`. Do this as a runtime tile filter, not by deleting source/bundle data.
- V5 whole-data audit now has two new gates: `scripts/tests/v5_data_readiness_audit.py` for static counts/coverage, and `scripts/tests/v5_plan_tail_anchor_audit.js` for the historic distance/plan-end regressions. Latest results are green: data readiness failureCount 0; plan-tail anchors failureCount 0, markerCount 3, shipRows 12, flightRows 16, busRows 1, TAK bus access time 08:58 from a Takamatsu-side BUS_STOP.

Next: turn the current V5 audit set into a fixed release gate and add only targeted scenarios if new product paths are promoted.
