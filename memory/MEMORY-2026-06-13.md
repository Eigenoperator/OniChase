# Daily Memory - 2026-06-13

- 2026-06-13 - Upgraded V5 local AI Hunter to the same multimodal beam-search substrate as Runner. The shared action search can now generate rail, walking, flight, ship, and loaded scoped-bus actions for either seat. Runner keeps escape-oriented scoring and a 240-minute horizon; Hunter now uses capture-oriented scoring, same-place/same-vehicle bonuses, a 600-minute pursuit horizon, wider rail candidate intake, and a wider beam so long-distance Shinkansen intercepts are visible. Added mid-game local AI replanning: during later planning windows, AI truncates future steps and replans only when its current preview is at a station, bus stop, airport wait, or port wait; if replanning fails, it restores the previous future plan. Updated `scripts/dev/run_v5_ai_self_play.js` with `hunter_multimodal_static` and `hunter_multimodal_replan`, plus hourly replan simulation. Latest `--load-bus` report in `reports/v5_ai_self_play_report.json`: 24 games, Runner 12 wins / 50%, Hunter 12 wins / 50%; rail Hunter one/two/three are 1/4, 1/4, 2/4, while both multimodal Hunter strategies are 4/4. No sanitize errors and no pre-06:30 captures counted. Validation stayed light: inline `docs/v5.html` script `node --check`, self-play script `node --check`, and Playwright self-play only; no heavy V5 release gate.
