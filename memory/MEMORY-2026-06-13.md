# Daily Memory - 2026-06-13

- 2026-06-13 - Upgraded V5 local AI Hunter to the same multimodal beam-search substrate as Runner. The shared action search can now generate rail, walking, flight, ship, and loaded scoped-bus actions for either seat. Runner keeps escape-oriented scoring and a 240-minute horizon; Hunter now uses capture-oriented scoring, same-place/same-vehicle bonuses, a 600-minute pursuit horizon, wider rail candidate intake, and a wider beam so long-distance Shinkansen intercepts are visible. Added mid-game local AI replanning: during later planning windows, AI truncates future steps and replans only when its current preview is at a station, bus stop, airport wait, or port wait; if replanning fails, it restores the previous future plan. Updated `scripts/dev/run_v5_ai_self_play.js` with `hunter_multimodal_static` and `hunter_multimodal_replan`, plus hourly replan simulation. Latest `--load-bus` report in `reports/v5_ai_self_play_report.json`: 24 games, Runner 12 wins / 50%, Hunter 12 wins / 50%; rail Hunter one/two/three are 1/4, 1/4, 2/4, while both multimodal Hunter strategies are 4/4. No sanitize errors and no pre-06:30 captures counted. Validation stayed light: inline `docs/v5.html` script `node --check`, self-play script `node --check`, and Playwright self-play only; no heavy V5 release gate.
- 2026-06-13 - Expanded the V5 AI self-play harness into a seeded tournament runner with target-direction randomization, transport mode profiles, per-game decision traces, optional canonical replay export, and progress logging. Added fair-information Hunter variants: `hunter_belief_static` plans against a phantom Runner generated from public prior information, while `hunter_belief_replan` holds Tokyo until the 07:00 observation, then replans hourly from observed Runner state without reading Runner future steps. The belief model now predicts downstream rail/bus alighting points from observed in-transit states before generating a phantom future. Kept omniscient Hunter strategies as a strength ceiling. Current 48-game replay audit in `reports/v5_ai_self_play_report.json` plus `reports/v5_ai_self_play_replays/`: Runner 24 / Hunter 24, no planning failures, no sanitize issues; belief Hunter 2/12, omniscient Hunter 22/30. Current 96-game aggregate in `reports/v5_ai_self_play_tournament_96.json`: Runner 48 / Hunter 48, no planning failures, no sanitize issues; belief Hunter 5/25, omniscient Hunter 43/63. Runner still uses rail in every game and only occasionally ship/walking, so the next AI quality target is stronger flight/bus-aware Runner exploration and better non-omniscient Hunter belief coverage.
