# Daily Memory - 2026-04-29

- 2026-04-29 11:40:40 PDT - On the first maintenance prompt of the new natural day, checked `AXIOMS.md` diary rules and found `diary/DIARY-2026-04-28.md` missing. Backfilled it in concise Chinese from `memory/MEMORY-2026-04-28.md` and the April 28 commit chain, covering the v4 route-choice/through-running audit push, transfer-equivalence work, Kintetsu station-context recollection, Hachioji Yokohama Line pollution fix, and the continued no-room-server-deploy rule.
- 2026-04-29 12:04:15 PDT - Added named-train route-choice categories in v4. The compact timetable now preserves `displayName` and `headsign`, so trains like `むさしの号` can be separated from their physical route. `docs/v4.html` now creates synthetic route choices for named limited express trains, groups names such as `あずさ`, `かいじ`, `富士回遊`, and `むさしの号`, and keeps Meitetsu train labels on the user's axiom of `名鉄（○○線）`. Extended `scripts/tests/v4_route_choice_audit.js` to fail if named limited express trains are not separated and added explicit checks for `むさしの号` at `大宮` and `八王子`. Rebuilt gameplay bundles and verified the full browser audit across 9052 stations with `anomalyCount: 0`. No room server deploy.
- 2026-04-29 13:43:37 PDT - Investigated missing-looking `ひたち`/`ときわ` at 東京. The raw 常磐線 JR East official collection had the complete set (`ひたち` 30 unique trains through 東京, `ときわ` 38 through 東京), but `build_v4_current_train_collection.py` dropped many southbound trains because 東京 was context-matched under the 常磐線 collector with a large distance. Added a reviewed direct-service exception for JR East `ひたち`/`ときわ` context matches at 品川/東京/上野 and deduped these services by operator + train number + display name so duplicate 東北線/常磐線 collection copies collapse to one train. Rebuilt current/gameplay bundles. Browser audit now shows 東京 `ひたち` 30 / `ときわ` 38 and 上野 `ひたち` 30 / `ときわ` 39, with `anomalyCount: 0`. No room server deploy.
- 2026-04-29 13:57:47 PDT - Checked for other `ひたち`/`ときわ`-style direct-service drops. Added reusable `scripts/ingest/audit_v4_dropped_direct_services.py`, which groups trains rejected by current-merge validation because of excessive context match distance and compares dropped named-service keys against the current collection. Found the same JR East boundary-station pattern for `成田エクスプレス`, `あずさ`, `かいじ`, `わかしお`, `さざなみ`, and `しらゆき`; broadened the reviewed JR East direct-service exception and dedupe rule. Rebuilt current/gameplay bundles. Browser route-choice audit now shows 東京 `成田エクスプレス` 39, `かいじ` 8, `あずさ` 5, plus 八王子 `かいじ` 24 / `あずさ` 48, and still `anomalyCount: 0`. No room server deploy.
- 2026-04-29 17:44:02 PDT - Promoted Scorp's coupled split/join train axiom into `AXIOMS.md`: coupled-to-uncoupled direction should first show an umbrella `A・B` train row, then require a portion choice before alighting stations; uncoupled-to-coupled direction stays as separate trains; shared coupled segments count as `same_train`. Added `data/v4_coupled_service_registry.json` and `docs/data/v4_coupled_service_registry.json` with 18 regular passenger coupled-service families, updated `audit_v4_coupled_services.py` to read the registry, and regenerated `data/v4_coupled_service_audit.json` with `known=18`, `known_pair=7`, `known_missing=7`, `generic=17`, `high=0`. No room server deploy.
- 2026-04-29 18:22:00 PDT - Wired the coupled split/join registry into `docs/v4.html`. The website now loads `docs/data/v4_coupled_service_registry.json`, collapses detected paired same-station same-direction departures on a shared segment into an umbrella row like `はやぶさ・こまち`, requires a second portion choice before showing downstream stops, and treats registered coupled pairs as `same_train` during detected shared time windows in live capture and replay simulation. Verified JS syntax with `node --check` and reran the coupled audit (`known=18`, `known_pair=7`, `known_missing=7`, `generic=17`, `high=0`). No room server deploy.
