# Daily Memory - 2026-05-06

- 2026-05-06 09:42:03 PDT - Started the new-day sync after the v4 Sunrise/mini-Shinkansen/limited-express checkpoint. The previous day's diary and memory already record the detailed fixes; today's sync note records that the GitHub update should include the changed code/data/docs while leaving large local cache directories untracked.
- 2026-05-06 10:21:27 PDT - Expanded Sunrise validation from a Tokyo-only regression into a multi-station, both-direction focused audit. `v4_route_choice_audit.js` now checks Tokyo, Yokohama, Kagawa Takamatsu, Izumoshi, and Okayama, including same-name station disambiguation for `高松`, Seto/Izumo portion terminals, Tokyo-bound branch departures, and no seasonal 91/92 rows. `docs/v4.html` now treats the current station as valid split/join evidence for coupled rows and gives Sunrise synthetic portions dedupe priority over overlapping shared-segment source fragments. Focused route-choice audit passed with `0` anomalies.
- 2026-05-06 10:56:12 PDT - Created `V4_FINISH_CHECKLIST.md` as the capped 10-gate finish plan for v4. The gates are route-choice release audit, selected-train highlight, coupled/through-running, long-distance playability, same-name station disambiguation, ordinary-train coverage, planner interaction, performance, release notes/known limitations, and GitHub Pages final acceptance. Updated `STATUS.md` so future v4 work starts from that checklist.
- 2026-05-06 14:00:58 PDT - Worked through finish gates `7-1-2`. Planner interaction now writes `data/v4_planner_interaction_release.json` and passes with `ok: true`, `failureCount: 0`. Route-choice now writes `data/v4_route_choice_audit_release.json` and passes full staged audit with `anomalyCount: 0`, `9052` station groups, `111201` trips, and `1589911` route-choice/label rows. Selected-train highlight now has `scripts/tests/v4_selected_train_highlight_release_gate.js`, writes `data/v4_selected_train_highlight_release.json`, and passes `8` shards with `failureCount: 0`, `111201` checked trips, and `829302` future-stop coverage cases. Fixes included adjacent bad trace fallback for `踊り子`-style segments, avoiding long-range fallback overreach that broke 鹿島臨海鉄道, and pinning `サンライズ瀬戸` `高松` to the Kagawa JR Shikoku station instead of same-name stations.
- 2026-05-06 14:47:17 PDT - Worked through the remaining local v4 finish gates after `7-1-2`. Gate 3 coupled/through-running is green: `audit_v4_coupled_services.py` reports `knownSeedsMissingServicePortions: 0`, `genericHighConfidenceCandidateCount: 0`, and focused route-choice reports `anomalyCount: 0`. Gate 4 long-distance playability is green with `1000/1000` cases, `3612` waypoint audits, and `0` anomalies. Gate 5 now has `scripts/tests/v4_same_name_station_audit.js`, covering `15` cases across `高松/小倉/大宮/府中/郡山/福井/寺田`, all green. Gate 6 sparse/hub ordinary coverage is green: sparse checked `9052` stations / `11848` route choices with `0` suspicious choices, and hub checked `21/21` hubs with `0` anomalies. During this pass, the gameplay builder learned regional named limited-express families including `みどり`, `ハウステンボス`, `ソニック`, `きりしま`, `うずしお`, and `宗谷`, while broad JR Hokkaido bracketed service-group labels now fall back to physical lines instead of leaking into player route choices. Because the bundle was rebuilt, Gates 1/2/7 were rerun on the new data: route-choice `0` anomalies across `1590564` rows, selected-train `0` failures across `111215` trips / `829737` future-stop coverage cases, and planner interaction `ok: true`.
- 2026-05-06 15:00:51 PDT - Finished Gate 8 local performance/flicker acceptance. Added `scripts/tests/v4_performance_gate.js`, which performs repeated local loads and a live planner 2/3 refresh probe. The first run exposed a real perf issue: when the player had already chosen a line, every live minute still recomputed all route choices for the station before rendering trains. `docs/v4.html` now skips full-station route-choice recomputation on 2/3 and refreshes only the selected route's train rows. Latest Gate 8 pass writes `data/v4_performance_release.json` with `ok: true`, `failureCount: 0`, p95 map ready `1313ms`, p95 timetable ready `10218ms`, p95 timetable load `8905ms`, `0` empty planner mutations, and max live planner render `117.9ms`. Planner interaction was rerun afterward and remains `ok: true`.
- 2026-05-06 15:42:00 PDT - Finished the three v4 pre-release hardening items Scorp requested before release notes. Added `V4_DATA_REBUILD.md` for minimum source-data reproducibility, `V4_SERVICE_MODEL.md` for through/coupled/Shinkansen-exception/Sunrise/NEX/Kanku-Kishuji/mini-Shinkansen rules, and `V4_TRACE_QUALITY.md` for trace release thresholds. Updated `V4_DATA_FOUNDATION.md`, `V4_FINISH_CHECKLIST.md`, and `STATUS.md` with current manifest counts and hardening evidence. Re-ran limited-express trace suspect audit: `111983` checked trips, `122` reviewed suspects, `0` unreviewed; `ひたち`/`ときわ` are release-classified as real reviewed express skips over the Joban route. Re-ran current train health: `126190` trains, `0` duplicate service ids, `0` short trains, `0` missing station stops, `0` bad time order. Fixed `audit_v4_data_quality.py` so compact route/coupled names are decoded, reviewed Chiba Urban Monorail numeric line labels stop misfiring, and browser-derived `むさしの号` coverage is accepted; data-quality audit now passes with `0` errors and `0` warnings.
- 2026-05-06 15:58:00 PDT - Optimized v4 loading after Scorp called out slow load. Added compact timetable stop encoding v2 so default route ids and same arrival/departure times are elided; compact timetable gzip dropped from about `10.6M` to `8.6M` and uncompressed JSON from `63.5M` to `42.4M`. Browser init now fast-paths ordinary non-Sunrise/non-circular trips, lazily computes coupled station-name sets, caches unchanged route/trip highlight and empty plan-board renders, and lazily caches station+route departure candidates for live 2/3 planner refresh. Latest performance gate is green: p95 map ready `1293ms`, p95 timetable ready `5810ms`, p95 timetable load `4548ms`, max live planner render `85.4ms`, `0` empty mutations. Data-quality, planner interaction, and sparse route-choice audits were rerun and stayed green.
