# Daily Memory - 2026-05-04

- 2026-05-04 05:00:42 UTC - Revised v4 selected-train map highlighting so train paths follow the trip's own future `lineTrace` ranges instead of re-guessing each station pair from all candidate station routes. Selecting a train now highlights only the upcoming portion from the current boarding/current sequence forward; through-service trips include their downstream route ranges. Station highlighting still follows the train's future reachable stops, but its physical station feature matching now uses the same future trace routeIds. Browser probe selected a two-route through trip and confirmed `selected-trip-path` contained exactly those two future trace routeIds with no JavaScript errors.
- 2026-05-04 05:09:46 UTC - Confirmed GitHub Pages had deployed the selected-train highlight change, then debugged Scorp's `のぞみ1` report. Root cause was that `futureLineTraceRanges` filtered the train's own through/combined route id (`東海道・山陽新幹線`), leaving only smaller `東海道新幹線` and `山陽新幹線` trace fragments. Fixed selected-train highlighting to prefer the trip's own `routeId` when it can geometrically cover the current-to-terminal segment, while still falling back to future `lineTrace` ranges when the primary route cannot cover the whole run. Browser probe now shows `のぞみ1` as one `東海道・山陽新幹線` feature from 東京 to 博多 with all 12 future stops.
- 2026-05-04 05:14:30 UTC - Added `scripts/tests/v4_selected_train_highlight_audit.js` to make the selected-train highlight regression globally checkable. The audit loads v4 in a stubbed browser, scans all timetable trips, and verifies that any trip whose primary `trip.routeId` can cover the selected future run collapses to one primary-route highlight instead of being split by smaller `lineTrace` fragments. Nationwide run checked `115098` trips, `37322` multi-trace trips, and `783103` primary-cover start cases with `failureCount: 0`.
