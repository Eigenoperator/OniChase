# Daily Memory - 2026-05-04

- 2026-05-03 22:00:42 PDT - Revised v4 selected-train map highlighting so train paths follow the trip's own future `lineTrace` ranges instead of re-guessing each station pair from all candidate station routes. Selecting a train now highlights only the upcoming portion from the current boarding/current sequence forward; through-service trips include their downstream route ranges. Station highlighting still follows the train's future reachable stops, but its physical station feature matching now uses the same future trace routeIds. Browser probe selected a two-route through trip and confirmed `selected-trip-path` contained exactly those two future trace routeIds with no JavaScript errors.
- 2026-05-03 22:09:46 PDT - Confirmed GitHub Pages had deployed the selected-train highlight change, then debugged Scorp's `のぞみ1` report. Root cause was that `futureLineTraceRanges` filtered the train's own through/combined route id (`東海道・山陽新幹線`), leaving only smaller `東海道新幹線` and `山陽新幹線` trace fragments. Fixed selected-train highlighting to prefer the trip's own `routeId` when it can geometrically cover the current-to-terminal segment, while still falling back to future `lineTrace` ranges when the primary route cannot cover the whole run. Browser probe now shows `のぞみ1` as one `東海道・山陽新幹線` feature from 東京 to 博多 with all 12 future stops.
- 2026-05-03 22:14:30 PDT - Added `scripts/tests/v4_selected_train_highlight_audit.js` to make the selected-train highlight regression globally checkable. The audit loads v4 in a stubbed browser, scans all timetable trips, and verifies that any trip whose primary `trip.routeId` can cover the selected future run collapses to one primary-route highlight instead of being split by smaller `lineTrace` fragments. Nationwide run checked `115098` trips, `37322` multi-trace trips, and `783103` primary-cover start cases with `failureCount: 0`.
- 2026-05-03 22:51:32 PDT - Re-debugged Scorp's "no visible change" report and found the actual remaining issue: `のぞみ1` selected the correct primary route id, but `routeSliceCoordinates` chose a single nearby Okayama-area geometry fragment instead of the route graph's full 東京-博多 path. Updated route slicing to prefer the route graph when graph endpoints are much closer than the best single polyline, and tightened primary-route train highlighting so it only collapses to the primary route when the slice endpoints are close to the selected start and terminal. `のぞみ1` now renders one `東海道・山陽新幹線` feature with `10572` coordinates from 東京 (`139.76682,35.67856`) to 博多 (`130.42129,33.5933`). Updated the nationwide audit to check geometry endpoints; latest run scanned `115098` trips, `37322` multi-trace trips, and `357226` verified primary-cover geometry cases with `failureCount: 0`.
- 2026-05-03 23:12:39 PDT - Fixed live/on-train future planning. `planCursorPreview` now uses the full planned endpoint when the player already has unresolved future steps beyond the current live-time carrier, so a player currently riding a train can plan onward from their future alighting station instead of being stuck on the `Available stops` page. Browser probe: with current live state still `TRAIN`, a planned alight at 新伊丹 now shows `1/3 Lines at 新伊丹` with route rows; a train without a planned alight still shows `3/3 Available stops`, preserving the required alight choice.
- 2026-05-03 23:27:16 PDT - Fixed live timetable staleness in the right-side planner. Departure queries now use `max(plan cursor minute, current game minute)` so at 06:30 the player no longer sees trains that departed at 06:05 from a node cursor that still had the scenario start time. Added `state.trainOutlookHtml` diffing so `renderTrainOutlook` only rewrites the planner DOM when the generated options actually change, reducing visible flicker during per-second planning countdown renders. Browser probe at 東京 with game time 06:30 showed earliest route-row departure `06:30`; after advancing to 06:31 earliest became `06:31`; a same-content `renderGame()` call kept the same `.planner-stack` DOM node.
- 2026-05-04 00:27:20 PDT - Finished the smoother right-side planner refresh pass. The planner now wraps each page body in a stable container, assigns durable `data-row-key` values to line, train, coupled-train, portion, destination, nav, and empty rows, and patches matching rows in place instead of replacing the whole panel. Same-page minute updates keep existing DOM nodes, update row text/attributes, fade new/removed rows, and hold body height briefly to avoid list collapse; cross-page 1/3 -> 2/3 -> 3/3 transitions still replace page content cleanly so old page rows do not mix with the new page. Click handlers are rebound with `onclick` assignment to avoid duplicate actions on retained nodes. Browser probes confirmed 東京 1/3 keeps the same route row while earliest departure advances `06:30` -> `06:31`, and 2/3 keeps the same train row across a minute update with no JavaScript errors.
- 2026-05-04 10:47:47 PDT - Added `scripts/tests/v4_planner_interaction_audit.js` as the formal Playwright regression for the right-side planner gameplay flow. The audit loads v4 with a MapLibre stub and real timetable data, starts the runner at 東京 at 06:30, verifies 1/3 line selection, clicks 山手線, verifies 2/3 train selection with no pre-current-time departures, advances live time to 06:31 and checks rows patch in place, selects a train, verifies 3/3 downstream destination selection, clicks a destination once and confirms exactly `BOARD_TRAIN + RIDE_TO_STATION` are added, then moves live time onto the train and confirms future planning resumes from the planned alighting station at 1/3. Local run passed with `failureCount=0`, route `山手線`, trip `山手線`, board `06:31`, alight `06:42`, destination `日暮里`, and no console/page errors.
- 2026-05-04 12:23:23 PDT - Fixed the v4 route-choice audit red lights. Player-facing route choices now special-case JR East 成田支線 segments so 成田/酒々井/佐倉 show `成田線` instead of source `総武線`, 京成スカイアクセス airport-side segments show `京成成田空港線` instead of leaking `京成本線`, and スーパーはくと/智頭急行 through trips on 鳥取-郡家-智頭 show the local `因美線` boarding line. Added `むさしの号` as a named-train route choice for 大宮-八王子 武蔵野線 through services. Updated the audit's allowed physical-trace exceptions for 東京 southbound `東海道線`, 成田支線, 京成成田空港線, 因美線, and source-mislabeled shinkansen segments; also refreshed stale minimum count floors for current `踊り子` and `富士回遊` choices while preserving required train-number checks. Verification: `v4_route_choice_audit.js` passed with `anomalyCount=0` over `9052` stations and `115104` trips; `v4_planner_interaction_audit.js` passed with `failureCount=0`; `v4_selected_train_highlight_audit.js` passed with `checkedTrips=115098`, `checkedPrimaryGeometryCases=357226`, `failureCount=0`.
- 2026-05-04 12:27:19 PDT - Promoted Scorp's new-data overlap lesson into `AXIOMS.md` as "New Data Must Be Audited Against Old Data." The rule says every new timetable/route/station/operator/cache/derived source must be checked against existing sources for overlap and priority conflicts before it is treated as authoritative, especially for route-choice and train-label data where new source route identities can override old correct physical boarding-line choices. It also requires deterministic merge/priority rules plus nationwide same-class audits when a conflict is found.
- 2026-05-04 12:41:13 PDT - Fixed the v4 selected-train highlight bug for 大阪→関西空港 services. Root cause was that train-path rendering preferred a single route slice from trace start to trace end, so Osaka Loop airport-bound trains could choose the east/right half from 大阪 to 天王寺 and ignored the train's stop sequence; `はるか` was worse because its coarse `東海道線` trace has no intermediate stop between 大阪 and 天王寺. The web highlighter now validates whether a full trace slice covers intermediate stops, falls back to scored adjacent-stop physical segments when the trace route is too coarse, and supports reviewed via-station hints for no-stop physical segments. Added a regression to `v4_selected_train_highlight_audit.js` requiring the first 40 大阪→関西空港 Osaka Loop cases, including `はるか`, to highlight the west side via 西九条/弁天町/大正 and not the 京橋/鶴橋 side. Verification passed: selected-train highlight audit `failureCount=0`, planner interaction audit `failureCount=0`, and route-choice audit `anomalyCount=0`.
- 2026-05-04 12:50:29 PDT - Tightened the selected-train highlight principle after Scorp correctly rejected the remaining fallback logic: selected-train path rendering no longer uses scored candidate physical routes to guess a train's path. It now only uses the train's own future trace plus explicit `REVIEWED_TRIP_PATH_HINTS`. The reviewed airport hints split 大阪/天王寺/関西空港 no-stop segments into real path legs: 大阪環状線 via 西九条, 阪和線 via 日根野, and 関西空港線. Station halo route ids now use the same reviewed path legs, so `はるか3` from 大阪 highlights 大阪 as `大阪環状線`, 天王寺 as `大阪環状線`/`阪和線`/`関西空港線`, and 関西空港 as `阪和線`/`関西空港線`, with no `東海道線` leakage. Verification: selected-train highlight audit `failureCount=0` and planner interaction audit `failureCount=0`.
- 2026-05-04 13:01:07 PDT - Fixed another selected-train highlight edge for browser-stitched through trains. `futureLineTraceRanges` was still allowed to collapse a stitched direct train to its primary `trip.routeId` when that route could geometrically cover the endpoints, which erased the recorded current/downstream trace sections. For example, JR Hokkaido stitched 函館→札幌 trains collapsed to `函館線` instead of keeping `函館線 -> 室蘭線 -> 千歳線`; from 東室蘭, the corrected future ranges now start with the current `室蘭線` section and then continue onto `千歳線`. The selected-train highlight audit now treats stitched through trains as trace-authoritative and fails if any recorded current/downstream trace route disappears. Verification: selected-train highlight audit `failureCount=0` and planner interaction audit `failureCount=0`.
- 2026-05-04 13:21:11 PDT - Added Scorp's continuity rule to `AXIOMS.md`: every player-facing map highlight must be one continuous physical or reviewed path chain, and ambiguous long-distance services must be source-checked before adding reviewed assumptions. Enforced it in v4 selected-train path rendering by rejecting station-pair route slices whose endpoints do not anchor to the two stops, anchoring accepted slices at station coordinates, and stitching adjacent same-route ranges through their boundary station. The nationwide audit now checks selected-train path continuity, initially catching 阪急神戸線 fragment gaps, 広島電鉄 fallback micro-segments, and an erroneous Sky Access `京成高砂 -> 東松戸` `京成金町線` trace; the latter is reviewed-normalized to `京成成田空港線` only for train-path highlighting so ordinary 北総線 route choices remain unchanged. Verification: `v4_selected_train_highlight_audit.js` passed with `checkedTrips=115098`, `checkedContinuousPathCases=353904`, `failureCount=0`; `v4_route_choice_audit.js` passed with `anomalyCount=0`; `v4_planner_interaction_audit.js` passed with `failureCount=0`.
- 2026-05-04 13:32:37 PDT - Fixed the 湖西線 data split that Scorp suspected around 近江今津. Root cause was a JR West official timetable table shape: some 湖西線 trains are presented as adjacent columns on the same official train page, so the collector treated the same physical train as separate source rows, making many runs appear to start/end at 近江今津. Added source-level reviewed split-column stitching for JR West 湖西線 when adjacent rows share the source page, boundary station, and a `0-15` minute boundary gap. Rebuilt JR West official data, current collection, and gameplay bundles; the JR West audit now reports `reviewedSplitColumnStitchCount=19`, `reviewedSplitColumnSourceRowCount=39`, `0` unmatched stops, and `0` bad time-order trains. Gameplay now contains complete chains such as `3891M+3811M+3411M` 敦賀->姫路, `3143M+3443M` 敦賀->姫路, `3438M+3138M` 姫路->敦賀, `4841M+128M` 近江今津->米原, and `143M+4848M` 長浜->近江今津. Added `scripts/tests/v4_jrwest_kosei_split_stitch_audit.js`; verification passed for the new audit, route-choice audit (`1624259` rows, `0` anomalies), planner interaction audit (`failureCount=0`), selected-train highlight audit (`checkedTrips=115117`, `failureCount=0`), and `py_compile` on the collector.
- 2026-05-04 14:05:42 PDT - Fixed the remaining selected-train highlight bug where through trains on corridor alias routes showed only later fragments and dropped the current physical section. Reproduced it with `あさま602`/北陸新幹線+上越新幹線: from 高崎 the trace ranges included the current 上越新幹線 section, but the downstream `SHINKANSEN_HOKURIKU` alias had no physical geometry, so the path renderer omitted 熊谷->大宮->上野->東京. Added a cached `physicalRouteIdForTraceStopPair` resolver used by selected path and station halo rendering: normal trace routes still trust the recorded trace, while through-service aliases resolve each adjacent stop pair to a real physical shinkansen route. Shinkansen candidate scoring now rejects non-shinkansen physical lines, preventing 大宮->上野 from falling onto 東北本線. Probe now shows 高崎 start highlighting `上越新幹線 -> 東北新幹線` and future station halos through 東京. Expanded `v4_selected_train_highlight_audit.js` with a 北陸/上越 future-stop coverage rule; verification passed with `checkedFutureStopCoverageCases=171`, `checkedContinuousPathCases=355591`, `failureCount=0`, plus planner interaction `failureCount=0` and route-choice `anomalyCount=0`.
- 2026-05-04 14:17:55 PDT - Audited and fixed `サンダーバード` trace identity. Official JR Odekake train pages include pass-through rows (`レ`) showing Thunderbird runs from Osaka/Shin-Osaka/Kyoto to Tsuruga via the JR Kyoto/Tokaido section and then the Kosei Line, but the parser keeps only timed stops; long stop pairs such as 京都->敦賀 and 敦賀->近江今津 were therefore inferred from source timetable tabs or endpoint station lines. Added reviewed physical-segment overrides for 京都/敦賀, 京都/近江今津, 京都/堅田, 敦賀/近江今津, and 敦賀/堅田 to `build_v4_gameplay_bundle.py`, and disabled source-line terminal-tail preservation for limited express trains so Thunderbird no longer gets pulled entirely onto the source tab line. Rebuilt gameplay bundles; `サンダーバード1/3/5` now trace `東海道線 -> 湖西線`, `2/4` and other Tsuruga-origin runs trace `湖西線 -> 東海道線`, and `43/49` keep Osaka-Kyoto on `東海道線` before switching to `湖西線`. Added `scripts/tests/v4_jrwest_thunderbird_trace_audit.js`, which passes for all `33` current Thunderbird trips. Verification also passed: Kosei split-stitch audit `failureCount=0`, selected-train highlight audit `failureCount=0`, route-choice audit `anomalyCount=0`, and planner interaction audit `failureCount=0`.
- 2026-05-04 14:25:59 PDT - Added a report-only nationwide limited-express/long-distance trace suspect audit. `scripts/tests/v4_limited_express_trace_suspect_audit.js` reads the actual compact gameplay timetable and map bundle, finds named limited express and shinkansen candidates, checks long adjacent timed-stop gaps, single-trace long trips, virtual shinkansen corridor pairs, and endpoint-line mismatches, then writes `data/v4_limited_express_trace_suspect_audit.json` without failing the build. Latest run scanned `115924` trips, found `3022` candidate trips, checked `30984` adjacent pairs, and reported `3386` suspects: `3198` long adjacent stop pairs, `1553` very-long pairs, `188` long single-trace mismatch candidates, `1947` virtual shinkansen corridor pairs, `3007` normal shinkansen skip candidates, and `27` reviewed Thunderbird 湖西線 physical segments. Top conventional actionable group is currently `ひたち` 上野/水戸, where southbound and northbound traces need source review for the Ueno/Tokyo-side section.
