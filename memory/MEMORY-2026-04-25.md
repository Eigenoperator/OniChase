# Daily Memory - 2026-04-25

- 2026-04-25 13:46:47 PDT - Backfilled `diary/DIARY-2026-04-24.md` after the v3 release-candidate freeze work, summarizing the planner audit reduction from large warning debt to `9` known external/homonym data edges, the Sotetsu through-alias rebuild, Tokyo Metro Odakyu limited-express route identity fix, and the creation of `V3_RELEASE_CANDIDATE_NOTES.md`. Prepared to sync the diary/control-file update to GitHub while leaving unrelated local dirty files and source caches uncommitted.
- 2026-04-25 14:18:04 PDT - Began v4 implementation with nationwide real physical rail geometry and `station_identity_v2`. Added reusable N02-2024 build/audit scripts, generated `data/v4_japan_physical_map.json.gz` and `data/v4_station_identity_audit.json`, and verified `10235` physical stations, `9048` station groups, `21932` track centerlines, `178` operators, `427` same-name split names, and `0` line coverage warnings.
- 2026-04-25 14:25:51 PDT - Added reusable v4 MapLibre-source and nationwide line-inventory exporters. Generated public GeoJSON layers under `docs/data/v4_maplibre` with `21932` track features, `9048` station group points, and `10235` physical station points, plus line inventory outputs with `596` operator-line pairs, `552` unique line names, `178` operators, `0` stationless lines, and `0` trackless lines.
- 2026-04-25 15:15:08 PDT - Built the first public `docs/v4.html` MapLibre viewer for the v4 nationwide physical map. The page loads the v4 manifest, displays nationwide physical tracks and zoom-tiered station labels, supports optional physical-station dots, and exposes searchable `596` operator-line inventory rows with selected-line real-geometry highlighting. Also updated the landing page and README to link the v4 public page.
- 2026-04-25 15:32:39 PDT - Responded to v4 map review by adding a Natural Earth Japan land outline, a `596`-feature low-zoom `track_overview.geojson`, lazy high-detail track loading, lazy physical-station loading, high-zoom station-group dots so labels do not appear without black station circles, and a reusable `audit_v4_track_continuity.py` report. The continuity audit found `12` multi-component operator-line pairs out of `596`, suggesting most visible discontinuity is either source topology or same-operator color ambiguity rather than exporter-wide data loss.
- 2026-04-25 15:48:52 PDT - Changed the v4 land layer to outline-only rendering by removing `land-fill`, raised the MapLibre land source `maxzoom` from `7` to `14`, reduced source tolerance, and regenerated `japan_land.geojson` at `0.006` simplification tolerance so the Japan coastline keeps more detail when zoomed in.
- 2026-04-25 15:55:02 PDT - Generated a dedicated v4 continuity review map for the `12` multi-component operator-line warnings. Added `scripts/ingest/render_v4_track_continuity_highlights.py`, `docs/data/v4_maplibre/track_continuity_highlights.geojson`, `docs/data/v4_track_continuity_highlights.svg`, and `docs/v4_continuity.html`; verified the HTML, SVG, and GeoJSON over a local HTTP server.
- 2026-04-25 15:58:37 PDT - Fixed v4 double-line rendering where low-zoom `track_overview` simplified geometry remained visible on top of exact physical `track_centerlines`. The overview and selected-overview layers now fade out and stop rendering by zoom `7.65`, detailed physical tracks start at zoom `7.55`, and high-detail track loading begins at zoom `7.25`; local HTTP checks confirmed `v4.html` and `track_centerlines.geojson` load correctly.
- 2026-04-25 16:08:13 PDT - Rebuilt the v4 Japan land outline from geoBoundaries Japan ADM0 instead of the previous unfiltered Natural Earth countries source. The public map now uses `japan_land_overview.geojson` for low zoom and lazy-loads the high-detail `japan_land.geojson` detail outline from zoom `5.45`, keeping Japan-only coastline geometry sharper without adding land fill.
- 2026-04-25 16:20:28 PDT - Fixed v4 station label tiering so national-scale labels no longer persist after zooming in. `label-national` now fades out by zoom `6.8`, `label-major` takes over at normal detail size, and regional/local/all labels use mutually exclusive `label_rank` ranges to prevent duplicate large-scale station names.
- 2026-04-25 16:30:06 PDT - Reworked v4 station labels toward the v3 MapLibre logic after zooming still felt unnatural. Removed the `label-national` to `label-major` handoff, replaced it with stable `label-hub`, `label-regional`, `label-local`, and `label-all` layers, kept rank ranges mutually exclusive, added `text-optional`/`text-justify:auto`, and changed `symbol-sort-key` to negative `label_rank` so high-priority station labels win collision placement first.
- 2026-04-25 16:41:53 PDT - Fully switched v4 station label rendering to the v3 MapLibre structure. `build_v4_maplibre_sources.py` now exports `station_labels.geojson` with v3-style `label_rank` and `sort_rank`; `docs/v4.html` loads it as `stationLabels` and uses the v3 `addLabelLayers` implementation with `label-hub`, `label-major`, and `label-local`.
- 2026-04-25 17:15:40 PDT - Added prefecture/location-note enrichment to v4 station identity. `build_v4_japan_physical_map.py` now loads geoBoundaries JPN ADM1 boundaries, assigns `prefectureCode`, `prefectureNameJa`, `prefectureNameEn`, `prefectureAssignmentMethod`, and `locationNote` to every physical station, aggregates those fields on station groups, and exports `location_note` through v4 MapLibre station/group/label sources. Verification assigned `10235/10235` physical stations, with `5` coastal/artificial-island stations using the `nearest_boundary` fallback.
- 2026-04-25 17:52:54 PDT - Continued v4 continuity cleanup. Fixed the continuity audit spatial hash for larger endpoint snap tolerances, changed the default snap tolerance to `500m`, added review classifications and component lengths, regenerated the audit/highlight outputs, and reduced the actionable continuity set from `12` raw warnings to `6` reviewed multi-component operator-lines with `0` unreviewed. Promoted the architecture rule that v4 timetable stop matching must reuse the v3 station alias/station-group method into `AXIOMS.md`.
- 2026-04-25 18:58:04 PDT - Continued v4 official line-color coverage. Added high-confidence line colors for Osaka Metro, Nagoya, Kyoto, Kobe, Yokohama, Fukuoka, Sapporo, Sendai, Kita-Osaka Kyuko, and Hankyu groups, plus real company fallback colors for major non-Tokyo operators. Regenerated v4 MapLibre sources and inventories; official-line color coverage increased from `108` to `152` operator-line pairs, fallback decreased to `444`, and `scripts/ingest/audit_v4_line_colors.py` now writes `data/v4_line_color_audit.json` for repeatable color-coverage checks.
