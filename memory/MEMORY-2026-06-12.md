# Daily Memory - 2026-06-12

- 2026-06-12 - Scorp asked to focus on playing with AI and making it smarter, with all assistant-facing replies in Chinese. Implemented a V5 local AI strategy pass in `docs/v5.html`: the local computer opponent now builds and scores rail candidate plans from the first 80 catchable departures instead of choosing the first usable ride. Hunter AI prefers intercept/same-station/same-train opportunities near the human plan tail; Runner AI prefers greater distance, longer escape rides, and higher-onward-option stations. Also fixed the initial planning behavior so the local AI refreshes its plan before LIVE/Replay, reflecting the latest human plan instead of only the plan visible when the countdown started. Verification stayed in V5 safe mode: `git diff --check` and extracted inline script `node --check` only; no heavy bundle rebuilds, gzip scans, tile rewrites, or browser/data audits.
- 2026-06-12 - Extended the AI work so local AI Runner has actual long-line multimodal planning. `docs/v5.html` now has a synchronous Runner beam search that temporarily validates candidate step lists through the existing `previewPlayer` rule engine, searches up to 4 legs / 240 in-game minutes, and can use rail, walking, flights, ships, and bus planner data when scoped bus tiles are already loaded. The score rewards distance from Hunter, distance from Hunter's plan tail, onward mobility, mode diversity, flights/ships/buses as escape jumps, and elapsed escape progress; it penalizes same-place/same-vehicle capture risk and low-option endpoints. Hunter AI stays on the earlier rail intercept strategy for now. Validation stayed light: `git diff --check` and extracted inline script `node --check`; no V5 browser/data/heavy bundle audit.
- 2026-06-12 - Added `scripts/dev/run_v5_ai_self_play.js` for lightweight V5 AI self-play. The harness runs real `docs/v5.html` logic through Playwright, starts both seats at Tokyo with Runner released at 06:00 and Hunter released at 06:30, ignores pre-06:30 captures only inside the harness, and tests a 4x4 strategy matrix. It also validates generated plans through the formal replay expander and trims invalid strategy tails, after discovering that preview-valid long rail chains can still fail `buildResult()` catchability. Latest `--load-bus` run wrote `reports/v5_ai_self_play_report.json`: 16 games, Runner 12 wins / 75%, Hunter 4 wins / 25%; Runner multimodal strategies were 4/4, single-leg rail Runner was 1/4, and Hunter three-leg rail was the strongest tested hunter strategy at 2/4. Bus planner loaded, but the winning Tokyo multimodal paths used rail + ship + walking. This was self-play only, not a heavy V5 release gate.
