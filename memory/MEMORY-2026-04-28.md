# Daily Memory - 2026-04-28

- 2026-04-28 10:45:13 PDT - Started a v4 checkpoint session after reading `AXIOMS.md`, `HISTORY.md`, `IDENTITY.md`, `MEMORY.md`, `SOUL.md`, `STATUS.md`, and `V4_DATA_FOUNDATION.md`. The working direction is to stabilize the already-expanded v4 nationwide data into a gameplay/playtest checkpoint rather than continue only open-ended ingestion.
- 2026-04-28 10:47:59 PDT - Rebuilt the v4 current train collection, train health audit, gameplay bundle, room-server bundle, and planner corridor audit. Current verified outputs: `127938` weekday train instances in `data/v4_current_weekday_train_instances.json.gz`; clean health audit with `0` duplicate service ids, `0` duplicate signatures, `0` short trains, `0` missing station refs, and `0` bad time-order trains; gameplay bundle with `10239` physical stations, `9052` station groups, `617` service routes/patterns, `127933` compact trips, and `5` skipped short gameplay trips; room-server bundle with `9052` station groups and `127933` SQLite-backed trips; planner corridor audit with `6` hubs and `0` warnings.
- 2026-04-28 10:49:11 PDT - Did not commit/sync yet because the worktree contains a broad pre-existing dirty v4 ingestion state with many untracked collectors, local caches, and generated artifacts. A deliberate staging pass is needed to separate stable scripts/docs/selected artifacts from ignored local caches before pushing.
- 2026-04-28 10:51:17 PDT - Started a local docs server on port `8765` and ran Playwright-backed v4 browser probes against `http://127.0.0.1:8765/v4.html`. The v4 page loaded the new gameplay bundle/timetable, built browser state with `9052` station groups and `127183` stitched gameplay trips, surfaced Tokyo route choices, passed entry-mode and replay-core probes, and confirmed a v4-specific 池袋 `東京メトロ副都心線` route-filter check with `207` departures and `0` 川越/埼京 contamination rows.
- 2026-04-28 11:04:17 PDT - After Scorp pointed out that 上野東京ライン still contained 京浜東北-like contamination and that the fix must be network-wide rather than line-specific, changed v4 route-choice logic so a departure's player-facing category no longer borrows arbitrary physical routes from the boarding station. Added a global excessive station-match distance quality gate (`1500m`) in `build_v4_current_train_collection.py`, dropping `8444` badly matched source trains plus the existing `35` unmatched-ref trains. Rebuilt v4 outputs: current collection `120425` trains, clean health audit, gameplay bundle `120420` compact trips / `618` service routes, room-server bundle `120420` SQLite trips, planner corridor audit `6` hubs / `0` warnings. Browser QA confirmed 東京・上野・品川・新橋 上野東京ライン samples have `0` 京浜/根岸/3号線/IR/富山/横須賀/総武/北総 contamination rows in the first `120` departures per station.
- 2026-04-28 11:08:53 PDT - Upgraded `data/v4_current_train_collection_audit.json` to include dropped-train counts by source and source/reason. Current dropped source distribution: `6574` from `v4_navitime_non_jr_weekday_train_instances_v0_1`, `1414` from `v4_jreast_official_weekday_train_instances_v0_1`, `273` from `v4_jrwest_official_weekday_train_instances_v0_1`, `129` from `v4_jrcentral_navitime_weekday_train_instances_v0_1`, `49` from `v4_jrkyushu_navitime_weekday_train_instances_v0_1`, and `40` from `v4_jrshikoku_navitime_weekday_train_instances_v0_1`. Rebuilt v4 current/gameplay/room bundles again and reran browser probes (`axioms`, `entry-modes`, `replay-core`) successfully against local `v4.html`.
- 2026-04-28 13:36:23 PDT - Investigated the v4 through-service handling code and found the root cause of the stale `東海道線+総武線` direct-train label: `stitchThroughRunningTrips` preserved source physical `routeId`s in `throughRouteIds`, and `formatTripLabel` used those ids before applying the Yokosuka/Sobu Rapid virtual corridor. Updated `docs/v4.html` so stitched trips that classify as `isYokosukaSobuRapidTrip` display `横須賀線・総武快速線`. A nationwide browser/data audit found `678` stitched through trips, covering Yokosuka/Sobu Rapid, Keikyu/Toei Asakusa, and Keisei/Toei Asakusa chains, with `0` remaining `東海道線+総武線` or `総武線+東海道線` labels. Browser probes `entry-modes` and `replay-core` still pass against local `v4.html`.
- 2026-04-28 13:41:44 PDT - Added reusable script `scripts/ingest/audit_v4_through_services.py` and generated `data/v4_through_service_audit.json`. The script audits full v4 timetable data, separating review-oriented lineTrace route transitions inside one trip from split-trip candidates at the same boundary station within `300s`, then marks which split candidates are covered by current browser stitching. First run: `120420` trips, `1026` split candidates, `942` browser-stitchable, and `84` uncovered review candidates. Major uncovered groups include JR Hokkaido `千歳線 -> 函館線`, `函館線 -> 室蘭線`, JR Hokkaido 石北/宗谷 continuations at 旭川, JR Kyushu 鹿児島/日豊, plus smaller pairs like `京阪電気鉄道:京津線 -> 京都市:東西線` and `野岩鉄道:会津鬼怒川線 -> 会津鉄道:会津線`. The script was refined so stop-level display-route changes are diagnostic only and not counted as proof of physical direct service.
- 2026-04-28 13:52:58 PDT - Implemented the first reviewed through-service display rules in `docs/v4.html`. Confirmed split direct-service rules now stitch JR Hokkaido `函館線/室蘭線/千歳線/宗谷線/石北線` continuations, `京阪電気鉄道:京津線 -> 京都市:東西線`, and `野岩鉄道:会津鬼怒川線 -> 会津鉄道:会津線`. Browser labels for these reviewed direct trains use the service/number when available and the train preview row adds a `直通 A -> B` route-chain subtitle. The audit now reports `1026` split candidates, `994` browser-stitchable, `52` confirmed-direct, `26` likely reused-number/data-context, and `32` remaining uncovered review candidates. Browser syntax, `entry-modes`, and `replay-core` probes passed against local `v4.html`.
- 2026-04-28 14:00:18 PDT - Clarified `scripts/ingest/audit_v4_through_services.py` output so the audit visibly explains how through-running is checked. `data/v4_through_service_audit.json` now includes a `methodology` block, candidate `decision` evidence with station/time/train-number details, and clearer classifications: `942` covered by existing browser rules, `52` confirmed direct, `26` reviewed likely reused-number/data-context non-UI cases, and only `6` still `needs_review`.
- 2026-04-28 14:08:35 PDT - Fixed the v4 青梅 station Central/Ome display issue. Root cause: player-facing route choice trusted stop-level `displayRouteId/outgoingRouteId` before checking the next physical segment, so duplicated Central/Ome source rows could put the same 青梅-branch train under both contexts. `docs/v4.html` now normalizes Chuo/Ome departures whose boarding and next station are both on the Ome branch to `青梅線`, and the dedupe key now includes physical `青梅線` plus public train number. Extended `scripts/ingest/audit_v4_through_services.py` with reusable player-facing departure checks; the audit now reports `1901` branch display conflicts and sample evidence such as 青梅 `1004H` raw `中央線` -> expected `青梅線`. Browser runtime probe confirms 青梅 route choices only show `青梅線` and no duplicate visible rows in the sampled departures.
- 2026-04-28 14:12:10 PDT - Investigated the GitHub Pages deployment failure suspicion and found the likely issue was oversized/stale static payload rather than `data/v4_through_service_audit.json` (`384K`). `docs/` had grown to about `246M` with unreferenced `v4_current_weekday_train_instances`, full timetable bundles, `v4_maplibre`, and old v3 Tokyo tile artifacts. Updated `scripts/dev/build_web_client_bundle.py` to prune stale `docs/data` artifacts and only copy current page inputs; local `docs/` is now about `44M`. Verified `docs/v1.html`/`v2.html`/`v3.html`/`v4.html` script syntax and a local v4 browser load/青梅 probe after pruning.
- 2026-04-28 15:00:16 PDT - Checked Scorp's Tokyo station `東海道線` concern in the v4 browser runtime. Result: 東京 legitimately exposes both `東海道線` and `上野東京ライン`; `東海道線` rows are Tokyo-origin/terminating southbound services such as `729M` to 熱海 and `踊り子`/`湘南`, while cross-Tokyo/Ueno services remain under `上野東京ライン`. Found and fixed a separate visible duplicate-source issue by extending `dedupeBoardableDepartures` to use public train number in the loose player-facing dedupe key. Browser probe confirms sampled 東京 `東海道線` and `山手線` rows no longer duplicate.
- 2026-04-28 15:04:57 PDT - Promoted Scorp's unified line-equivalence principle into `AXIOMS.md`: deciding whether two services are the same line, equivalent direct service, or mergeable player-facing line must be based on whether the player boards/alights on the same platform or same boarding face for the relevant movement.
- 2026-04-28 15:11:53 PDT - Applied the platform/boarding-face axiom to v4 player route choices. Added `boardingFaceRouteNamesForStop` in `docs/v4.html` so corridor choices check the current boarding stop -> next stop physical face before merging. 東京 no longer exposes separate `東海道線`; same-platform Tokyo southbound services such as `729M`/`1731M` now appear under `上野東京ライン`, while `横須賀線・総武快速線`, `中央線快速`, and 青梅 branch behavior remain separated by boarding face. Browser probe also found remaining 品川 anomalies (`成田エクスプレス`/横浜市 source rows exposing `東海道線`) for later source/face audit.
- 2026-04-28 15:13:36 PDT - Disabled automatic Render v4 room-server deployments after Scorp said not to deploy the server again because it keeps failing. `.github/workflows/deploy-render-v4.yml` now has only `workflow_dispatch`, and `AXIOMS.md` records server deployment as manual-only unless Scorp explicitly re-enables it.
- 2026-04-28 15:25:17 PDT - Fixed the next layer of v4 direct-running/player-route anomalies in `docs/v4.html` and added reusable runtime audit `scripts/tests/v4_route_choice_audit.js`. Route choice now validates stop-level route ids against the actual current->next station segment, skips terminal rows when building station route choices, treats 東京総武快速地下段 source rows as `横須賀線・総武快速線`, splits 大宮 northbound movements to `東北線`/`高崎線`, and filters misleading JR East trunk fallback labels from non-trunk trips such as 品川 `成田エクスプレス`. The new Playwright audit against local `docs/v4.html` reports `0` anomalies for 東京, 品川, 大宮, and 青梅; syntax checks passed and the reusable v4 through-service audit still runs successfully.
- 2026-04-28 15:39:41 PDT - Applied Scorp's revised v4 display definition that `上野東京ライン` should be weakened to only the `東京`-`上野` core segment. `AXIOMS.md` now records this rule. `docs/v4.html` now carries the queried station group through departure rows, only returns `上野東京ライン` for actual `東京`/`上野` core-segment boardings, and rewrites source `上野東京ライン` rows outside the core to the current physical segment: south side `東海道線`, north side `東北線`/`高崎線`/`常磐線`. The reusable Playwright audit `scripts/tests/v4_route_choice_audit.js` now scans all stations for forbidden outside-core `上野東京ライン`; it passed with `0` anomalies.
- 2026-04-28 15:54:42 PDT - Expanded `scripts/tests/v4_route_choice_audit.js` into a reusable all-station route-choice audit. It now scans every future departure from all `9052` station groups and fails if a player-facing route choice either does not match the current boarding stop -> next stop segment or appears outside its allowed virtual corridor station set. Fixed the issues it found in `docs/v4.html`: `中央・総武線各駅停車` no longer leaks to far Central/Sobu endpoints, `横須賀線・総武快速線` no longer leaks past the rapid corridor, `京浜東北線・根岸線` no longer appears on 山手線-only loop segments, `中央線快速` no longer appears when the queried station is a transfer-equivalent non-JR station such as 京王八王子, and りんかい線/埼京直通 rows no longer show as 山手線 on the Rinkai segment. The latest Playwright audit against local `docs/v4.html` reports `checkedRows=1675021`, `checkedChoices=1679010`, `segmentMismatchCount=0`, `virtualOutsideAllowedStationCount=0`, and `anomalyCount=0`.
- 2026-04-28 15:57:50 PDT - Updated the Ueno-Tokyo display axiom and v4 route choice logic for Scorp's Tokyo-station directionality rule: at `東京`, southbound/current-next movements toward the Tokaido side display `東海道線`, while northbound movements toward `上野`/the northern trunk display `上野東京ライン`; raw northern trunk labels such as `東北線` must not appear at Tokyo. Fixed source rows whose trip route and boarding face were `東北線` but whose next station from Tokyo was `新橋`/`品川` by applying the Tokyo boundary rule before trusting source route labels. Extended `scripts/tests/v4_route_choice_audit.js` with a Tokyo forbidden-north-trunk assertion and reran the Playwright audit: 東京 choices are now `東京メトロ丸ノ内線`, `中央線快速`, `京浜東北線・根岸線`, `東海道線`, `山手線`, `上野東京ライン`, and `横須賀線・総武快速線`; global counts remain `checkedRows=1675021`, `checkedChoices=1679010`, `segmentMismatchCount=0`, `virtualOutsideAllowedStationCount=0`, `anomalyCount=0`.
- 2026-04-28 15:59:55 PDT - Refined the Ueno-Tokyo axiom to the full through-running chain Scorp specified: `東海道線` south of `東京`, `上野東京ライン` only between `東京` and `上野`, then `東北線`/`宇都宮線`/`高崎線`/`常磐線` north of `上野` by branch/platform. Updated `docs/v4.html` so Ueno southbound/Tokyo-bound movements return `上野東京ライン`, and Ueno northbound movements are split into `東北線`, `高崎線`, or `常磐線`; `東海道線` no longer appears as a Ueno route choice. Extended `scripts/tests/v4_route_choice_audit.js` with Ueno forbidden-south-trunk assertions. Latest Playwright audit against local `docs/v4.html`: 東京 choices include `東海道線` and `上野東京ライン` but no northern raw trunk; 上野 choices include `上野東京ライン`, `東北線`, `高崎線`, `常磐線` but no `東海道線`; global counts remain `checkedRows=1675021`, `checkedChoices=1679010`, `segmentMismatchCount=0`, `virtualOutsideAllowedStationCount=0`, `anomalyCount=0`.
- 2026-04-28 16:06:42 PDT - Fixed Scorp's follow-up that 上野 `東北線` still contained many 高崎線 trains. Root cause: the 上野 branch classifier looked mostly at the next station (`尾久`/`赤羽` shared by multiple northern branches), so 高崎-bound trains such as `1820E` to `高崎` and `1830E` to `籠原` were labeled `東北線`. `docs/v4.html` now checks downstream stops after 上野 and prioritizes 高崎線 if the trip enters branch stations such as `宮原`, `上尾`, `熊谷`, `籠原`, or `高崎`; 東北線/宇都宮線 is reserved for downstream 宇都宮-side stops. The Playwright audit now includes a reusable 上野高崎線-hidden-by-東北線 assertion. Latest audit: 上野 `高崎線` choices increased from `42` to `129`, 上野 `東北線` dropped from `219` to `132`, and global counts remain `checkedRows=1675021`, `checkedChoices=1679010`, `segmentMismatchCount=0`, `virtualOutsideAllowedStationCount=0`, `anomalyCount=0`.
- 2026-04-28 16:24:51 PDT - Added Scorp's selected-line train-label axioms to `AXIOMS.md`: selected train rows must not expose raw `x号線`, limited express/Shinkansen rows must include a public train number when one exists, and ordinary through-running rows should show the current operating company's boarding-segment line. Updated `docs/v4.html` so route-like service names such as `2号線日比谷線` are treated as ordinary line labels, ordinary through-service rows show the current boarding-face line, limited express/Shinkansen rows keep train names/numbers ahead of segment labels, and `広島新交通1号線` displays as `広島高速交通アストラムライン`. Extended `scripts/tests/v4_route_choice_audit.js` with a reusable all-station selected-train label scan; latest Playwright audit reports `checkedRows=1675021`, `checkedChoices=1679010`, `checkedLabels=1679010`, `rawNumberedLineLabelCount=0`, `limitedOrShinkansenMissingNumberCount=0`, and `anomalyCount=0`.
- 2026-04-28 16:40:01 PDT - Revised the selected-train ordinary through-running axiom from "current operating company's boarding segment" to Scorp's direction-side rule: for an `A-B-C` through run, an `A-B` segment train heading toward `C` displays the `C` line, while the reverse displays the `A` line even if the vehicle belongs to company `C`; for `A-B(-D)-C`, the branch label appears only when the train is actually heading toward branch `D`. `docs/v4.html` now computes `directionTerminalRouteIdForTrainLabel` from future `lineTrace` plus the physical terminal-adjacent segment, with Ueno north branch overrides to avoid noisy trace labels. The reusable Playwright audit now counts `throughDirectionLabelMismatchCount`; latest scan reports `checkedLabels=1679010`, `rawNumberedLineLabelCount=0`, `limitedOrShinkansenMissingNumberCount=0`, `throughDirectionLabelMismatchCount=0`, and `anomalyCount=0`. Spot checks: 北千住 toward 南栗橋 labels `東武日光線`, 北千住 toward 中目黒 labels `東京メトロ日比谷線`, 上野 southbound labels `東海道線`, and 上野 northbound labels `高崎線`/`東北線`.
- 2026-04-28 16:42:11 PDT - Strengthened the room-server deployment axiom after Scorp said "dont deploy the room server any more": `AXIOMS.md` now forbids Render room-server deployment unless Scorp explicitly reverses the axiom in a future written instruction, and `STATUS.md` reflects the same stronger rule. No deploy was run.
- 2026-04-28 16:50:19 PDT - Investigated Scorp's screenshot question about `路線` and the last Yokohama route. Runtime route inspection showed `路線` was `横浜市 3号線`, i.e. Yokohama Municipal Subway Blue Line, and the long final label was `横浜高速鉄道みなとみらい21線`. Updated `docs/v4.html` so Yokohama municipal `1号線/3号線` display as `横浜市営地下鉄ブルーライン`, `4号線` as `横浜市営地下鉄グリーンライン`, `みなとみらい21線` as `みなとみらい線`, and Chiba Urban Monorail's `1号線/2号線` as `千葉都市モノレール1号/2号` without raw `号線`. Extended `scripts/tests/v4_route_choice_audit.js` to fail generic player-facing route labels. Latest Playwright audit: `checkedRows=1675021`, `checkedChoices=1679010`, `genericRouteLabelCount=0`, `rawNumberedLineLabelCount=0`, and `anomalyCount=0`; Yokohama choices now include `横浜市営地下鉄ブルーライン` and `みなとみらい線`.
- 2026-04-28 16:59:26 PDT - Fixed the Yokohama Tokyu/Fukutoshin through-running display bug Scorp reported. Root cause: 横浜 Tokyo Metro Fukutoshin/Yurakucho through rows had noisy stop-level `京急本線` traces and the player display could still use far terminal-side labels such as `西武池袋線`/`東京メトロ有楽町線`. `docs/v4.html` now derives non-JR through route choices from the adjacent boarding stop -> next stop direction and, for selected ordinary rows, prefers that adjacent direction line at the boundary. Runtime probe confirms 横浜 no longer shows `東京メトロ副都心線`; trains heading to `反町`/`菊名` show `東急東横線`, while trains heading to `新高島`/`みなとみらい` show `みなとみらい線`. Extended `scripts/tests/v4_route_choice_audit.js` with Yokohama remote through route/label checks. Latest Playwright audit: `checkedRows=1675021`, `checkedChoices=1675374`, `checkedLabels=1675374`, all Yokohama remote through counts `0`, and `anomalyCount=0`.
- 2026-04-28 17:03:31 PDT - Promoted Scorp's revised JR East northern-trunk scope into `AXIOMS.md` and v4 display logic: `東北本線` is the main line from `東京` through `上野`/`大宮` toward `盛岡`, `常磐線` starts as the branch at `上野`, `高崎線` starts as the branch at `大宮`, and `上野東京ライン` is only the public nickname for `東北本線` on the `東京`-`上野` segment, not the classification principle. `docs/v4.html` now normalizes JR East `東北線` display to `東北本線`, keeps 東京-上野 as `上野東京ライン`, shows 上野 northbound Takasaki-bound trains as `東北本線` until 大宮, and shows `高崎線` only on the 大宮 branch. Updated `scripts/tests/v4_route_choice_audit.js` to forbid premature 上野 `高崎線` and to expect the selected label from the scoped JR northern-trunk route. Runtime probe checked 東京/上野/大宮 choices and labels; full Playwright audit remains clean with `1675021` rows, `1675374` choices/labels, and `anomalyCount=0`.
- 2026-04-28 17:17:05 PDT - Fixed Scorp's "shinkansen disappear" report. Root cause: major stations such as `東京` can have multiple transfer-equivalent station groups; the first `東京` group exposed conventional/metro routes, while Shinkansen departures lived in another same-name same-station group, so planning route choices could hide Shinkansen depending on which group was selected. `docs/v4.html` now uses `includeTransferEquivalents` for player-facing departure/route-choice queries and station route chips, so selecting the conventional `東京` group still shows `東海道・山陽新幹線`, `東北・北海道新幹線`, `上越新幹線`, and `北陸新幹線`. Extended `scripts/tests/v4_route_choice_audit.js` with required Shinkansen visibility assertions for 東京/品川/大宮. Runtime probe confirmed Tokyo exact choices lacked Shinkansen but merged choices include all expected Shinkansen; full Playwright audit remains `0` anomalies.
- 2026-04-28 17:21:48 PDT - Added reusable Playwright audit `scripts/tests/v4_transfer_equivalent_route_audit.js` to catch any line hidden by transfer-equivalent station group splitting. The first run found `18` missing merged route choices across `14` clusters, including virtual-corridor labels splitting back into physical labels (`京浜東北線・根岸線`, `横須賀線・総武快速線`, `中央・総武線各駅停車`) and a transitive 難波/JR難波/南海難波 gap. Fixed `docs/v4.html` so corridor eligibility considers the query group, actual boarding group, and equivalent groups; segment candidates are valid across equivalent groups; and `equivalentStationGroupIds` returns the full connected component instead of only direct neighbors. Rerun results: transfer-equivalent audit `57` clusters, `116` station-group members, `195` exact route choices, `0` missing routes; existing v4 route-choice audit also remains `0` anomalies.
- 2026-04-28 19:07:04 PDT - Investigated all v4 train lines still involving the old `v3_release_candidate` data after Scorp asked whether data collection was wrong. Added reusable audit `scripts/ingest/audit_v4_v3_release_candidate_quality.py` and generated `data/v4_v3_release_candidate_quality_audit.json`. Findings: `39605` retained old-v3 trains across `108` operator-line pairs; the hard error set is `291` Tokyo Metro `9号線千代田線` trains whose old broad v3 `SG_SHINJUKU` origin rematched onto `新宿 / 4号線丸ノ内線`, including `62` rows headed to `小田原`. This confirms the 新宿→小田原/千代田线 issue is not just UI, but old v3 station-group pollution. No room-server deploy was run.
- 2026-04-28 19:10:55 PDT - Fixed the v4 planning regression where selecting a train could leave no selectable alighting stops. Root cause: after Shinkansen visibility was restored with transfer-equivalent station-group route lists, `availableDepartures()` could choose trains from equivalent groups, but `pendingDepartureContext()` re-resolved the selected train using exact station-group departures only. Updated `docs/v4.html` so pending train lookup also uses `includeTransferEquivalents: true`. Extended `scripts/tests/v4_transfer_equivalent_route_audit.js` to simulate selecting merged-equivalent trains and require downstream destinations; latest audit checks `57` clusters, `116` station groups, `195` exact routes, and `386` pending trips with `0` missing destinations. Full v4 route-choice audit still reports `0` anomalies. No room-server deploy was run.
- 2026-04-28 19:21:04 PDT - Added the v4 playtest interchange rule that nearby JR/private terminal groups with normalized matching names are direct transfer-equivalent for planning while preserving physical geometry. `docs/v4.html` now strips more branded railway prefixes such as `名鉄` and `近鉄`, records transfer-equivalent links with `transferMinutes: 0`, and exposes `transferMinutesBetweenStationGroups()` as the future walking-time interface. `名古屋`, `名鉄名古屋`, and `近鉄名古屋` now form one transfer-equivalent set, so 名古屋 route choices include JR/subway/あおなみ plus `名古屋鉄道名古屋本線` and `近畿日本鉄道名古屋線`. The browser transfer audit now checks `74` clusters, `151` station groups, `244` exact routes, and `493` pending trips with `0` anomalies; full route-choice audit remains `0` anomalies. No room-server deploy was run.
