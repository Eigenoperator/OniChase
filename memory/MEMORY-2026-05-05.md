# Daily Memory - 2026-05-05

- 2026-05-05 01:14:16 PDT - Fixed the v4 sparse route-choice/single-candidate train audit follow-up by adding station chunking to `scripts/tests/v4_route_choice_audit.js`, adding virtual `SHINKANSEN_AKITA` and `SHINKANSEN_YAMAGATA` corridors in `docs/v4.html`, and restoring Tokyo `秋田新幹線` planning via reviewed `こまち` synthetic branch trips. Verification after the fix: route-choice known-station audit `0` anomalies, route-choice trip chunk `0-10000` `0` anomalies, selected-train highlight chunk `0-30000` `0` failures, planner interaction `0` failures, and nationwide sparse route-choice chunks over all `9052` station groups found `278` sparse choices with `0` suspicious.
- 2026-05-05 01:15:15 PDT - Wrote `diary/DIARY-2026-05-05.md` and updated `STATUS.md`/`HISTORY.md`. GitHub sync was not performed in this step because the worktree still contains many large generated/cache artifacts and unrelated pre-existing modifications that need selective staging before a safe commit.
- 2026-05-05 10:20:47 PDT - Corrected the coupled-service axiom after Scorp clarified that 直挂/併结 is not a through-running route-merge rule. Non-Shinkansen coupled trains keep the umbrella train row + portion picker rule (`成田エクスプレス`, `関空快速・紀州路快速`), while Shinkansen coupled services are a display exception: no `東北・北海道・秋田新幹線` route choice and no `はやぶさ・こまち` umbrella row; they still count as `same_train` during the shared physical segment. Verification: full `scripts/tests/v4_route_choice_audit.js --page-url http://127.0.0.1:4177/docs/v4.html` scanned `9052` station groups, `111175` trips, and `1589722` route-choice/label checks with `0` anomalies, including `368` Shinkansen coupled same-train equivalence edges.
- 2026-05-05 10:37:26 PDT - Removed extra generic `直通` display text from v4 train labels and stitched-train preview text so 秋田新幹線 rows, current-plan cards, replay rows, and bottom scrolling playback show only the normal public train/route name. Added a global route-choice audit counter that fails if synthetic/stitched trips expose appended `直通` display text; latest full audit reports `throughDisplayTextCount: 0` and `0` anomalies.
- 2026-05-05 10:47:47 PDT - Verified the no-extra-`直通` display rule after optimizing the audit check: `node --check scripts/tests/v4_route_choice_audit.js` and `git diff --check` passed; a dedicated browser probe scanned all `111175` trips and `565` stitched-trip previews with `labelMatchCount: 0` and `previewMatchCount: 0`, while confirming Tokyo still has `秋田新幹線` and no merged `東北・北海道・秋田新幹線`; a formal route-choice audit chunk (`trip 0-999`, `station 0-999`) finished with `anomalyCount: 0`.
- 2026-05-05 13:08:14 PDT - Repaired the `つばさ` source-pair path without frontend fabrication. `merge_train_instance_datasets.py` now preserves segmented `Tsubasa` B/M source rows by train instance, prefers mini-Shinkansen route identity (`SHINKANSEN_YAMAGATA`/`SHINKANSEN_AKITA`), and `build_v3_train_index.py` keeps the same-number B/M pair when an overlapping unpaired seasonal row has identical stops/times. Rebuilt the v3/v4 chain including `v3_tokyo_bundle`: gameplay now has `14` full Tokyo-Fukushima-Yamagata/Shinjo `Tsubasa` synthetic trips, Tokyo exposes `山形新幹線`, and route-choice known/full audits pass with `0` anomalies.
- 2026-05-05 13:08:14 PDT - Optimized `scripts/tests/v4_route_choice_audit.js` full nationwide scanning into controlled stages and chunked global scans. Full default audit now runs `global`, `duplicates`, `known`, and `focused` as separate page runs; global defaults to `20000`-trip chunks. The first post-optimization full command completed in `3:58.60`, scanned `1589947` global rows/labels, `9052` duplicate stations, `579` stitched previews, and reported `0` anomalies.
- 2026-05-05 13:08:14 PDT - Cleared the priority `ひたち` limited-express trace suspects. `build_v4_gameplay_bundle.py` now applies reviewed Joban limited-express segment identity for `ひたち`/`ときわ`: 品川-東京 as `東海道線`, 東京-上野 as `東北線`, 上野/仙台 to the Joban-side next stop as `常磐線`; named limited expresses no longer preserve source-line terminal tails incorrectly. `v4_limited_express_trace_suspect_audit` now has `0` remaining `ひたち` suspects and records 上野-土浦/水戸 plus 仙台-相馬 as reviewed long physical segments.
- 2026-05-05 13:57:32 PDT - Fixed the follow-up Yamagata Shinkansen selected-train highlight bug. Root cause: after `Tsubasa` B/M source pairing was restored, the public `山形新幹線` corridor identity leaked into the physical lineTrace for the Tokyo-Fukushima shared trunk, so selected-train highlighting could color the shared segment as `山形新幹線` instead of the real `東北新幹線`. `build_v4_gameplay_bundle.py` now writes mini-Shinkansen shared trunk segments for `Tsubasa` and `Komachi` as physical `東北新幹線` while still exporting public `SHINKANSEN_YAMAGATA`/`SHINKANSEN_AKITA` service patterns for route choice. `docs/v4.html` keeps a reviewed fallback with the same rule, `AXIOMS.md` records that mini-Shinkansen public corridor identity and physical trace identity must stay separate, and `v4_selected_train_highlight_audit.js` now checks all current `14` synthetic `つばさ` trips. Verification: raw compact scan found `14` Tokyo-Fukushima `Tsubasa` source segments with `0` leaked `山形新幹線` trace, selected-train audit sample checked `5000` trips plus all `14` Tsubasa cases with `0` failures, and full route-choice audit completed in `4:21.75` with `1590754` global rows/labels and `0` anomalies.
- 2026-05-05 14:07:19 PDT - Corrected the mini-Shinkansen branch identity axiom after Scorp pointed out that branch-side Shinkansen must not be displayed/classified as ordinary limited express or ordinary rail. `build_v4_gameplay_bundle.py` now keeps `Tsubasa` branch segments as `SHINKANSEN_YAMAGATA` and `Komachi` branch segments as `SHINKANSEN_AKITA`; only shared Tokyo-Fukushima/Morioka segments resolve to physical `東北新幹線` for continuous highlight geometry. Ordinary `奥羽線` and `田沢湖線` remain separate `rail` service routes, and the selected-train audit now fails if `つばさ`/`こまち` recorded trace identity leaks into those ordinary branch lines. Verification: raw compact scan found `つばさ 55`, `こまち 41`, and `badTrace 0`; selected-train audit sample checked `5000` trips, `14` Tsubasa shared cases, and `131` mini-Shinkansen independent-trace cases with `0` failures; route-choice `known,focused` stages reported `0` anomalies.
- 2026-05-05 17:27:09 PDT - Finished Scorp's requested `1/2/3` pass. `v4_route_choice_audit.js` now has stages `global`, `duplicates`, `known`, `mini-shinkansen`, and `focused`, emits progress to stderr, and reuses one loaded page for global trip chunks; latest default full run completed in `3:26.71` with `1589947` global rows/labels, `9052` duplicate stations, and `0` anomalies. The mini-Shinkansen stage checks `8` branch stations with `450` Shinkansen entries and `457` ordinary entries, all `0` anomalies. Limited-express trace triage now filters normal Shinkansen express skips, records reviewed long skips separately, fixes `ひだ36号` 名古屋-米原 to `東海道線`, and adds reviewed path hints for NEX 東京-成田空港/空港第2ビル, あずさ 上諏訪-松本, and スペーシア日光 大宮-栃木; latest suspect audit reports `unreviewedSuspectCount: 0`.
- 2026-05-05 17:42:34 PDT - Fixed the Sunrise coupled-service regression. Official JR West Sunrise timetable confirms `サンライズ瀬戸` and `サンライズ出雲` run coupled from 東京 to 岡山, then split to 高松 and 出雲市. `docs/v4.html` now normalizes mixed Sunrise rows into Seto/Izumo portions, filters seasonal `サンライズ出雲91/92` out of regular gameplay, and counts the Tokyo `サンライズ瀬戸・出雲` route choice as one train. `scripts/tests/v4_route_choice_audit.js` focused stage now asserts one Tokyo Sunrise umbrella row with portion terminals `高松` and `出雲市`; browser probe and `known,focused` route-choice audit both passed with `0` anomalies.
- 2026-05-05 19:40:48 PDT - Followed up on Scorp's “Sunrise still not correct” report. The remaining player-facing issue was the 2/3 train-row umbrella label: playable rows were already collapsed to one coupled row, but the label still rendered as duplicated `サンライズ瀬戸・サンライズ出雲`. `coupledUmbrellaLabel()` now normalizes that pair to `サンライズ瀬戸・出雲`, and the focused route-choice audit now checks the actual Tokyo selected-route 2/3 rows: exactly one coupled row, label `サンライズ瀬戸・出雲`, portions to `高松` and `出雲市`. Verification passed with `0` anomalies.
