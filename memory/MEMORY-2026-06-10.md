# Daily Memory - 2026-06-10

- 2026-06-10 13:10:44 PDT - Repo review after V5 data gates redirected active work back to V4. Current checked-in V4 evidence is not fully green: `data/v4_route_choice_audit_release.json` has `anomalyCount: 2`, `data/v4_selected_train_highlight_release.json` has `failureCount: 200`, and `data/v4_data_quality_audit.json` has `errorCount: 1`. First triage targets are `埼京線` route-choice physical/source identity, selected-train endpoint/path coverage around `越後湯沢` / `熱海` / `尾久` / `佐和` / `田端`, and 八王子 `むさしの号` station coverage. Do not continue V5 expansion until V4 evidence and docs are reconciled unless Scorp changes priority.
- 2026-06-10 14:48:09 PDT - V4 repair pass made the current route-choice/data-quality/selected-highlight evidence green. Route-choice current rerun isolated the remaining anomaly to 上野->日暮里 `常磐線`; `scripts/tests/v4_route_choice_audit.js` now allows that exact passenger-facing Joban choice over the northern trunk physical trace, and `data/v4_route_choice_audit_release.json` is `anomalyCount: 0` / `tripCount: 110489`. `docs/v4.html` now adds bounded selected-path station-pair fallback for non-strict missing geometry and reviewed Kintetsu limited-express hints through 伊勢中川 / 大和八木 / 生駒 / 伊勢市 / 近鉄四日市; current 8-shard selected-highlight scan checked `110489` trips with `failureCount: 0` (artifact ignored by git). `data/v4_data_quality_audit.json` rerun reports `errorCount: 0` / `warningCount: 0`. Follow-up: route-choice global stage stayed `0` anomalies after these edits, but standalone `--stages duplicates` timed out at `180s`, so profile/fix that audit stage before calling the full default route-choice script performance-green.
- 2026-06-10 15:45:52 PDT - Fixed the V4 route-choice `duplicates` stage timeout by chunking duplicate station scans on one loaded page (`--station-chunk-size`, default `1000`). Duplicates-only now checks all `9052` station groups with `duplicateStationTitleCount: 0` in about `82s`. Full default `v4_route_choice_audit.js` now completes all stages in about `255s` with `anomalyCount: 0`, `stationCount: 9052`, and `tripCount: 110489`; promoted this to `data/v4_route_choice_audit_release.json`. Reran `audit_v4_data_quality.py --browser-audit-json data/v4_route_choice_audit_release.json --fail-on-error`; data-quality remains `errorCount: 0`, `warningCount: 0`.
- 2026-06-10 16:33:18 PDT - Scorp confirmed V4 can be treated as finished for now and asked to move back to V5. A first attempt to kick off V5 checks used unsafe parallel/heavy commands and stressed the editor; stop doing that. V5 work must proceed in safe release-contract mode: no heavy V5 bundle rebuilds, large gzip scans, planner/map tile rewrites, or parallel browser/data audits inside Codex. Current V5 scope should be documented from existing evidence first: rail + flight + ship + scoped bus gameplay, with bus exposure limited to airport buses and port-connector buses while ordinary local/highway/night bus data remains source/runtime-only unless explicitly promoted.
