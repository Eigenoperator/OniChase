# Daily Memory - 2026-05-11

- 2026-05-11 PDT - Fixed duplicate `つばさ` rows in v4 planning 2/3. Root cause was that the UI kept both the original `東京 -> 福島` source segment and the synthetic `東京 -> 山形/新庄` mini-Shinkansen through trip, because the old boardable-departure dedupe key included first/last stops. `docs/v4.html` now dedupes Shinkansen rows by logical route choice, boarding station, departure minute, public train number, and next stop, and prefers synthetic coupled-through trips. Validation: local `v4_route_choice_audit.js --stages mini-shinkansen,known` passed with `anomalyCount=0`; online GitHub Pages probe confirmed no duplicate `山形新幹線` rows at `東京`, `福島`, or `山形`. Deployed commit `49a67d7`.
- 2026-05-11 19:05:31 PDT - Continued v4 release gate work after the `つばさ`/`むさしの号`/limited-express highlight fixes. Fixed selected-train physical path issues where source route labels omitted adjacent real junction segments: `石北線` to `旭川` via `新旭川`, `函館線/室蘭線/千歳線` through `沼ノ端`, `徳島線` via `佃`/`佐古`, and `芸備線` via `備中神代`. Fixed short `埼京線` and 相鉄-JR through trains around `新宿`/`池袋` so route choice and highlight stay on the reviewed physical passenger corridor instead of leaking transfer-equivalent subway/山手線 candidates. Added the adjacent-trunk selected-path rule to `AXIOMS.md`. Gate results: planner interaction `0` failures, trip real-geometry `strictMissingRealPathCount=0`, coupled display `0` failures after guarding missing MapLibre layers, route-choice known/focused/mini `0` anomalies, route-choice global shards `0` anomalies, high-risk selected-train highlight `0` failures, and sampled ordinary selected-train shards through `0-6000` `0` failures. Full ordinary selected-train scanning remains very slow and should be optimized before treating it as a fast release gate.
