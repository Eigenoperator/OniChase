# Daily Memory - 2026-05-01

- 2026-05-01 14:24:03 PDT - Continued the nationwide limited-express number-gap investigation after the Thunderbird/Tsuruga fix. Fixed `parse_jreast_train_detail.py` so JR East single-train continuation pages with adjacent columns, such as `成田エクスプレス 9号（新宿－東京－成田空港）`, merge into one stop sequence instead of overwriting the origin segment. Added JR East extra scan stations for `成田線`, `常磐線`, and `中央線`; added NEX and Chuo official supplements to current collection defaults; rebuilt NEX, Joban, Chuo, current, gameplay, and the limited-express gap audit. Also added train-number text to JR East official `service_instance_id`s so one multi-column detail page cannot create duplicate service IDs. Verified NEX 9/11/13/17/21/25/29/33/37/41/45/49 are present in current with complete official stop sequences where applicable. Official JR East 2026-05 pages show remaining Tokiwa/Azusa/Kaiji/Fuji-Kaiyu flags are current timetable skips or different-origin patterns rather than missing parser data. JR West spot checks show remaining Kounotori/Kinosaki/Super Hakuto/Super Matsukaze flags are already present from different origins or seasonal/scope differences. Validation: py_compile clean for modified ingest scripts, NEX/Joban/Chuo official audits all report duplicate ids 0 / unmatched 0 / bad time order 0, current writes 125331 trains with duplicate service ids 0 and short trains 0, route display integrity reports 0 missing trace segments, 0 ambiguous trace segments, and 0 duplicate train-station route memberships. No room server deploy.
- 2026-05-01 14:33:47 PDT - Converted the limited-express number-gap audit from a broad candidate list into a cleaner red-light queue. Added reviewed gap patterns for already-confirmed normal cases: alternate-origin services (`成田エクスプレス`, `こうのとり`, `きのさき`, `ときわ`, `わかしお`, `スーパーはくと`, `スーパーまつかぜ`), official sparse numbering (`あずさ`, `ときわ`, `富士回遊`), and parallel-family numbering (`にちりんシーガイア5号` vs plain `にちりん`). Re-ran the audit: `gapCandidateCount=30`, `highPriorityGapCandidateCount=2`, `reviewedGapPatternCount=12`. Remaining high-priority data gaps are old private-source rows where online/current references indicate missing trains: Odakyu `はこね24/26/28` and Tobu `スペーシアＸ8/10`. Validation: `python3 -m py_compile scripts/ingest/audit_v4_limited_express_number_gaps.py`.
- 2026-05-01 14:45:35 PDT - Searched the remaining private-rail limited-express red items online before adding anything to the train dataset. Confirmed from Tobu's official Spacia X timetable that `スペーシアＸ8号` and `10号` are Kinugawa-onsen-origin services, not Tobu-nikko-origin services; current already contains both as `鬼怒川温泉 -> 浅草`. Confirmed public Odakyu stop tables for `はこね24/26/28` mark them as Saturday/holiday trains, so adding them to the current weekday snapshot would pollute weekday data. Updated `REVIEWED_GAP_PATTERNS` instead: Spacia X becomes `alternate_origin_pattern`, Hakone becomes `holiday_only_pattern`. Re-ran the audit: `gapCandidateCount=28`, `highPriorityGapCandidateCount=0`, `reviewedGapPatternCount=14`. Validation: `python3 -m py_compile scripts/ingest/audit_v4_limited_express_number_gaps.py`.
- 2026-05-01 14:59:45 PDT - Checked the remaining 28 low-priority limited-express number gaps against current source URLs and online/official references. All were normal skipped-number patterns: farther-origin through services, farther-terminal services, branch destinations, short turns, official sparse numbering, or holiday-only services. Added reviewed gap patterns for the rest of the JR West (`くろしお`, `こうのとり`, `きのさき`, `はるか`, `スーパーまつかぜ`), JR East (`ひたち`, `いなほ`, `湘南`, NEX), JR Kyushu/Shikoku/Central NAVITIME-backed (`ソニック`, `ひゅうが`, `あしずり`, `ひだ`), and Odakyu/Tobu source-snapshot cases. Re-ran the audit: `gapCandidateCount=0`, `highPriorityGapCandidateCount=0`, `reviewedGapPatternCount=42`. Validation: `python3 -m py_compile scripts/ingest/audit_v4_limited_express_number_gaps.py`.
- 2026-05-01 15:08:34 PDT - Added UI/browser regression coverage for the reviewed limited-express train-number cases in `scripts/tests/v4_route_choice_audit.js`. The audit now includes route-choice count floors for 敦賀, 京都, 新大阪, 白浜, 新宿, 大船, 成田空港, 松本, 大月, 品川, 東京, and 上野, plus exact train-number presence checks for `サンダーバード`, `はるか`, `くろしお`, `成田エクスプレス`, `あずさ`, `かいじ`, and `富士回遊` examples that previously looked like skipped-number gaps. Validation: `node scripts/tests/v4_route_choice_audit.js --page-url http://127.0.0.1:4173/docs/v4.html` reports `anomalyCount=0`, `checkedRows=1617356`, and `checkedLabels=1617356`.
- 2026-05-01 15:21:28 PDT - Began the pre-player-interaction nationwide hub data acceptance pass. A focused browser probe found that reviewed transfer pairs could over-apply to distant same-name stations: the `JR小倉`/`小倉` direct pair was valid in Kyoto but also linked 北九州 小倉 to 京都 小倉 through transfer-equivalent search, leaking `近畿日本鉄道京都線` and `奈良線` into the Kyushu hub route list. Added a `5km` cap for reviewed direct transfer equivalence in `docs/v4.html` and matching Python audits, then added `scripts/tests/v4_hub_data_audit.js` to disambiguate same-name hubs by required routes and check route-count floors plus forbidden remote routes. Validation: hub audit checks `21/21` hubs with `anomalyCount=0`; transfer-equivalent route audit reports `anomalyCount=0`, `checkedClusters=75`, `checkedStationGroups=153`, `checkedPendingTrips=757`; full route-choice audit remains `anomalyCount=0`, `checkedRows=1617356`, `checkedLabels=1617356`; py_compile passed for modified Python audits.
- 2026-05-01 15:30:40 PDT - Added `scripts/tests/v4_long_distance_playability_audit.js`, a staged real-route browser audit for long-distance and remote-station playability. It validates that each leg can be planned by choosing a real departure from the current station to the next waypoint, rather than relying on a broad graph search that gets swamped by local high-frequency routes. The 30-case set covers trunk trips and remote endpoints: 白浜, 城崎温泉, 天橋立, 高山, 新宮, 出雲市, 宇和島, 佐世保, 長崎, 宮地, 枕崎, 稚内, 根室, 大湊, 女川, 銚子, 奥多摩, 河口湖, 南小谷, 浜坂, 境港, 奈半利, 宿毛, and ゆいレール. Validation: `node scripts/tests/v4_long_distance_playability_audit.js --page-url http://127.0.0.1:4173/docs/v4.html` reports `passedCaseCount=30`, `caseCount=30`, `anomalyCount=0`.
- 2026-05-01 15:35:05 PDT - Extended the long-distance playability audit so each reached origin, transfer, and final waypoint also becomes a station-surface data check. For every waypoint the browser audit now checks expected route-choice titles, non-empty boardable departures, and candidate departures toward the next staged stop; terminal/no-downstream rows are tracked as `skippedNonBoardableDepartureCount` instead of false anomalies. Validation: `node scripts/tests/v4_long_distance_playability_audit.js --page-url http://127.0.0.1:4173/docs/v4.html` reports `caseCount=30`, `passedCaseCount=30`, `waypointAuditCount=72`, `anomalyCount=0`.
- 2026-05-01 15:42:51 PDT - Upgraded `scripts/tests/v4_long_distance_playability_audit.js` from fixed-only regression cases to fixed plus seeded random route-chain acceptance. Random mode builds a raw-service station pool, chooses a random origin, rolls 1-4 real departures/downstream stops to create its own planned route and destination, then validates the generated waypoints through the same browser station-surface checks. Default `--random-count` is now `20`, with `--seed` for reproducibility. Validation: `node scripts/tests/v4_long_distance_playability_audit.js --page-url http://127.0.0.1:4173/docs/v4.html --seed 20260501` reports `caseCount=50`, `fixedCaseCount=30`, `randomCaseCount=20`, `passedCaseCount=50`, `waypointAuditCount=137`, `anomalyCount=0`, `elapsedMs=5052`; sample random cases include 別保→根室, 法隆寺→姫路, 湯檜曽→新潟, 浦川→伊那北, 三谷→温泉津, and 新神戸→熊本.
- 2026-05-01 16:12:41 PDT - Expanded the long-distance playability audit to `100` fixed-class cases and `100` random cases by default. Fixed-class now means the original `30` hand-authored routes plus `70` stable novelty-biased coverage cases; both coverage and random generation score candidates by new route titles and new station groups before distance/leg count, so the audit prefers lines and stations not already covered. Also fixed a false positive where late final destinations with no later boardable departure were reported as waypoint failures; only waypoints with a next target now require boardable departures. Validation: `node scripts/tests/v4_long_distance_playability_audit.js --page-url http://127.0.0.1:4173/docs/v4.html --seed 20260501` reports `caseCount=200`, `handFixedCaseCount=30`, `coverageCaseCount=70`, `fixedCaseCount=100`, `randomCaseCount=100`, `passedCaseCount=200`, `waypointAuditCount=682`, `anomalyCount=0`, `elapsedMs=32416`; summary script counted `495` unique waypoint stations and `165` unique leg routes.
