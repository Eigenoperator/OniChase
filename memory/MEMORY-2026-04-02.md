# Daily Memory - 2026-04-02

- 2026-04-02 13:53:48 PDT - On the first prompt of the new natural day, read `STATUS.md`, checked the `diary/` folder, confirmed that `DIARY-2026-03-31.md` and `DIARY-2026-04-01.md` were missing, and backfilled both diary entries in concise Chinese based on actual recorded project activity.
- 2026-04-02 13:56:59 PDT - Created `ENGINE_ARCHITECTURE.md` to formalize the engine boundary strategy: keep `data schema`, `rules`, `simulation input/output`, and `frontend-engine JSON contracts` independent so the current Python core can later be replaced module by module without rewriting the whole project.
- 2026-04-02 13:58:55 PDT - Compressed `STATUS.md` from a long milestone dump back into a short handoff-oriented control file and archived the older stable progress into `HISTORY.md`, in line with the 50-line status rule from `AXIOMS.md`.
- 2026-04-02 14:02:30 PDT - Reworked `planner.html` toward a more playable local client presentation by strengthening the map-first layout, adding runner/hunter match cards, a bottom quickbar, a map legend, and layered route traces so the active plan reads more like a game board than a raw editor.
- 2026-04-02 14:35:46 PDT - Repaired the local launcher path after Scorp reported that `START_ONICHASE_LOCAL.desktop` would not open reliably: the desktop entry now launches `START_ONICHASE_LOCAL.sh` directly, and the shell launcher now appends to `.onichase-launch.log` for easier debugging of future launch failures.
- 2026-04-02 14:39:15 PDT - Created the first native local client shell in `local_client.py` using `tkinter` because `PySide6` is not installed in the current environment; also added `START_ONICHASE_CLIENT.sh`, `START_ONICHASE_CLIENT.desktop`, and `LOCAL_CLIENT.md` so OniChase can now be opened as a local desktop window without relying on the browser path.
- 2026-04-02 14:41:34 PDT - Fixed the new client launcher permissions after Scorp reported that double-clicking `START_ONICHASE_CLIENT.desktop` only showed the file contents; both the desktop entry and the shell launcher are now executable, which is required for GNOME/Nautilus to treat them as launchers instead of plain text.
- 2026-04-02 14:48:41 PDT - Added first direct map interaction to `local_client.py`: the local client now supports left-button drag panning on the map board, and `.gitignore` now excludes the generated launcher logs `.onichase-launch.log` and `.onichase-client-launch.log`.
- 2026-04-02 14:51:23 PDT - Added mouse-wheel zoom and station-click selection to `local_client.py`; the local map now supports pan, zoom, and station inspection, and the selected station is highlighted with contextual info in the right-side panel.
- 2026-04-02 14:55:40 PDT - Reorganized the workspace by responsibility: moved the native client into `app/`, browser prototypes into `ui/`, split `scripts/` into `engine`, `ingest`, and `dev`, repaired launcher/runtime paths to the new locations, and added `WORKSPACE.md` as the concise directory map.
- 2026-04-02 15:00:21 PDT - Upgraded `app/local_client.py` from a map shell into the first interactive local play workstation: the right side now includes action buttons to set a start station, add earliest boarding, add ride-to-selected, add wait steps, undo or clear steps, and run the real simulator in-window to view a compact result summary.
- 2026-04-02 15:51:40 PDT - Refined `app/local_client.py` toward the intended play flow: at-station state now shows the next catchable departures from the current station as direct boarding buttons, on-train state now shows reachable downstream stops as direct alighting buttons, and clicking a reachable stop on the map while on a train can immediately append the corresponding `RIDE_TO_STATION` step.
- 2026-04-02 16:00:29 PDT - Extended the on-train route display in `app/local_client.py`: the client now shows the full remaining stop list for the current service, and for loop services it stops at one full loop ahead by cutting at the first return to the boarded station.
- 2026-04-02 16:04:56 PDT - Made the native client's right-side column scrollable with the mouse wheel when hovered, so long departure lists, destination lists, and simulation summaries remain usable even in smaller window sizes.
- 2026-04-02 16:11:12 PDT - Added a top-bar `Settings` button to `app/local_client.py` and introduced the first adjustable local-client preference: UI font size. The client now uses a shared font/style system so cards, map labels, helper text, and buttons scale together when the setting changes.
- 2026-04-02 16:12:50 PDT - Increased the default UI font-size offset in `app/local_client.py` so the first-launch experience is more readable without requiring any manual settings adjustment.
- 2026-04-02 16:15:43 PDT - Increased the native client's default UI font-size offset again to a much larger baseline and expanded the settings slider maximum, so first launch is noticeably bigger and the user still has room to scale up further from Settings.
- 2026-04-02 16:17:53 PDT - Changed the native client's default font-size offset to the user-requested `10` level and replaced the fixed left-right layout with a draggable `PanedWindow`, so the right planning column can now be widened directly by dragging the divider.
- 2026-04-02 16:20:28 PDT - Added a vertical `PanedWindow` inside the native client's right column so the `Info`, `Actions`, and `Result` sections can now be resized independently by dragging their internal dividers.
- 2026-04-02 16:22:31 PDT - Fixed the native-client launch crash caused by unsupported `highlightthickness` parameters on `tkinter.PanedWindow`; the resizable split layout now uses only compatible options.
- 2026-04-02 17:06:14 PDT - Upgraded the native client's `Result` section into a basic replay inspector: after `Run Simulation`, the client now stores the latest replay, shows a scrollable event list, and displays details for the selected event including time, type, trigger fields, and runner/hunter post-event state.
- 2026-04-02 17:17:24 PDT - Added a stronger real-time match-flow layer to `app/local_client.py`: the top bar now shows an explicit `PLANNING` countdown before `LIVE` begins, both sides are visible during planning, the opponent is hidden once live play starts, the plan panel marks `DONE` versus `NEXT` steps, and editing during `LIVE` now automatically trims only unresolved future steps so route changes branch from the current in-game moment.
- 2026-04-02 17:28:11 PDT - Linked the native client's replay list back into the board view: selecting a replay event now redraws the map at that event's `state_after`, temporarily reveals both sides for replay clarity, and approximates on-train positions by anchoring each train marker near the closest scheduled stop at the selected replay time.
- 2026-04-02 17:32:40 PDT - Reworked the native client's station-side planning flow into a clearer two-step railway-style interaction: the player now chooses a departure first, sees the selected train highlighted with route preview, then chooses a destination from a horizontally draggable strip of future stops; the selected service is also highlighted on the map, and the planning countdown was made much more visually prominent.
- 2026-04-02 17:57:15 PDT - Smoothed the native client's station-planning UX after Scorp reported that the plan area kept flashing and the destination step was hard to find: the action card now skips unnecessary rebuilds when only the planning countdown changes, which reduces flicker, and after choosing a train the right panel auto-scrolls toward the destination strip so the next step is visible immediately.
- 2026-04-02 18:02:20 PDT - Further clarified the native client's destination-picking step after Scorp reported that the station selection did not seem to display normally: the destination strip now has a stronger instruction line, a visible horizontal scrollbar, and a more aggressive auto-scroll after choosing a train so the `Ride Here` cards are easier to spot.
- 2026-04-02 19:40:56 PDT - Added a second, more direct Step 2 interaction after Scorp reported that the destination view still felt broken: once a train is selected, the client now also highlights all reachable downstream stations on the left map and allows clicking those highlighted stations directly to commit the board-and-ride choice, instead of forcing the player to rely only on the right-side strip.
- 2026-04-02 19:45:57 PDT - Hardened the right-side Step 2 display after Scorp reported that the destination stations still were not visible there: the action card now renders a guaranteed visible vertical destination list with explicit `Ride Here` buttons above the horizontal route strip, so destination choice no longer depends on that strip alone rendering correctly.
- 2026-04-02 19:58:23 PDT - Addressed the likely root cause of the remaining Step 2 visibility problem after Scorp pointed out that the Step 1/Step 2 area itself was too short: once a train is selected, the client now collapses the long Step 1 departure list into a compact selected-train summary with a `Choose Another Train` action, and the right-side pane automatically gives more height to the action area so Step 2 has more room.
