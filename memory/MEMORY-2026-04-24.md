# Daily Memory - 2026-04-24

- 2026-04-24 13:52:00 PDT - Backfilled `diary/DIARY-2026-04-23.md`, updated `STATUS.md` to reflect the clarified v3 focus on planner-facing departure correctness and top-level doc drift, and prepared a control-file-only sync to GitHub while leaving unrelated dirty local files and large source caches untouched.
- 2026-04-24 14:11:21 PDT - Added reusable `scripts/ingest/audit_v3_planner_departures.py`, wired it into `scripts/ingest/run_v3_data_quality_audits.py`, added `scripts/tests/test_v3_planner_departure_audit.py`, generated `data/v3_planner_departure_audit.json`, and refreshed `README.md` / `ROADMAP.md` / `STATUS.md` to match the real v3 MapLibre Tokyo path. Current planner-audit summary is `WARN`: same-operator forbidden borrowing is `0`, but there are still `83826` unsurfaced boardable trip stops and `314` visible station/route pairs with no boardable departure.
- 2026-04-24 14:36:14 PDT - Added `scripts/ingest/summarize_v3_planner_departure_audit.py` and generated `data/v3_planner_departure_summary.md` so planner warning families are readable without ad hoc JSON probing. Split strict physical segment matching from planner boarding matching in `docs/v3.html` / `ui/v3_maplibre.html` and `audit_v3_planner_departures.py`; this lets real private physical lines surface through-service departures without breaking selected-trip physical highlight scoring. The warning count dropped from `83826` to `39245` unsurfaced boardable trip stops, same-operator forbidden borrowing stayed `0`, and targeted unittest coverage passed.
- 2026-04-24 15:35:43 PDT - Fixed v3 planner through-service route identity and boarding visibility: Tokyo Metro route identity now uses service/signature inference, planner-only through aliases are separated from map geometry, physical alias routes for `みなとみらい21線` / `埼玉高速鉄道線` / `相鉄本線` / `相鉄いずみ野線` are preserved in the bundle, and selected physical segments now prefer alias-matched route specs. Rebuilt v3 bundles; planner audit is now `WARN` with same-operator forbidden borrowing `0`, unsurfaced boardable trip stops `337`, and no-boardable station/route pairs `105`; the v3 planner/page/physical-through-running unittest suite passed.
- 2026-04-24 16:00:11 PDT - Standardized reusable v3 audit entry points: `run_v3_data_quality_audits.py --list` now describes every check, component audits for map/timetable coverage and generated bundle integrity accept `--output`, raw train dataset auditing accepts repeated `--path` plus clearer missing-file errors, and `README.md` now lists the reusable audit/regression tools. Verified py-compile, the audit list command, single-dataset raw audit, coverage/bundle component outputs to `/tmp`, and the unified data-quality check.
- 2026-04-24 17:01:10 PDT - Ran the v3 release-check pass and fixed a selected-trip physical highlight regression at the 横浜高速鉄道みなとみらい21線 boundary: the bundle builder now maps display names/canonical station keys into `station_key_to_group`, split map/timetable bundles were regenerated, and the 池袋 17:01 副都心線 -> 東急東横線 -> みなとみらい21線 golden again resolves to 3 / 6 / 4 physical segments. Full v3 core unittest suite passed `30` tests; data-quality remains `WARN` only, with hard integrity checks still clean: forbidden same-operator borrowing `0`, duplicate unified ids/signatures `0`, rendered lines without trips `0`, and train stations without map groups `0`.
- 2026-04-24 17:17:42 PDT - Reduced v3 planner warning debt by removing geometry-only boardability from planner route visibility in `docs/v3.html` and `audit_v3_planner_departures.py`: visible station/route pairs with no boardable departure dropped from `105` to `0`, unsurfaced boardable trip stops dropped from `337` to `331`, and forbidden same-operator borrowing stayed `0`. Updated the planner audit regression so a fully-cleared no-boardable aggregate is accepted; full v3 core unittest suite passed `30` tests and `run_v3_data_quality_audits.py --checks all` reports only non-fatal WARN families.
- 2026-04-24 18:19:03 PDT - Reduced v3 planner unsurfaced warning debt from `331` to `9` by rebuilding the bundle with Sotetsu through aliases (`相鉄本線`, `相鉄いずみ野線`, `相鉄新横浜線`), adding `sotetsu` operator normalization, preserving no-boardable station/route pairs at `0`, and fixing Tokyo Metro Odakyu limited-express route identity so `メトロホームウェイ` no longer falls under `日比谷線`. Verified `run_v3_data_quality_audits.py --checks all` and the v3 core unittest suite (`30` tests OK); remaining `9` warnings are external/homonym edges rather than planner line-mixing.
