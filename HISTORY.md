## 2026-03-30

- Initialized project control files and memory structure.
- Loaded `AXIOMS.md` as the canonical workflow rules for future sessions.
- Loaded rule draft v0.5 from `rule/RULES_v0.5.md` as the current gameplay baseline and added an English working translation in `rule/RULES_v0.5-EN.md`.
- Created the first technical schema draft in `SCHEMA.md` to map gameplay rules into runtime data structures.
- Created `STATE_MACHINE.md` to define the minimal phase-based and event-driven flow of one match.
- Created `PROTOTYPE_SCOPE.md` plus three schematic Tokyo candidate maps to start selecting the first playable mini-network.
- Selected `Candidate B: East Core` as prototype v0.1 and created `MAP_CANDIDATE_B.md` as the first node-level map draft.
- Created `MAP_CANDIDATE_B_GRAPH.md` and a graph-ready East Core visualization to define prototype v0.1 adjacency and interchange structure.
- Locked the 10-node `Candidate B` East Core map as the first system test map.
- Created `data/candidate_b_bundle.json` as the first static map bundle for prototype v0.1.
- Created and ran `scripts/validate_candidate_b_bundle.py` to verify the first map bundle structure.
- Added multilingual node names to the schema and Candidate B bundle for English, Japanese, and Chinese display support.
- Created the first timetable stub and minimal simulation script, and ran the first executable movement/capture test on the Candidate B map.
- Shifted the main prototype direction from the coarse Candidate B map to a real-data Yamanote Line test map and created `YAMANOTE_IMPLEMENTATION_PLAN.md`.
- Deleted Candidate B implementation artifacts after the direction change and created `data/yamanote_stations.json` as the first real Yamanote dataset.
- Created `visuals/yamanote_line.svg` to visualize the full 30-station Yamanote loop with multilingual station labels.
- Refreshed `MEMORY.md` so long-term continuity now reflects the current Yamanote-first project direction.
- Researched the JR East official timetable structure and documented the extraction path in `YAMANOTE_TIMETABLE_EXTRACTION.md`.
- Updated the train data model so train instances are not bound to a single line, and added the first Yamanote station validator plus JR East timetable discovery script.
- Parsed the first real JR East Yamanote train-detail pages and saved a seed train-instance timetable dataset.
- Normalized the first real Yamanote train-instance seed dataset onto canonical station IDs.
- Built the first station-wide real timetable pull from Hamamatsucho and obtained 246 weekday counterclockwise Yamanote train instances.
- Built the matching clockwise Hamamatsucho station-wide pull and obtained 265 weekday Yamanote train instances.
- Strengthened the train model for loop-line behavior so repeated stations can remain inside one continuous service record.
- Refactored script naming so reusable tooling is named by responsibility or JR East page format rather than the temporary Yamanote dataset name.
- Created `scripts/validate_train_instances_dataset.py` to validate train-instance datasets for duplicate train numbers, time ordering, canonical station IDs, and loop-aware stop sequencing.
- Extracted shared normalization logic into `scripts/train_instance_normalization.py`, updated the builder and normalization scripts to reuse it, and re-normalized both Hamamatsucho station-wide weekday datasets.
- Revalidated the normalized counterclockwise (`246`) and clockwise (`265`) Hamamatsucho datasets successfully after adding `service_instance_id` and `loop_pass_index` to station-wide outputs.
- Created `scripts/merge_train_instance_datasets.py`, merged the clockwise and counterclockwise Hamamatsucho pulls into `data/yamanote_weekday_train_instances_merged.json`, and confirmed there were no duplicate train numbers across the two directions.
- Produced the first merged weekday Yamanote database with `511` train instances and validated it successfully.
- Created `scripts/render_train_timetable_svg.py` and rendered `visuals/yamanote_weekday_timetable.svg` as a time-space overview of the full weekday merged Yamanote timetable.
- Created `SIMULATION_INPUT.md` to define the first reusable player action schema for the real timetable simulator.
- Implemented `scripts/simulate_match_from_train_instances.py` as the first minimal two-player simulator over real train-instance data.
- Added `data/scenarios/yamanote_weekday_same_train_capture_v0_1.json` and successfully reproduced a real same-train capture on train `720G` at `08:11`.
- Upgraded the simulator result format to include a full `match_event_log` and precise `state_before` / `state_after` snapshots for each event.
- Added `data/results/yamanote_weekday_same_train_capture_v0_1.result.json` as the first checked-in replay-style simulation result for inspection.
- Switched capture resolution from interval-overlap scanning to event-driven immediate checks, and now record the triggering event on each `CAPTURE`.
- Added `data/scenarios/yamanote_weekday_arrival_into_waiting_hunter_v0_1.json` and validated the rule "arrival into waiting hunter" with a real Yamanote example at `UENO` `08:11`.
- Created `rule/RULES_v0.6.md` and `rule/RULES_v0.6-EN.md` to formalize the newly confirmed boundary rule for same-minute alight/board crossing.
- Added two real Yamanote validation scenarios for that boundary: same-train crossing captures successfully, while different-train crossing does not capture.
- Added a rule-document convention to `AXIOMS.md`: every new rule version file must state its main change at the top, and updated `RULES_v0.6` / `RULES_v0.6-EN.md` accordingly.
- Created `PLANNING_FORMAT.md` to define the first planning-friendly layer above primitive actions, including `BOARD_ANY_OF`.
- Extended the simulator to accept `plan` input, emit `resolved_actions`, and log `PLAN_RESOLUTION` events.
- Enriched replay output with `event_scope`, `timeline_lane`, and `event_family`, and validated the fallback-plan behavior with `data/scenarios/yamanote_weekday_plan_fallback_v0_1.json`.
- Created `debug_gui.html` as a local replay-inspection GUI with preset loading, file upload, event scrubber, state cards, raw event JSON, and a simplified Yamanote position strip.
- Added `DEBUG_GUI.md` with the local launch instructions (`python3 -m http.server 8000`) and usage notes for debugging.
- Created `planner.html` as a planning-first local GUI for assembling runner and hunter routes against the real Yamanote timetable.
- Added `PLANNER_GUI.md` documenting how to launch the planning GUI and what it currently supports.
- Initialized local git in the workspace, connected the project to `Eigenoperator/OniChase`, rebased onto the remote bootstrap commit, and pushed the first full project import to GitHub.
- Added a double-click local launch path via `scripts/run_local_site.py`, `START_ONICHASE_LOCAL.sh`, and `START_ONICHASE_LOCAL.desktop`.
- Verified that the local launcher successfully starts the planning site and automatically selects an open localhost port.
- Reworked `planner.html` from a utility-style planning board into a more client-like gameplay-planning prototype with runner/hunter modes, a map-first layout, HUD cards, train next-stop display, and a right-side current-plan panel.
- Replaced the odd freeform Yamanote map layout in `planner.html` with a typical closed circular loop: stations are now spaced evenly around the ring in route order and labels are pushed outward radially for a clearer player-facing mental model.
- Added a git-backup workflow rule to `AXIOMS.md`: after each substantive project change, commit and push the current state to GitHub by default.
- Added a dedicated local runner-plan test preset to `planner.html`: match start defaults to `06:00`, runner starts in `plan` mode with pre-match planning assumptions shown in the UI, hunter defaults to holding position until match end, and future steps can now be trimmed quickly with `Cut Future` during plan iteration.
- Made planning more legible inside `planner.html` by drawing the active player's current plan directly on the Yamanote map as a dashed route trace with numbered waypoints, and clarified that the one-minute runner planning window happens before the in-game clock begins at `06:00`.

## 2026-04-02

- Created `ENGINE_ARCHITECTURE.md` to lock in a clean five-layer boundary strategy: data, rules, engine, interface, and frontend.
- Formally decided that current optimization strategy should prioritize stable schemas and JSON engine boundaries first, rather than prematurely rewriting the core away from Python.
- Archived the former verbose milestone list out of `STATUS.md` and reduced `STATUS.md` back to a short handoff document under the intended 50-line limit.
- Upgraded `planner.html` from a plain planning tool into a more game-like local playtest UI with stronger map emphasis, live runner/hunter match cards, a bottom quickbar, a clearer legend, and layered active/passive route presentation.
- Fixed the local launcher path by changing `START_ONICHASE_LOCAL.desktop` to call `START_ONICHASE_LOCAL.sh` directly instead of using a nested `bash -lc`, and added `.onichase-launch.log` output for easier diagnosis when local launch fails.
- Built the first native local client prototype in `local_client.py` using `tkinter`, plus `START_ONICHASE_CLIENT.sh` and `START_ONICHASE_CLIENT.desktop`, so local playtesting no longer has to depend on a browser-based website.
- Fixed the new desktop client launcher permissions after Scorp reported that double-clicking still opened the `.desktop` file as text; both `START_ONICHASE_CLIENT.desktop` and `START_ONICHASE_CLIENT.sh` are now executable.
- Added first map-navigation interaction to `local_client.py`: the Yamanote board can now be dragged with the left mouse button, making it possible to inspect different parts of the map during local playtesting.
- Added mouse-wheel zoom and station-click selection to `local_client.py`, so the local map can now be panned, zoomed, and inspected directly instead of only being a static board.
- Reorganized the workspace by responsibility: native client code now lives in `app/`, browser prototypes in `ui/`, and scripts are split into `scripts/engine`, `scripts/ingest`, and `scripts/dev`; launchers and docs were relinked to the new structure and the layout was documented in `WORKSPACE.md`.
- Promoted the native client from a pure shell to a first playable planning workstation: `local_client.py` now supports setting starts, adding board/ride/wait steps, undoing or clearing steps, and running the real simulator directly inside the window to inspect capture/result summaries.
- Reworked native-client planning input toward a more game-like flow: when the player is at a station the right panel now lists upcoming departures from that exact station, and after boarding it lists reachable downstream stops for the current train; clicking a reachable stop on the map while on board can now directly add the ride step.
- Expanded the on-train information model in the native client so the train outlook and destination list now show the full remaining service ahead; for loop services such as Yamanote runs that return to the boarded station, the view is capped at exactly one loop instead of only a short teaser list.
- Added a scrollable right-side panel to the native client so smaller window sizes remain usable: the action list, departure list, destination list, and simulation summary can now all be browsed with the mouse wheel when the pointer is over the right column.
- Added a first real settings surface to the native client: a top-bar `Settings` button now opens a small preferences window where the user can change the global UI font size, and the client now uses a shared font system so labels, cards, map text, and button styles all scale together.
- Increased the default native-client font baseline so the very first launch is more readable without requiring the user to open `Settings` before playtesting.
- Raised the default font baseline much more aggressively and expanded the settings slider ceiling, so the local client now opens at a clearly larger reading size and still leaves extra room for even bigger text if needed.
- Switched the local client's main split layout to a draggable horizontal `PanedWindow`, so the user can resize the left map and right planning column directly; also aligned the new default launch font size to the user's requested `10` level.
- Added a second level of resizable layout inside the right column by turning `Info / Actions / Result` into a vertical `PanedWindow`, so the user can independently decide how much height each right-side section should receive.
- Fixed a native-client launch regression introduced by the resizable layout work: removed unsupported `highlightthickness` arguments from `tkinter.PanedWindow` so the client no longer crashes while building the window.
- Upgraded the native-client result area from a plain text summary into a first replay surface: after simulation the `Result` section now shows a scrollable event list and a detail panel for the currently selected event, including event metadata and the runner/hunter post-event state snapshot.
- Added a clearer match-flow layer to the native client: the top bar now shows an explicit `PLANNING` countdown before `LIVE` begins, both sides remain visible during planning, opponent position becomes hidden during live play, and editing the active plan during `LIVE` now automatically trims only unresolved future steps so route changes branch cleanly from the current in-game moment.
- Linked replay selection back into the board view: choosing an event in the native client's result list now repositions the map markers to that event's `state_after`, shows a replay-focus banner on the map, and approximates on-train positions by anchoring the train near the nearest scheduled stop at that event time.
- Reworked native-client planning into a more user-facing railway flow: while at a station, the right panel now asks the player to choose a real departure first and only then choose where to ride it, the selected train stays highlighted before commitment, long destination strips can be dragged horizontally, the selected service is highlighted on the map, and the planning timer is now much more visually prominent.
- Reduced native-client right-panel flicker during planning by avoiding unnecessary full rebuilds of the action card every countdown tick; after selecting a train, the panel now also auto-scrolls toward the destination strip so the next step is easier to find.
- Made the station-selection step easier to discover after a train is chosen: the destination strip now has a much clearer instruction line, scrolls the right panel further downward automatically, and includes an explicit horizontal scrollbar in addition to drag-scrolling.
- Added a second, more obvious destination-selection path for Step 2: after choosing a train, the client now also highlights all reachable downstream stations on the left map and lets the player click those highlighted stations directly to commit the board-and-ride choice.
- Hardened Step 2 visibility on the right side by adding a plain vertical `Visible destination list` above the horizontal route strip, so destination choice remains readable even if the strip layout feels unclear or renders badly on a given window size.
- Improved Step 2 layout ergonomics after it still felt too cramped: once a train is selected, the long Step 1 departure list now collapses into a compact selected-train summary with a `Choose Another Train` button, and the right-side pane automatically reallocates more vertical space to the action area.
- Added a GitHub-facing local deployment guide: created `DEPLOYMENT.md` with clone, Python/`tkinter`, launch, troubleshooting, and desktop-entry notes, and updated `README.md` so outside testers can find the local playtest path more easily.
- Fixed the core planning limitation that made the client feel like it could only plan one train at a time: during `PLANNING`, the action UI now follows a plan cursor based on the end of the currently planned route rather than only the live current state, and the plan board now shows that cursor location explicitly.
- Upgraded the upper-right plan area from a plain text dump to a route-board style card list, so each planned leg now reads as its own `from -> train -> to` block and chained plans are easier to scan during playtests.
- Made the plan board much more visually explicit after Scorp still could not find it: the plan board now sits at the very top of the upper-right info area, uses a colored header band, and the top info section gets more vertical space by default so the board is harder to miss.
- Fixed a planning-state bug that caused chained plans to collapse back down to a single visible leg: in `PLANNING`, appending a new train segment no longer trims away the earlier unexecuted plan history, so multiple planned cars now remain on the board together.
- Removed the native client's `MATCH TABLE` and `IMMEDIATE OPTIONS` panels entirely to simplify the right side and leave more room for the plan board, action workflow, and replay surfaces.
- Changed the plan board from a scroll-body card into a fixed top-right region so long Step 1 / Step 2 content below can no longer cover or push the plan board out of sight.
- Fixed the right-side planning scroll behavior after Scorp reported that manually scrolling down through `Choose Your Train` would jump back up on its own: the client now preserves the right-panel scroll position across ordinary renders and only auto-jumps when intentionally switching focus between Step 1 and Step 2.
- Made live player positions much easier to read on the map after Scorp asked for explicit in-game visualization: instead of tiny station-adjacent dots, the map now draws larger labeled player markers with callout boxes that show who the player is and which station or train-anchor they are currently at during live play and replay.
- Improved on-train visualization after Scorp pointed out that showing only boarding/alighting anchors was not enough: when a player is on a train, the local client now interpolates the marker position between consecutive stations based on the real timetable minute, so the piece visibly travels along the loop instead of snapping only to endpoints.
- Reduced right-side flicker after Scorp asked whether that area could stop flashing: the fixed `PLAN BOARD` now caches a render signature and skips full widget teardown/rebuild when its visible content has not actually changed, instead of recreating the whole board on every clock tick.
- Continued the anti-flicker pass on the right side after Scorp agreed to the “second cut”: the Step 1 / Step 2 action area now uses a content-based render signature derived from actual departure/destination options instead of the raw current minute, and minute-sensitive helper text was simplified so the panel no longer tears itself down every second just to update cosmetic timestamps.
- Built a new browser client directly from the current local playtest flow instead of reusing the older web planner: added `ui/web_client.html` with the same left-map / fixed right plan board / live planning clock structure, wired it to the real Yamanote station and train-instance datasets, added `scripts/dev/build_web_client_bundle.py` to generate a publishable `docs/` bundle, and added a GitHub Pages workflow so pushes to `main` can produce an online playable build.
- After Scorp said the online version was still not visible and required ongoing parity with local playtests, promoted a new axiom that the local and online primary clients must stay aligned to the same gameplay flow, and updated deployment guidance to explicitly call out the likely one-time GitHub Pages requirement: `Settings -> Pages -> Build and deployment -> GitHub Actions`, plus checking the `Deploy Web Client` Actions run on `main`.
- After Scorp asked to try deployment again, tightened the GitHub Pages workflow by adding the standard `actions/configure-pages@v5` step before building/uploading the `docs/` bundle, then triggered another deployment push so the repository can attempt a fresh Pages publish with the more canonical workflow shape.
- After the public web page finally came up, captured the first explicit hunter-mode design baseline in `HUNTER_MODE_PLAN.md`: during `PLANNING`, the hunter sees the runner exactly at station or abstractly `on the line` when riding; during `LIVE`, the hunter sees nothing; hunter movement operations stay symmetric with runner movement; and the first dedicated hunter test loop assumes a non-moving runner so hunter-side usability can be isolated first.
- Implemented the first hunter-mode visibility pass in both the native client and the browser client: when the hunter is active during `PLANNING`, a runner at station is shown exactly, a runner on train is reduced to the abstract state `On the Yamanote Line`, and during `LIVE` the runner is hidden again; in the same pass, runner plan traces are no longer leaked to the hunter map view.
- Added two dedicated stationary-runner hunter test scenarios under `data/scenarios/` so the first hunter-mode playtests can focus on interception flow rather than runner-side movement.
- Tightened the hunter-side wording after Scorp clarified that the client should not explicitly tell the hunter which line the runner is on: both clients now expose only a rough between-stations location such as `Between Tokyo and Ueno`, while the map position itself carries the rest of the clue.
- Tightened hunter visibility one step further after Scorp clarified that even between-station wording was too explicit: when the runner is riding, hunter-side UI now uses map-only position cues with no textual route hint, and `SCHEMA.md` now explicitly includes a future `MapGeometryBundle` layer for real station coordinates and line polylines.
- Added `ROADMAP.md` to lock the current product sequence to `v1 Yamanote`, `v2 full Shinkansen map`, and `v3 full Tokyo map`, so near-term work stays focused on validating the first Yamanote gameplay loop before scaling up.
- Added a `Start Game` control to both the native client and the browser client so playtests can end planning early and begin `LIVE` immediately, while clearly labeling this as a temporary single-player convenience rather than the final “both players agree to start” multiplayer rule.
- Updated test preset behavior so it now depends on the currently selected side: runner playtests keep the original `06:00 -> 07:00` window, while hunter playtests now use `07:00 -> 08:00` to avoid being cut off at `07:00`.
- Fixed the actual hunter preset control logic after Scorp noticed the hunter plan board was still filled by a hardcoded wait: in hunter playtests, the runner is now the passive waiting side and the hunter starts with an empty actionable plan in both clients.
- Fixed a loop-line map-selection bug in the native client: while planning on a train, map clicks for alighting now reuse the same downstream destination set as the right-side picker, so stations such as `TOKYO` are no longer incorrectly blocked by a hardcoded “same as board stop” guard.
- Fixed the more direct interaction-layer cause Scorp identified: player markers could physically cover station symbols and make them hard to click. The native client now uses a larger station hit radius plus small station-local marker offsets, while the browser client makes player overlays non-interactive so station clicks pass through.
- Added explicit live capture ending to both primary clients so hunter-mode playtests can verify catch judgement in real time: during `LIVE`, both clients now check the same minimal `same_train / same_node` conditions as the engine, immediately switch to `ENDED` on capture, and draw a clear map-side capture mark instead of silently continuing after the hunter has already caught the runner.
- Expanded that new live capture ending with a clearer “capture explanation” layer in both clients: the native client now fills the result/detail cards with capture time, rule, and both player states at the moment of capture, while the browser client now surfaces the same explanation in the info panel and quickbar so hunter-mode tests are easier to read without digging through raw logs.
- After the first stable `v1` self-test pass, started the visual preparation for `v2` by drawing a nationwide Shinkansen schematic draft in `visuals/shinkansen_v2_map_draft.svg`, laying out the first multi-line strategic bridge between the Yamanote-only prototype and the later full Tokyo network.
- Reworked that first `v2` map draft after it was judged too abstract: the Shinkansen visual in `visuals/shinkansen_v2_map_draft.svg` is now redrawn around the current real open network with real line names and current stations across the JR East, JR Hokkaido, JR Central, JR West, and JR Kyushu Shinkansen systems, instead of mixing in future extensions or purely invented strategic geometry.
- Started the first real `v3` pilot implementation on top of the nationwide Shinkansen scope: added `V3_PILOT_BUNDLE_PLAN.md`, generated `data/v3_shinkansen_bundle.json` as the first canonical `V3TransitBundle`, and rendered `visuals/v3_shinkansen_multiscale_map.svg` as the first dual-view `corridor / service` map visualization.
- Added the first browser-facing `v3` page in `ui/v3_web_client.html` and `docs/v3.html`, letting the first nationwide Shinkansen `V3TransitBundle` render as an interactive corridor/service map instead of a static SVG only.
- Extended that first `v3` web map with route-focused interaction: route cards and legend chips now select and highlight one service family across the map, selected-route stations gain their own emphasis, and label density reacts more strongly to zoom plus route context.
- Merged that route-focused interaction back into the `v2` nationwide Shinkansen web client: `ROUTE FOCUS` now exists there too, route cards and map polylines can drive route selection, and selected-route stations/labels gain their own visual emphasis on the playable `v2` map.
- Added the next `v3` GIS-first layer: exported standard GeoJSON sources (`stations`, `track_centerlines`, `service_paths`), generated a first tile-ready GeoJSON pyramid under `docs/data/v3_tiles/`, and extended `v3` from map-only interaction to a route timetable diagram driven by the same `V3TransitBundle`.
- Took the next step from “tile-ready” to “tile-driven”: the `v3` browser map now loads `stations / track_centerlines / service_paths` from the generated tile pyramid according to current viewport and zoom, while still sharing the same bundle-driven timetable diagram below the map.
- Continued refining the real-network `v2` Shinkansen draft after line-by-line feedback: fixed the Akita branch to split correctly from Morioka, cleaned up the Hokuriku continuation so it no longer visually stops at Toyama, repositioned the Kyushu / Nishi-Kyushu section to better reflect western Kyushu, and added a clearer connection cue from the Nishi-Kyushu branch back toward the main Kyushu Shinkansen side.
- Backfilled `DIARY-2026-04-03.md`, created `MEMORY-2026-04-04.md`, and recorded that there is currently no usable Notion integration or script in this repository/session, so a true Notion update is blocked until that tooling exists.
- Further cleaned the `v2` Shinkansen visual after Scorp pointed out readability issues: removed the gray helper lines, separated the Joetsu and Hokuriku corridors so they no longer ride on top of each other near Takasaki/Niigata, and re-laid the Yamagata branch from Fukushima so it no longer intersects the Akita branch.
- Continued tightening the `v2` Shinkansen draft around true network structure rather than just readability: the Joetsu line was moved to the upper side of Hokuriku, the Yamagata branch was kept clear of the Hokuriku corridor, and the Tohoku main line was extended all the way to Shin-Aomori so the Hokkaido branch now visually connects through the proper trunk instead of feeling like a detached segment.
- Chose `Plan A` as the formal `v2` map direction: stop treating the hand-edited nationwide SVG as the main source of truth and instead move to a geometry-driven pipeline based on real station coordinates plus real route order. Added `V2_GEOMETRY_PLAN.md` to define the datasets, renderer, cleanup rules, and migration path.
- Implemented the first actual `Plan A` artifact: added `scripts/dev/render_shinkansen_v2_from_geometry.py` and used it to generate `data/shinkansen_v2_stations.json`, `data/shinkansen_v2_routes.json`, and `visuals/shinkansen_v2_map_real_geometry.svg`, so `v2` now has a data-driven geometry seed rather than only a hand-edited draft SVG.
- Continued the `Plan A` groundwork by making the `v2` pipeline explicitly `lat/lon-ready`: `data/shinkansen_v2_stations.json` now includes `lat` and `lon` fields, and `scripts/dev/render_shinkansen_v2_from_geometry.py` now prefers projected real coordinates when available while still falling back to the current `geometry_seed` values.
- Connected a public Japan railway station dataset to the `v2` geometry pipeline and completed the first real coordinate enrichment pass: `101` of the `119` Shinkansen-map stations now carry actual `lat/lon` values, the generated map has been rerendered from those coordinates, and the enrichment logic now explicitly handles same-name collisions such as Omiya, Fukushima, Koriyama, Fukui, Kokura, and Nagasaki instead of silently picking the wrong prefecture.
- After noticing that the partly-real map still looked warped in places, fixed the deeper cause by adding `scripts/dev/estimate_shinkansen_v2_missing_latlon.py`: instead of mixing true geography with old hand-drawn seed points, the remaining unresolved stations now receive line-neighbor interpolated coordinates so the map is rendered from one consistent spatial system. At this point only `GALA_YUZAWA` remains without a resolved position.
- Completed the next `v2` coordinate cleanup pass by replacing `GALA_YUZAWA` and the remaining interpolated stations with precise points: the Shinkansen geometry dataset now has `119 / 119` positioned stations, with the coordinate status reduced to `100 real + 19 manual precise` and no remaining `estimated` or missing station positions.
- Removed the last `manual precise` fallback bucket from `v2`: added a cached `ja.wikipedia.org` station-coordinate source for the remaining Shinkansen-only stations, updated the enrichment pipeline to use it as a secondary public source, and brought the dataset to `119 / 119 real` positions sourced from `100` primary-source points plus `19` Japanese Wikipedia station-page coordinates.
- Fixed two remaining obvious same-name coordinate errors on the `v2` map after visual inspection: `SHIN_FUJI` on `TOKAIDO` was corrected from the unrelated Hokkaido `新富士` to the Shizuoka station, and `SENDAI_KYUSHU` on `KYUSHU` was corrected from the wrong `川内` to Kagoshima `川内`.
- Gave the `v2` data-driven Shinkansen map its first real visual polish pass: updated the renderer to add white route halos, colored route-name pills, stronger station-label halos, and a first round of hub-oriented label offsets so the map reads less like a raw debug artifact and more like a deliberate transport-game board.
- Followed that first polish pass with automatic label placement for the `v2` map: station names now choose among multiple candidate offsets and avoid already-placed labels, route-name pills, and nearby station circles instead of relying only on a small hand-written exception list.
- Extended the same avoidance logic to route-name pills on the `v2` map: line labels now choose among several nearby candidate positions and try to stay clear of other line labels, station names, and station circles instead of being pinned to a single static point.
- Rebalanced the `GALA Yuzawa` segment so it no longer reads like a full nationwide peer line on the `v2` map: the renderer now presents it as a subdued `Joetsu branch` label with weaker visual weight than the main Shinkansen trunks.
- Wrote `V2_TIMETABLE_PLAN.md` to start the next `v2` phase: the Shinkansen version will cover all trains on the map, use real timetables, and preserve real train identities such as `Nozomi 1` and `Kagayaki 503` instead of anonymous internal train IDs.
- Added `data/shinkansen_v2_source_inventory.json` as the first real timetable-ingestion inventory for `v2`, mapping official source entry points, route-family ownership, and operator-specific ingestion constraints across JR East, JR Central, JR West, and JR Kyushu.
- Started the first nationwide `v2` train-data build pass instead of only planning it: added official-train parsers/builders for JR West and JR Kyushu station timetables, fixed Japanese station-name normalization in the shared train-instance normalizer, and proved that JR West and JR Kyushu train-detail pages can be transformed into canonical `train_instances` while preserving real service names and numbers.
- Built and validated new `v2` source datasets from official station timetable pages: JR West `Shin-Osaka` eastbound and westbound, JR Kyushu `Hakata` and `Kagoshima-Chuo` for Kyushu Shinkansen, and `Takeo-Onsen` plus `Nagasaki` for Nishi-Kyushu. Merged with the previously collected JR East datasets, the nationwide weekday `v2` dataset first reached `1009` real train instances.
- Continued the `v2` nationwide coverage pass by adding missing reverse-direction entry points that were clearly absent in the merged first-stop distribution: JR East `Niigata` inbound for Joetsu plus JR East `Nagano` inbound/outbound and JR West `Tsuruga` inbound for Hokuriku. After remerging, the nationwide weekday dataset increased to `1119` validated train instances.
- Added `scripts/dev/render_shinkansen_v2_timetable_svg.py` and generated `visuals/shinkansen_v2_weekday_timetable.svg`, a multi-panel weekday timetable visualization that shows `v2` train density by real route family instead of forcing the whole nationwide network into a single unreadable time-space diagram.
- Normalized remaining mixed-language Shinkansen service names in the merged `v2` dataset by adding `つるぎ -> Tsurugi` to the merge/parser alias tables, so the nationwide bundle now presents player-facing train families consistently as romanized real service names such as `Nozomi`, `Kagayaki`, `Hakutaka`, `Kamome`, and `Tsurugi`.
- Broke through the JR Central blocker for `v2`: instead of relying on the insufficient English timetable page, discovered that each JR Central Shinkansen station-guide timetable page exposes a `ResultControl?st=...` JSON API that leads to official `tokainr.cgi` station timetable pages. Used this path to scrape all 17 Tokaido Shinkansen stations and aggregate station-departure grids into a new supplemental dataset of `43` real mid-origin / short-turn Tokaido services.
- Merged the new JR Central Tokaido supplemental dataset into the nationwide weekday bundle, increasing `data/shinkansen_v2_weekday_train_instances_merged.json` from `1119` to `1162` real train instances and improving the first-stop distribution with new starts at `Nagoya`, `Mishima`, `Shin-Yokohama`, `Shinagawa`, `Kyoto`, `Shizuoka`, `Hamamatsu`, and `Gifu-Hashima`.
- Updated the `v2` source inventory to reflect the real JR Central ingestion path: the project no longer treats JR Central as “basic timetable only,” but records the proven `station-guide -> ResultControl -> tokainr.cgi` chain as an official station-timetable source for Tokaido supplementation.

## 2026-04-05

- Continued the `v2` nationwide Shinkansen ingestion pass by investigating an obvious `JR Kyushu / Nishi-Kyushu` defect: official `Kamome` detail pages at `Takeo-Onsen` are dual-column pages that show both `Relay Kamome` and `Kamome`, and the old parser was incorrectly reading the left relay-service column instead of the actual Shinkansen column.
- Reworked `scripts/ingest/parse_jrkyushu_train_timetable.py` so JR Kyushu detail parsing now selects the target service column by line and service type, including proper right-column selection for `SHINKANSEN_NISHI_KYUSHU` pages.
- Rebuilt the official `Nishi-Kyushu` source datasets from `Takeo-Onsen` and `Nagasaki`, confirming that outbound `Kamome` trains now correctly run `Takeo-Onsen -> Nagasaki` instead of being silently reversed.
- Re-merged the nationwide weekday `v2` train-instance bundle after the parser fix; `data/shinkansen_v2_weekday_train_instances_merged.json` now contains `1180` validated real Shinkansen train instances.
- Verified that `Kamome` services now survive the national merge in both directions, with `22` starts from `Takeo-Onsen` and `23` starts from `Nagasaki`, and rerendered `visuals/shinkansen_v2_weekday_timetable.svg` to reflect the corrected nationwide timetable.
- Continued the nationwide `v2` data audit and found a second systemic issue: cross-operator duplicates were still surviving because the merge step treated synthetic `train_number` values from supplements as canonical instead of recognizing the same real service by its name and number.
- Updated the national merge logic to deduplicate by real service identity (`service_name + service_number`, plus direction when available), added broader Japanese-to-romanized service alias normalization for lines such as `Kagayaki`, `Hakutaka`, `Asama`, `Toki`, `Tanigawa`, `Hayabusa`, `Yamabiko`, `Nasuno`, `Komachi`, and `Tsubasa`, and reran the national merge.
- Tightened the merge key one step further so synthetic supplement direction tags no longer prevent true cross-operator service merges, then reran the audit: the nationwide weekday Shinkansen dataset now stabilizes at `1139` validated train instances.
- Spot-checked the remaining suspicious short services and found no new blocking integrity issues: `JR Kyushu` short `Tsubame` and `Kamome` services are backed by official train-detail pages, while the formerly suspicious short `Nozomi` supplement rows are now correctly absorbed into the longer through-service records during merge.
- Started the actual `v2` game layer instead of only the data layer: added `scripts/ingest/build_shinkansen_v2_bundle.py` to assemble stations, routes, adjacency, interchange flags, route summaries, and the validated nationwide train dataset into one reusable `data/shinkansen_v2_bundle.json`.
- Added the first `v2` local playable shell in `app/v2_local_client.py`: it renders the nationwide Shinkansen board from real station coordinates, lets the user click stations, browse real upcoming departures after the current cursor time, preview full remaining stop lists, and chain route legs into a first plan board.
- Added `START_ONICHASE_V2_CLIENT.sh` and `V2_LOCAL_CLIENT.md`, establishing the first dedicated launch path and scope document for `v2` gameplay work.
- Promoted that first `v2` shell onto GitHub Pages: added `ui/v2_web_client.html`, switched the static bundle builder to publish the nationwide Shinkansen client instead of the old Yamanote page, and updated `docs/` so the online version now mirrors the same `v2` click-station / browse-real-departures / append-plan-leg loop as the local shell.
- Upgraded both the local and web `v2` shells from “select a train and automatically ride to the terminal stop” to the first real routing flow: after choosing a departure, the player now picks one downstream stop as the destination leg before appending it to the `plan board`.
- Reworked the `v2` local client from a route-browser shell into a real match shell with `runner / hunter` modes, `PLANNING / LIVE / ENDED` flow, manual start, live player markers on the nationwide map, live capture checks, simulation results, and a replay event list.
- Pulled the `v2` web client onto the same main gameplay flow: it now includes `runner / hunter` mode switching, a visible phase clock, manual match start, live player positions on the nationwide map, and plan-step-based nationwide Shinkansen routing instead of only passive timetable browsing.

## 2026-04-06

- Backfilled `diary/DIARY-2026-04-05.md` to keep the daily diary chain continuous.
- Created `memory/MEMORY-2026-04-06.md` and recorded the daily sync kickoff for the new day.
- Continued `v2` web parity work by adding a simulator-backed result summary, replay event list, replay detail panel, and replay-driven map-state jumping to the online nationwide Shinkansen client.
- Split the GitHub Pages entry flow into three pages: a new landing page at `docs/index.html`, plus dedicated `docs/v1.html` and `docs/v2.html` playtest pages, and updated `README.md` so the repository homepage links directly to all three public URLs.
- Normalized `v2` right-panel train labels to English-first display on both the local and web clients, so timetable choices now show service names like `Nozomi 1` and `Kagayaki 503` instead of raw Japanese display names.
- Reworked the `v2` web layout into a fixed-viewport playtest screen: the page itself no longer grows vertically, the map panel now supports zoom and drag, station names reveal progressively based on zoom level, and the sidebar modules scroll inside their own regions instead of forcing the whole page to scroll.
- Followed up on the `v2` web map interaction pass: station dots now scale more gracefully with zoom, and panning limits are computed from the actual map content bounds so zoomed-in boards can still be dragged fully to the edges.
- Reopened the `v2` fixed-layout sidebar after it became too constrained: the right column now scrolls as a whole again while list regions remain scrollable, restoring normal interaction with lower planning and result modules.
- Replaced the failing `v2` web right-side departures/destination dual-list flow with the same unified planning logic used by `v1`: a single `Planning Actions` region now drives `choose train -> choose destination`, the `Train Outlook` panel previews the selected service, and clicking highlighted stations on the map now follows the same pending-train / onboard destination rules as the action panel.
- Refined the `v2` web board readability after the planning-flow switch: reduced station-label size and added direct map highlighting for every downstream stop of the currently selected train, so route choice is legible without relying only on the sidebar.
- Formally started the `v3` track by adding `V3_GIS_ARCHITECTURE.md` and `V3_GIS_SCHEMA.md`, defining OniChase's GIS-first Tokyo direction: physical network layer, service operation layer, station-group abstraction, multi-scale presentation, and the first canonical `V3TransitBundle` model.
- Realigned `v3` immediately after that first draft: `v3` is no longer the first Tokyo full-map version, but the GIS-first upgrade of the nationwide Shinkansen version. Added `V3_SHINKANSEN_SCOPE.md` and updated the roadmap and GIS docs so the architecture track now cleanly targets the same Shinkansen network as `v2`.
